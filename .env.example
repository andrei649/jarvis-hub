# Jarvis — Cloud LLM Credentials
# Rename to .env and fill in your keys.
# Cloud LLM is used as fallback for heavy reasoning by approved agents:
#   jarvis, athena, stark, vision, veronica

# !!! NEVER commit .env to git !!!

# Anthropic Claude (recommended — model: claude-sonnet-4-20250514)
ANTHROPIC_API_KEY=

# OpenAI GPT (alternative — model: gpt-4o)
OPENAI_API_KEY=
