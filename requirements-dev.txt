# Development & code-health tooling for Jarvis Hub.
# Install:  pip install -r requirements-dev.txt
# Run the full analysis:  python scripts/code_health.py
#
# Runtime + test deps live in requirements-beta.txt; this file is only the
# static-analysis toolchain so CI and contributors share identical versions.
#
# AUD-10 (F33): editable source. CI installs from the hash-pinned
# requirements-dev.lock; regenerate it with ./scripts/lock_deps.sh after edits.

ruff>=0.16.0
vulture>=2.16
