# Hermes Agent and the 2026 field — gap matrix for Nerva (research pass 2026-09-05/06)

> **Immutable research snapshot.** Five online researchers (Hermes primary sources, computer-use products, local automation building blocks, 24×7 goal-driven agents, adoption playbooks) plus one synthesis, read on 2026-09-05. Every claim carries a source URL and a confidence grade; contradictions and unverifiable claims are listed at the end. This document feeds [`docs/NERVA_2_ROADMAP.md`](../NERVA_2_ROADMAP.md) and is provenance, not current truth — do not "fix" its numbers.

## 1. Gap matrix

| Capability | Hermes | Best in field | Nerva (v1.0.0) | Verdict |
|---|---|---|---|---|
| Desktop control — Windows | Background computer_use via cua-driver over MCP stdio: UIAutomation tree + SendInput/PostMessage ('no focus steal'), SoM/ax capture, pixel fallback; off by default, destructive actions need approval, hard-blocked combos; cannot reach High-integrity windows (v0.20.3 'Cua Driver 0.20 contracts', v0.21.0 Windows destructive-command approvals). | Microsoft: Windows agent workspace (separate Windows session + agent account, per-folder Allow Always/Ask/Never) and UFO² hybrid UIA+vision with PiP virtual desktop (-51% LLM calls); windows-mcp-server (a11y-tree Snapshot, 'Prefer Invoke over Click', hash-chained audit, kill switch); Claude Cowork per-app permission + app blocklist; ChatGPT Computer Use foreground-only on Windows. | WindowsDesktopDriver (pywinauto/UIA) behind owner seam JARVIS_DESKTOP_HOST + JARVIS_DESKTOP_ISOLATED; GovernedDesktop (observe inline / mutate → approval / regex injection classifier); operator_router API→CLI→STRUCTURED_UI→VISUAL selects but registers no UI/VISUAL executor; no per-app permission ledger, no UIPI honest refusal, pywinauto unreleased since 2025-01. | **behind** |
| Desktop control — macOS | AX tree via private SkyLight SPIs + SLPSPostEventRecordTo (pid-scoped, no cursor warp), ~5–20 ms background events, stable TCC signing in v0.21.0; Apple 'can change them in any OS update'. | Claude Cowork (background windows on macOS 15+, per-app permission, banking/crypto off by default, isolated built-in browser 2026-08-26); ChatGPT 'locked use'; Perplexity Personal Computer (always-on Mac mini, approve from iPhone); Apple Siri AI intent-first via App Intents (2026-06-08); open a11y wrappers pyobjc 12.2.2 / pyax 0.3.4 (MIT). | No macOS driver at all (Windows-only seam); Tauri shell exists but no AX/CGEvent adapter, no TCC preflight. | **behind** |
| Desktop control — Linux | AT-SPI (X11 + Wayland) tree + XTest (X11) / virtual-keyboard (Wayland); Wayland capture needs XWayland; Linux is Tier-1 install target (desktop app macOS/Linux/Windows since v2026.6.5). | XDG RemoteDesktop portal v2 + libei (consent dialog, persist_mode/restore_token since portal 1.21, python-libei 0.4.0 MIT 2026-09-03); wdotool; Anthropic computer-use-demo Docker X11 reference; OSWorld harness (pyautogui in VM). | None; Pillow ImageGrab shells out to grim/spectacle on Wayland silently; no AT-SPI/libei adapter; docker execution target is the only Linux 'hands'. | **behind** |
| Browser automation | 11 browser_* tools incl. _snapshot (a11y), _vision, _console, _cdp raw passthrough, _dialog; engines Browser Use CLI 3.0 (default), Lightpanda, local Chromium, Camofox, cloud Browserbase/Firecrawl/Nous gateway; LAN/loopback URLs auto-route to local Chromium sidecar; consent-gated real-profile snapshot (v0.20.6); desktop app drives in-app browser (v0.21.0); Playwright installed by installer. | Anthropic browser_toolset_20260801 (31 tools, a11y refs [ref_N] + coordinates, network-layer allowlist with redirect re-check, private-range block, JS/upload/console disabled by default); Claude in Chrome GA with per-action classifier (0.3% ASR Fable 5); browser-use (112k stars, allowed/prohibited_domains, Online-Mind2Web 97% cloud); Skyvern code caching replay; Playwright MCP a11y-snapshot default + --allowed-origins; Chrome auto browse takeover; WebMCP origin trial. | Governed Playwright browser behind JARVIS_PLAYWRIGHT_HOST with URL-string allowlist/SSRF checks and approval gates; no aria-snapshot observation, no proxy-level egress enforcement, no CDP tool, no real-profile snapshot, no LAN sidecar routing, no replay. | **behind** |
| Terminal targets (local/docker/ssh/cloud) | 7 backends: local, docker, ssh, modal, daytona, vercel_sandbox, singularity; serverless hibernate/wake; docker --cap-drop ALL, no-new-privileges, pids-limit; dangerous-command check runs on local/ssh but is skipped on container/cloud ('container is boundary'). | Claude Managed Agents (stateless harness, session event log, credentials never in sandbox, deferred provisioning); OpenAI Agents SDK SandboxAgent; OpenHands ACP 'run anywhere' backends; Devin cloud with ACU caps; Anthropic containment (Seatbelt/bubblewrap + approval). | execution.py: docker is the only wired backend; local and ssh return explicit local_transport_not_implemented / ssh_transport_not_implemented; policy plane (targets.py allow/approval_required/deny + hash-chained audit) already exists; no cloud/serverless target. | **behind** |
| Visual grounding | SoM numbered overlay + 'ax' tree capture via cua-driver; pixel coordinates as fallback; vision via auxiliary model (cloud or local). | Open-weight local grounders: Qwen3.8-27B (Apache-2.0, OSWorld-Verified 84.3 self-reported, 16–18 GB local), Holo-3.1-35B-A3B Q4 GGUF (Apache-2.0, 21.3 GB), Qwen3-VL-4B/8B, UI-TARS-1.5-7B, OmniParser v3 detector (MIT); frontier Claude/Qwen3.8-Max ~85–86% OSWorld-Verified vs 72.4% human; Anthropic zoom tool + 1,000–1,800 tokens/screenshot guidance; UFO² hybrid UIA+vision. | screen_grounding.py parse_grounding + fuse_with_a11y and llm/vlm.py (JARVIS_VLM_BACKEND=lmstudio\|custom, pinned JARVIS_VLM_MODEL) exist; only '(x, y)' convention parsed; no preset table for open grounders; VISUAL route has no registered executor; no SoM pre-processor. | **behind** |
| Skill self-creation | skill_manage create/patch/edit/delete/write_file/remove_file; system-prompt nudge to record workflows; background self-improvement review on auxiliary model; /learn distils corpora into knowledge-base skills; progressive disclosure L0 ≈3k tokens; optional skills.write_approval staging; official GEPA/DSPy self-evolution repo (phase 1, $2–10/run, human review). | Hermes is the leader; Claude Code desktop scheduled tasks store prompts as SKILL.md; Gemini Spark 'teachable skills'; Vercel skills.sh open ecosystem (agentskills.io standard). | Governed capability-acquisition loop (gap → research → generate → sandbox → approve → registry), CDX-8 PENDING_REVIEW quarantine, signing, 78 pinned Hermes skills, importer; no patch-level refinement, no post-session review, no /learn, no progressive disclosure, no evolutionary optimisation. | **behind** |
| Memory | MEMORY.md (~800 tokens) + USER.md (~500 tokens) frozen snapshot in system prompt; FTS5 session_search; memory.write_approval; 8 provider plugins (Honcho, Mem0, Supermemory…) one active; /journey timeline; cron persistent memory + notepad (v0.21.0). | Claude Managed Agents 'Dreams' (offline consolidation into a new store, input never modified, reviewable); Honcho cross-session user modeling; Windows Recall (enclave, opt-in, disputed); Rewind/Limitless killed on consent grounds. | Conversation + vector store + bi-temporal KG with RRF fusion, ingestion pipeline (Facebook/WhatsApp), digital twin (Howard), living memory + persona layer, nightly reflection/consolidation, preference learning; no candidate-store promotion, no explicit prompt-cache-friendly frozen snapshot. | **ahead** |
| Scheduling / cron | cronjob tool (NL + cron + ISO), continuity=true, context_from, no_agent script-only, wakeAgent pre-run gate at $0, monitor-mode skips LLM when unchanged, incidents keyed by error signature, blocked_config pre-run validation, model drift guard, executions.db ledger, cron cannot create cron, cron_mode deny\|approve, 60 s tick. | Claude Managed Agents scheduled deployments (per-run budget copied to each session, budget_reached pause, no backfill, jitter); Claude Code routines (untrusted fire payload, 'green ≠ success'); Devin automations (ACU cap, invocations/hour, queue depth, long-running session receives triggers); Claude desktop tasks (one catch-up run, 1-min interval); Manus same-thread scheduled tasks. | SQLite task queue + worker + risk policy, observers/watchers, night shift reversible-only window, morning brief/evening retro, nightly reflection, interrupt budget ≤4/day; no continuity/context_from, no incident dedup, no pre-run validation, no per-run budget or invocation cap, no missed-run policy, no drift guard. | **behind** |
| Messaging gateways | 27 platforms in one gateway process (Telegram, Discord, Slack, WhatsApp incl. Cloud API, Signal, iMessage via Photon/BlueBubbles, Matrix, Teams, LINE, ntfy, IRC, HA…); allowlists or DM pairing (8-char code, 1 h TTL, lockout); approvals via chat buttons, 300 s fail-closed timeout; multi-profile routing; durable delivery ledger; scale-to-zero. | OpenClaw (WhatsApp/Telegram/Slack/Discord/Google Chat/Signal/iMessage, pairing default; but unofficial WhatsApp bans); Poke (no app, phone number → iMessage/SMS, first AI agent on Apple Messages for Business 2026-06-04); a16z: consumers reach agents by messaging. | Telegram/Discord/Slack/email/voice channels with decision inbox on Telegram; owner bound via static allowed_user_ids; governed SenderPairing (H12.19) exists but default-off behind JARVIS_CHANNEL_PAIRING; no delivery ledger, no iMessage-class channel. | **behind** |
| Subagents | delegate_task with fresh context, list/steer/stop children, JSON-schema-validated outputs, per-delegation cost, leaf-tool restrictions, background fan-out, tail -f transcripts; defaults raised to 250 iterations / 10 concurrent (v0.21.0); Bot Mode named agents, message_agent, bounded group rounds (3 rounds/10 msgs), hermes peer cross-machine, A2A v1.0. | Claude Code agent teams (shared task list, file locks, teammate cannot supply consent, TaskCompleted veto hooks; warns against unattended long runs); Anthropic 16-agent C compiler (task-lock files, tests as oracle); Microsoft Agent Framework Magentic (progress ledger, max_stall/reset, plan review); OpenAI Agents SDK max_turns + approvals. | 18 specialist agents + orchestrator, ToolRPC, model-directed tool loop (default-off), swarm router with dev locks, BudgetLedger + LoopDetector, A2A module; no mid-flight steer/stop, no typed child outputs, no company room/roster surface, no cross-node peer. | **behind** |
| Context compression | Auto-compression at 50% + gateway safety net at 85%; protected head + protect_last_n tail; iterative re-summarization; lean/legacy modes; per-model thresholds; pluggable ContextEngine ABC (incl. lossless); session lineage; prompt-cache-friendly memory snapshot; /context meter; strict redaction at compaction boundaries. | Hermes is the most documented; Anthropic guidance on screenshot pruning (~every 25 turns) and cache breakpoints; Managed Agents sessions exceeding context via event log; Claude Code /compact. | context_compressor.py exists per report references, but no documented two-tier policy, lineage, protected head, per-model thresholds or cache-aware snapshot; local 32–128k windows at risk in 24/7 runs. | **behind** |
| MCP | Native client (stdio, HTTP, OAuth PKCE/refresh/step-up), hermes mcp serve exposes Hermes as MCP server, curated catalog 50+ vendors disabled by default, stdio env stripping, sampling/elicitation gated, Unicode TAG stripping, MCP 2.x SDK / 2026-07-28 protocol, lazy startup, health checks + usage overlay. | Windows agent connectors (MCP servers) + announced Windows MCP registry; Playwright MCP; UFO³ MCP; Anthropic MCP in Cowork; Claude Code routines include all connectors by default (a risk). | MCP support present per context (mcp.mutating kind referenced by reports) and ToolRPC; no MCP server exposure of the governed operator, no catalog, no stdio env isolation documented. | **behind** |
| Local models | ~50 providers; Ollama zero-config, vLLM, SGLang, llama.cpp, LM Studio, LiteLLM, any OpenAI-compatible endpoint with auto-select; auxiliary model for vision/summaries; fallback chains; local voice (faster-whisper, NeuTTS); but golden path is Nous Portal paid subscription + Tool Gateway. | Ollama (180k stars, install.sh with GPU detection, loopback default) and LM Studio (GUI, OpenAI+Anthropic-compatible API on :1234) as runtimes; NVIDIA NemoClaw bundles OpenShell + Nemotron for OpenClaw/Hermes; open grounders Qwen3.8-27B/Holo3.1 run locally. | Local-first by design: LM Studio/Ollama default, cloud opt-in per agent, %-local north-star metric, LOCAL_ONLY posture monitor; no local-model inventory-driven installer tiering yet, no drift guard for unattended jobs. | **ahead** |
| Approval / safety | approvals.mode smart (LLM reviewer auto-approves low risk) / manual / off (--yolo); cron/unattended modes deny\|approve; hardline blocklist even under yolo; deny fnmatch rules; protected paths; SSRF guard; tirith pre-exec scanner (fail-open); context-file injection scanning; protected agent-instruction files require write approval (v0.21.0); computer_use destructive actions need approval with hard-blocked combos; no per-kind contracts, no taint escalation, container backends skip checks. | Deterministic-first: Claude Code auto mode (tiers + classifier, 'prompt is the only authority', misses 17%); OpenAI Auto-review (reviewer agent, sandbox independent of approval); universal consequential-action confirmation + banking/crypto blocked; Cowork per-app permission; OpenClaw exec approvals (deny/allowlist/ask/auto/full, ask fallback deny); OpenHands ConfirmRisky + ensemble analyzers + stuck detector; Copilot Studio warns human review is not a fail-safe. | Action Kernel with 21 privileged kinds (GRANT/QUEUE/DENY), contracts, taint escalation, risk tiers, graduated autonomy ladder, money/locks/security never above queue, kill-switch, interrupt budget; missing: per-app/site permission ledger, hardline static floor, presence gating, local injection classifier on observations, approval TTL/fail-closed timeout. | **ahead** |
| Audit | No tamper-evident action audit: issue #487 (hash-chained action log) closed 'not planned'; plain-text append_audit_log for skill ops only; cron executions.db history (v0.19.0); session export --redact; HMAC-signed outbound webhooks. | windows-mcp-server hash-chained audit 'optionally keyed and anchored off-box' + tiered kill switch; Devin screen recordings of work; Perplexity claims 'auditable and reversible' (unverified); agentguard-spend-hermes third-party Ed25519 logs. | Tamper-evident Merkle audit chain across kernel actions, verify_chain, hash-chained target audit, cortex decision records, episodes; no off-box anchoring, no shareable receipt. | **ahead** |
| House / physical | 4 HA tools (list_entities/get_state/list_services/call_service) auto-enabled with HASS_TOKEN + gateway adapter forwarding filtered state_changed events; no house model, no cameras, no device risk tiering. | Hermes' HA event adapter is the only notable competitor feature; Gemini Spark/Apple are app-centric; no vendor ships a house world model or camera-aware agent. | Home Assistant house brain with per-device risk policy (locks/security pinned to queue), Frigate cameras read-only, media director present(), world model contracts (Nerva 2.0), WorldView 4D OSINT; pull-based (no WebSocket event observer). | **ahead** |
| Install / onboarding | curl install.sh / PowerShell one-liner managing Python 3.11, Node, ripgrep, ffmpeg; desktop installers macOS/Windows; hermes setup wizard, hermes doctor, hermes import-agent (Claude Code/Codex migration); Tier-1 macOS/Windows/Linux/Docker with real-OS CI; ~4-week major cadence. | OpenClaw (~5 min to chat, Quick Start reuses detected AI access, Telegram pairing); Ollama install.sh (GPU detect, systemd, loopback); Claude Code (brew/winget/apt/dnf signed, auto-update channels, claude doctor); Poke (phone number only, no app). | install.sh/INSTALL.bat + install_smoke.py (~30 s boot + one chat turn), onboarding command-center API with wizard + honest first_actions, funnel events; but assumes git checkout + Python 3.12 on PATH, no hosted one-liner, no runtime/GPU detection, no doctor, no cask/winget/GHCR/signed Tauri; GAP-0: 24,182 visitors / 0 partners converted. | **behind** |
| Mobile | No official iOS/Android app (issue #11911 open); Termux Tier-2; third-party clients (Hermex iOS, OnDev Android) connect to self-hosted API; messaging platforms are the mobile surface. | Claude Cowork on iOS/Android + Dispatch (assign from phone, finish on desktop); Perplexity Personal Computer approve from iPhone; ChatGPT ~900M weekly actives; consumer agents reached via WhatsApp/Telegram per a16z. | Expo mobile app + Telegram decision inbox already exist; no store presence, no 'dispatch' run type framing. | **parity** |
| Ecosystem size | 241,911 stars / 49,683 forks / 760+ contributors on 2026-09-05; hub indexes '88k+ skills' across 8 sources (secondary: 90,700 across 11 registries); MCP catalog 50+ vendors; listed in NVIDIA NemoClaw and Ollama integrations; ~2,475 PRs per monthly release. | OpenClaw 389k stars / 81.7k forks (most-starred GitHub repo since 2026-03-03) but ClawHub 11.9% malicious skills wave and 40,214 exposed gateways; skills.sh ~669k skills (secondary); browser-use 112k; Ollama 180k. | 78 pinned Hermes skills, signed marketplace, importer from Hermes/OpenClaw/GitHub; 0 design partners converted (GAP-0); no listing in skills.sh/Ollama/LM Studio integrations. | **behind** |

## 2. Adoptable features, ranked (governed designs)

| # | Priority | Size | Feature | Who has it | Why it matters for Nerva | How Nerva would do it, governed |
|---|---|---|---|---|---|---|
| 1 | P0 | XL | Cross-OS accessibility-first governed desktop drivers (macOS AX via pyobjc, Linux AT-SPI + libei/XDG RemoteDesktop portal, Windows uiautomation second backend) with a host capability probe and honest-refusal contract | Hermes cua-driver (AX/UIA/AT-SPI, pid-scoped input, SoM capture); Anthropic browser_toolset a11y refs; windows-mcp-server 'Prefer Invoke over Click'; UFO² hybrid UIA+vision; pynput IS_TRUSTED / cliclick / python-libei portal consent as refusal precedents | Owner goal is 'fully control a PC/Mac/Linux like Hermes'; Nerva has only a Windows pywinauto seam and operator_router registers no UI/VISUAL executor. A11y-first perception is token-cheap (no VLM), survives layout shifts, gives the kernel nameable targets to audit, and macOS is where proprietary CUAs still only reach 31–44%. | New agents/core/host_probe.py (default-on, observe-only) reporting find_spec for pywinauto/uiautomation/pyobjc/gi/libei, AXIsProcessTrustedWithOptions(prompt=False), CGPreflightScreenCaptureAccess, XDG_SESSION_TYPE + portal RemoteDesktop version>=2, Windows TokenIntegrityLevel of target HWND; desktop_host_macos.py / desktop_host_linux.py mirroring desktop_host.py's adapter protocol behind GovernedDesktop (requires_kernel=True), default-off via JARVIS_DESKTOP_HOST=1 + JARVIS_DESKTOP_ISOLATED=1 per OS; register OperatorRoute.STRUCTURED_UI (AXPress/AXSetValue, Atspi tree, UIA Invoke) and VISUAL (CGEventPost/libei/SendInput) executors; kernel kinds under desktop.* — capture/snapshot = read-only GRANT, click/type/drag = reversible (GRANT only with seam on), key combos / focus_app on password fields / system dialogs = external → QUEUE; hardline list (lock screen, log out, empty trash, typed shell) enforced in kernel/syscalls.py not the driver; taint from browser/chat content escalates GRANT→QUEUE; refuse elevated/UIPI targets and never ship uiAccess manifest; public AX/AT-SPI/portal APIs only (no SkyLight SPIs, no KWin private EIS, no uinput); local VLM only for fallback. |
| 2 | P0 | M | Per-app / per-site permission ledger (once / session / always / never) with first-use prompt and default deny list | Claude Cowork (permission before each new app, investment/crypto blocked by default, user blocklist); ChatGPT Computer Use (per-app Always allow, revocable); Windows agent workspace (Allow Always / Ask every time / Never per folder); Manus (Allow Once / Always Allow per command); OpenClaw exec approvals ('Always Allow Here'); Copilot Studio website + process-name allowlist | The single most consistent 2026 operator UX and a direct instantiation of the graduated-autonomy ladder at app/site granularity; Nerva has a browser egress allowlist but no durable app-level grant store for the desktop driver, so every run re-asks or over-grants. | DesktopAppPolicy in desktop_operator.py keyed by process name / bundle id / window class, plus extend BrowserPolicy in browser_agent.py; grants stored in the SQLite autonomy DB with scope {once, session, always, never}, owner, timestamp, originating action id; first contact with unknown app/site → kernel QUEUE → Telegram decision-inbox card with the three choices; 'never' entries are hard-deny (not approvable); default deny list ships banking, brokerage, crypto wallets, password managers, SSO/IdP, adult, pirated; ledger visible/revocable in HUD; earned autonomy per app feeds preference learning. |
| 3 | P0 | M | SSH and local execution transports under the existing targets.py policy plane (serverless target later as opt-in plugin) | Hermes 7 backends (local/docker/ssh/modal/daytona/vercel_sandbox/singularity) with hibernate/wake; OpenHands ACP backends anywhere; Managed Agents stateless harness | execution.py refuses local and ssh with explicit not-implemented reasons; 'work like a company toward any goal' cannot reach the owner's other machines. The policy plane (allow/approval_required/deny + hash-chained audit) already exists — only the transports are missing. | Implement ssh (asyncssh, key-only, host allowlist) and local transports in agents/core/environments/execution.py consuming targets.py decisions and its hash-chained audit; kernel kind node_dispatch; ssh = external tier → QUEUE by default, local = reversible only inside a write-safe root else QUEUE; keep dangerous-command checks ON for container targets (do not copy Hermes' skip); per-target default-off flags in the registry; modal/daytona only as later opt-in plugin. |
| 4 | P0 | M | Goal contract + judge loop with deterministic gates (outcome / verification / constraints / boundaries / stop_when; verdicts done\|blocked\|continue\|wait; shell gates must exit 0; max_turns auto-pause) — fail-closed | Hermes Persistent Goals (/goal, /goal gate add, goals.max_turns=20, judge fail-open); Hermes v0.18.0 completion contracts; Anthropic harness evaluator with hard thresholds and sprint contracts | The missing 'company mode' primitive: a standing objective that keeps working across turns/sessions but can only declare success with evidence. Nerva has Mission (max_steps/max_seconds) and iteration_budget.py but no judge/contract loop; this raises the accepted-action north-star and stops premature 'done'. | Extend agents/core/autonomy/missions.py Mission with a frozen contract (5 fields) and gates list; judge = local model call recorded as a cortex decision record per verdict; gate commands run as kernel kind mission.gate at READ_ONLY tier inside the docker (later local/ssh) target; blocked → decision-inbox card within InterruptBudget; judge failure is fail-CLOSED (pause) for tier ≥ EXTERNAL; flag autonomy.goal_loop default off; contract shown in Mission Control. |
| 5 | P0 | M | Context compaction engine: 50%/85% two-tier trigger, protected head + protect_last_n tail, iterative re-summarization, session lineage, pluggable ContextEngine, per-model thresholds, step-level token/image accounting with screenshot pruning | Hermes context-compression docs; Anthropic screenshot-pruning + cache-breakpoint guidance; Managed Agents event-log sessions | 24/7 runs and long night-shift tasks must survive local 32–128k windows; without pruning a local model dies after ~20 screenshots (1,000–1,800 tokens each). Also makes the operator measurable against %-local and p95 metrics. | agents/core/memory/compaction.py behind a ContextEngine ABC; read-only kernel action (no approval) but every compaction writes an episode + parent_session_id lineage row into the Merkle audit; summaries by the local auxiliary model by default (cloud opt-in per agent); lean/legacy modes and per-model thresholds in config (conservative 60%/85% default-on); extend cost_tracker/iteration_budget with per-step tokens/images/wall-clock and a drop-images-first rule; expose /context in HUD and Telegram; publish operator p50/p95 to /api/metrics/north-star. |
| 6 | P0 | S | Bounded-by-default runtime: per-run budget copied onto every scheduled session, invocation-rate cap and concurrency/queue depth per trigger, pause-with-reason, plus runaway breakers (5 consecutive provider idle-timeouts, stuck patterns: same action→same observation 4+, action→error 3+, monologue 3+, alternating 6+, stall counter with capped resets) | Claude Managed Agents budget.max_list_cost / budget_reached; Devin ACU cap + invocations/hour + queue depth; OpenClaw PR #76345 breaker; OpenHands StuckDetector; Microsoft Magentic max_stall_count/max_reset_count; Hermes issue #26382 shows the cost of not having it | The best-documented 24/7 failure mode is cost/compute blowups from triggers firing repeatedly or loops wedging overnight; local models time out and loop more than frontier APIs. kernel/budget.py has BudgetLedger + LoopDetector for identical actions but not timeouts, alternation, monologue, or per-schedule caps. | Add run_budget (max_tokens, max_wall_seconds, max_steps, optional max_cost) and max_invocations_per_hour / max_concurrent / queue_depth to schedule and watcher definitions; kernel authorize consults BudgetLedger per mission; exhaustion → Mission PAUSED with paused_reason + one inbox card; extend LoopDetector with the OpenHands pattern set and an idle-timeout breaker in the ToolRPC/model-directed loop; trip → task FAILED + incident (not retry); thresholds configurable but hard-capped; never 'unset = unlimited'. |
| 7 | P0 | M | Scheduled-run continuity and hygiene: continuity (last output re-injected), context_from chaining, no_agent script-only jobs, wakeAgent / monitor-mode gate that skips the LLM when nothing changed, incidents keyed by error signature with ack, blocked_config pre-run validation at zero LLM calls, pinned model + drift guard, missed-run policy (one catch-up, no backfill, deterministic stagger), no recursive schedule creation | Hermes cron (v0.21.0 'scheduled jobs stopped being goldfish', model_drift_guard, incidents); Managed Agents deployment_run error records and no-backfill; Claude desktop tasks single catch-up run | Watchers/night shift/morning brief run from scratch each time and either spam the owner (eating the ≤4 push/day budget) or burn GPU on jobs that cannot succeed; a local-first box sleeps and naive schedulers replay a day of missed runs at wake; an unattended job must never silently switch to a cloud model. | Add continuity, context_from, pinned_model, no_agent, continue_mission_id fields to task-queue/schedule rows (autonomy/nl_schedule.py + heartbeat.py); pre-flight validator sets blocked_config and emits one escalation; incidents via autonomy/error_logger.py with signature ack; monitor-mode compares watcher snapshot hash before invoking the LLM; drift guard enforced in llm/ provider resolution (cloud requires per-agent opt-in); on wake at most one catch-up per task with missed_since injected; kernel kind schedule.create from unattended runs allowed only for one-shot same-mission follow-ups, recurring → DENY; monitors stay read-only tier; default-on for validation/incidents/missed-run, default-off for continuity. |
| 8 | P0 | M | Prompt-injection screening on every observation (a11y text, page text, OCR/VLM text, trigger payloads) with steer-to-confirm, taint escalation, and an action-vs-intent second pass; untrusted fire-payload envelope; inter-agent messages can never satisfy approval | Anthropic screenshot injection classifiers + Claude in Chrome per-action classifier (<0.08% ASR); OpenAI Auto-review; Claude Code routines <routine-fire-payload> and agent-teams consent rules ('teammate can't supply consent'); Copilot Studio harmful-instruction detection; ShadowPrompt and ClawHub incidents show the attack is real | Every vendor states injection is unsolved and page/screen content will be obeyed; Nerva's taint model is the right primitive but GovernedDesktop.classify_injection is a regex, and watcher/webhook/email-triggered tasks are the main injection path in a 24/7 system. | Route each observation through a local small-model classifier (LM Studio/Ollama, flag operator.injection_classifier default-off until benchmarked) in addition to the regex; on hit mark run tainted, downgrade to reversible-only, force QUEUE for next mutating step, record snippet in audit chain; before any external/irreversible step a local pass compares the step to the cortex decision record goal and vetoes mismatches; in autonomy/worker.py govern_enqueue wrap trigger-derived text in a taint envelope (existing escalation forces ASK for tier ≥ EXTERNAL) and require the schedule prompt to reference the payload explicitly; a2a.py/sub-agent messages carry origin tags that apply_decision rejects as approval sources (default-on, XS). |
| 9 | P0 | S | Batch actions with sequential stop-on-first-failure semantics and a post-action a11y snapshot (optional screenshot) attached to every step result | Anthropic computer/browser toolsets ('Not executed: an earlier action in this turn failed.'); Qwen open-computer-use post-action screenshots; browser-use max_actions_per_step=4; UFO² speculative multi-action (-51% LLM calls) | Cuts model round-trips for local models while keeping each step individually auditable; a failed step never lets later steps run blind; directly reduces night-shift iteration_budget consumption. | DesktopActionExecutor / GovernedBrowser accept a step list, execute in order, halt at first error, mark remaining steps not-executed in the audit chain, attach post-action snapshot to each result; batch size capped (e.g. 4) via iteration_budget; every mutating step passes classify()/contract checks individually before the batch starts; default-on for read-only batches, flag for mutating batches; later add post-condition checks per step (speculative planning) bounded by the same budget. |
| 10 | P0 | M | Hosted one-line native installer with runtime/GPU detection that ends inside the Command Center, plus `nerva doctor` | OpenClaw install.sh → onboard wizard (~5 min); Hermes install.sh/install.ps1 + hermes setup/doctor; Ollama install.sh (check_gpu, CUDA/ROCm, systemd, loopback bind); Claude Code claude.ai/install.sh + claude doctor | Every fast-spreading 2026 agent collapsed install to one pasted line; Nerva's install.sh assumes a git checkout and Python 3.12 on PATH and prints a 'start it yourself' banner; GAP-0 (2026-09-01) says friction, not quality, is what fails (24,182 visitors / 0 partners). | Wrap install.sh/INSTALL.bat + scripts/install_smoke.py into hosted install.sh/install.ps1 that fetches the signed release bundle from release.yml (SBOM + checksums exist), detects Ollama (:11434) / LM Studio (:1234) and GPU/VRAM via local_model_inventory, picks a model tier from docs/HARDWARE_BENCHMARKS.md, runs the smoke test, starts the service bound to 127.0.0.1 only, opens /api/onboarding/command-center; add nerva doctor over /readyz + smoke; pre-runtime so no kernel kind; installer must never write a 0.0.0.0 bind or a cloud key; default-on for design_partner posture. |
| 11 | P0 | S | Telegram-first 'pair in 60 seconds': t.me/<bot>?start=<code> deep link + governed pairing-code approval on by default; WhatsApp only via official Cloud API | OpenClaw dmPolicy pairing with 1 h expiry ('Telegram is the fastest channel, just a bot token'); Hermes DM pairing (8-char code, lockout) and static TELEGRAM_ALLOWED_USERS; Poke phone-number-only onboarding | a16z finds consumers reach agents by messaging them like a friend; Telegram >1B MAU; the decision inbox already lives on Telegram; today binding is a static allowed_user_ids list and the governed H12.19 SenderPairing is default-off. | Flip JARVIS_CHANNEL_PAIRING on inside product_posture.py WAVE1/design_partner; channels/telegram.py handles /start <code> by presenting it to SenderPairing; unknown sender without code → PENDING request surfaced as a decision-inbox card (kernel QUEUE, owner approval, rate-limited bounded list already implemented); wizard prints deep link with rotating code and records funnel.channel.paired; Discord/Slack use the same gate via gateway.py; no Baileys-style unofficial WhatsApp. |
| 12 | P0 | M | Killer first action: 'your first governed morning brief' — propose → approve in Telegram → verifiable audit-chain receipt, fully local, zero API keys, inside 30 minutes (Gate-2 🚧4 / S8) | Hermes cron delivered to a Telegram home channel; OpenClaw scheduled tasks; Cursor/ChatGPT screenshot-able 'wow'; nobody ships it with an approval queue and tamper-evident receipt | Retention data says a meaningful action in session one retains 2–3x; the north-star is accepted autonomous actions and first_actions already lists morning_brief; this is the demo only a governed system can give. | Wizard autonomy step enqueues a real reversible-tier task brief.morning (read-only kind → GRANT for compute, QUEUE for the push) through the SQLite task queue; Telegram card approves via existing decision-inbox callback; Command Center shows the verify_chain-green audit entry as a receipt; record funnel.first_action.accepted and expose time_to_first_accepted_action_s from north_star.py; interrupt budget respected; LOCAL_ONLY posture asserted. |
| 13 | P0 | S | Activation + retention instrumentation: activation = first accepted autonomous action ≤30 min after install; D1/D7/D30 = ≥1 accepted action; weekly accepted actions per active user; local-only with opt-in aggregate export | Industry retention benchmarks (D7 median 8%, productivity 15%); OpenClaw/Hermes publish only stars; Claude Code exposes doctor but no default telemetry | The 2026-09-01 owner decision is to measure the first-value path as activation rate; north_star.py computes accepted actions but has no cohort/activation block and design partners cannot hand back a number without breaking the zero-telemetry proof. | Extend compute_north_star() with activation {installed_at, first_accepted_at, ttfa_s, activated_30m} and retention {d1,d7,d30} from funnel.* events + TaskQueue.updated_at; render in Command Center; export only via the offline support bundle or POST /api/metrics/share as kernel kind metrics.export at external tier → QUEUE, default-off, aggregate-only, PII-free; tests/test_no_telemetry_proof.py stays green. |
| 14 | P1 | M | Skill refinement loop: patch-level skill edits, post-session self-improvement review on a local auxiliary model, write-approval staging directory reviewed in the morning brief | Hermes skill_manage patch + skills.write_approval + background self-improvement review; Claude Code desktop tasks storing SKILL.md | Nerva generates and quarantines skills but has no cheap iterative refinement; the 'lessons, not logs' patch loop is what makes skills improve with use. | Extend agents/core/skills/loader.py with patch_skill and a nightly-reflection hook that proposes patches; proposals land as PENDING_REVIEW diffs in the decision inbox (reversible tier → QUEUE with one-tap approve, batched into the morning brief to respect the interrupt budget); auto-approve only for trusted non-builtin skills inside the night-shift reversible window; signing re-applied after approval. |
| 15 | P1 | M | `/learn`: distil a directory, doc site or corpus into a knowledge-base skill (lean SKILL.md + index + references/) with L0/L1/L2 progressive disclosure exposed through ToolRPC | Hermes /learn + skills_list/skill_view levels (~3k tokens catalogue) | Cheap procedural knowledge acquisition for local models with small windows; complements the vector store with citable procedures. | learn command in the skills loader reusing the ingestion pipeline + vector store to write references/, registering the skill quarantined; research phase read-only (GRANT), skill write = QUEUE; three-level catalogue API in ToolRPC so the model-directed tool loop loads full content lazily; network fetches only via the governed browser/HTTP seam. |
| 16 | P1 | M | Multi-source skills hub (skills.sh, /.well-known/skills/index.json, GitHub taps, ClawHub/LobeHub) with install-time security scanner, trust tiers, behavior-vs-declaration sandbox diff, scan-time quarantine, `nerva skills add <owner/repo>`, and publishing Nerva's starter skills to skills.sh | Hermes 8-source hub + scanner + builtin/official/trusted/community tiers + NVIDIA SkillEvaluator; OpenClaw security.installPolicy; Vercel skills.sh (17 agents); Unit 42 / Koi findings (22 MB README padding evasion, 341 malicious ClawHub skills) | Nerva already imports Hermes/OpenClaw/GitHub skills and runs a capability-acquisition loop; scanner + tiers let it safely tap the 88k+ ecosystem instead of pinning 78, and being absent from skills.sh means Nerva users cannot ride the gallery and Nerva is not discoverable there. | Extend skills/importer.py + marketplace.py with source adapters and a trust field (builtin/official/signed/community); scan_skill() runs static checks (size/padding anomalies, curl\|sh, base64 blobs, undeclared endpoints) plus a declared-vs-observed diff from the existing sandbox run (egress, files touched), verdict stored with the signature; kernel kind skill.install: signed/official → GRANT to load, community → QUEUE and JARVIS_REQUIRE_SIGNED_SKILLS enforced, dangerous → hard quarantine; skills requesting desktop/browser capabilities default-off; fetches through the SSRF-guarded seam. |
| 17 | P1 | M | Subagent orchestration API: list/steer/stop running children, JSON-schema-validated child outputs, per-delegation cost, leaf-tool restrictions, task claims with depends_on + file locks, completion-veto hooks | Hermes delegate_task v0.21.0; Claude Code agent teams (shared task list, file locks, TaskCompleted veto); Anthropic C-compiler run (current_tasks/ lock files); OpenAI Agents SDK max_turns | 'Work like a company' needs steerable parallel workers with typed hand-offs and conflict-free task claiming; the 18 agents lack mid-flight steering, typed contracts and a quality gate that can refuse 'done'. | Orchestrator kernel kind agent with children charged to BudgetLedger dimensions (tokens, wall-time, recursion, LoopDetector); child tool allowlist excludes schedule/memory-write/messaging and all money/locks/security kinds; concurrency default 3 (not 10), raised only by owner; child outputs validated against pydantic contracts (Nerva 2.0 contract-first); mission steps get depends_on + SQLite claim locks surfaced with routers/swarm.py read_dev_locks; a TaskCompleted-style hook in autonomy/executor.py can veto completion; no nested teams. |
| 18 | P1 | L | Bot Mode-style 'Company Room': named specialist bots with roles, roster-aware message_agent, bounded group rounds (3 rounds / 10 messages), per-bot routines, cross-machine peers over authenticated API | Hermes Bot Mode + hermes peer (default-on in desktop since v0.21.0); Sintra/Lindy 'AI employee' framing (marketing only) | The concrete 'company' UX: specialists with roles, routines and a shared room; Nerva has 18 specialist agents but no room/handoff surface for the owner. | Company Room view in React HUD/Telegram over existing agents; inter-agent messages are kernel kind agent (read-only tier) with bounded rounds enforced by the ledger; each bot's toolsets/skills enablement stored as contracts; peers between Nerva nodes use ToolRPC with bearer keys as node_dispatch external tier (QUEUE until paired); default-off NERVA_COMPANY_MODE; roles map to capabilities/gates/budgets, not job titles. |
| 19 | P1 | S | Static hardline blocklist + owner deny-globs evaluated before any autonomy level + `approvals suggest` history mining that excludes destructive classes | Hermes security model (hardline list executes nowhere, deny fnmatch before yolo, approvals suggest); Claude Code auto-mode block rules | Cheap, legible floor beneath the kernel; mined allowlist suggestions reduce interrupts without loosening irreversible tiers. | kernel/syscalls.py static hardline list (rm -rf /, mkfs, dd to devices, fork bombs, pipe-to-shell) that DENYs regardless of autonomy level plus owner deny-globs; suggest job runs in nightly reflection proposing GRANT patterns only for reversible tier, surfaced in the morning brief. |
| 20 | P1 | S | Network-layer egress allowlist for the operator browser with redirect re-check, private-range/loopback blocking, http/https-only schemes, plus consent-gated real-profile snapshot and LAN-local sidecar routing | Anthropic browser tool docs; windows-mcp-server loopback proxy; browser-use allowed/prohibited_domains; Hermes consent-gated profile snapshot (v0.20.6) + private/LAN URL local Chromium routing; Cowork isolated profile with site-by-site login import | BrowserPolicy checks the URL string before navigation but a redirect, iframe or fetch can leave the allowlist unnoticed; proxy-level enforcement makes the allowlist a real boundary and blocks SSRF into HA/Frigate; profile snapshot lets the operator act as the owner without ever opening the live profile. | Launch the Playwright context in browser_playwright.py with a local proxy / route interception enforcing the suffix allowlist, RFC1918/loopback denial (explicit allow_private_urls for the house LAN), scheme allowlist; re-validate final URL after navigation; log every block as an audit event; use_real_profile owner flag copies the profile to a temp dir, session logged as kernel kind browser_tab, profile use = external tier → QUEUE on first use per domain; never attach to the owner's live Chrome. |
| 21 | P1 | M | Presence-gated 'watch mode' actions with explicit 'take over' handoff | ChatGPT agent watch mode (pauses when user inactive) + takeover; Chrome auto browse 'Take over task'; Gemini Agent pause/take over; Copilot Studio live test view | Fits the ladder between 'queue for approval' and 'auto': some actions are acceptable only while the owner watches, and the owner needs a one-click way to take the mouse; natural UX for remote company mode. | requires_presence attribute on kernel action kinds resolving to GRANT only while desktop_host reports an active session/recent input and the HUD watch panel is open, else QUEUE; 'Take over' button in HUD/Tauri pauses the run, releases input, resumes after the owner marks the step done; pauses are audit events; night shift never satisfies presence. |
| 22 | P1 | M | Credential takeover: kernel-mediated credential.fill from OS keychain / password manager so the model never sees secrets; SecretSource plugin (keyring, Bitwarden/1Password); stripped env for MCP subprocesses | ChatGPT takeover mode; Chrome auto browse via Google Password Manager after authorization; Copilot Studio stored credentials auto-filled; Hermes v0.19.0 SecretSource, v0.20.6 keychain encryption, MCP safe env baseline | Money/locks/security are already above the queue; login flows are the remaining place an operator would read passwords into context and the audit log; removes plaintext .env as the only secret store. | KERNEL-mediated action kind credential.fill (never model-visible): operator detects a login form by a11y role, kernel fills from Windows Credential Manager / macOS Keychain / secret-service for that exact origin/app with a per-origin grant in the permission ledger; LLM only receives 'filled'; alternative 'take over to sign in'; default-off per origin; pluggable SecretSource in config with keyring backend; MCP subprocesses launched with Hermes-style safe baseline env. |
| 23 | P1 | L | Isolated operator session/profile: dedicated browser user_data_dir per run; on Windows a second session / low-privilege 'nerva-operator' account (or Windows agent workspace when available); on macOS background-window control | Cowork built-in browser isolated profile; Windows agent workspace (separate session + agent account, six known folders); UFO² PiP virtual desktop; Anthropic/Microsoft dedicated-VM guidance | Lets the operator work 24/7 without stealing the owner's mouse and prevents inheriting the owner's logged-in sessions. | Phase 1 (S): dedicated Playwright user_data_dir, no owner cookies by default. Phase 2 (L): execution target local-isolated in execution_provider_contract; Windows driver in a second session under a scoped account; kernel refuses irreversible desktop kinds unless target is isolated or presence-gated. |
| 24 | P1 | L | Record-then-replay operator scripts with automatic fallback: successful governed runs persisted as deterministic step lists (a11y refs + values + post-conditions) replayable at near-zero tokens under the original approval scope | Skyvern code caching (run_with='code', falls back to agent on layout change); browser-use replays (secondary) | Recurring chores become deterministic, cheap and auditable; episodes + Synapse skills SDK are the natural home; replay is more local-friendly than fresh reasoning each night. | On success persist the step list as an 'operator script' artifact in the episode store; run_with=replay executes via the same GovernedBrowser/GovernedDesktop path (same kernel checks/allowlists) verifying each post-condition; mismatch → halt and re-enter agent mode only if the run's tier allows; replays carry original approval scope, any deviation (new domain/app) re-queues; feeds night shift as reversible-only jobs. |
| 25 | P1 | M | Reviewer-agent auto-review for reversible/low-external kinds (local model given goal + cortex decision record + proposed step; uncertain → QUEUE) | OpenAI Auto-review (sandbox independent of approval, 'not a deterministic guarantee'); Anthropic per-action classifier; Hermes smart approvals; Skyvern validator; Google User Alignment Critic (secondary) | Reduces the owner's interrupt load without loosening the ladder; money/locks/security stay human-only. | kernel/binding.py mediation mode kernel.auto_review (default-off) for reversible/external-low kinds only; allow/deny with reasons stored in the audit chain; denials and 'uncertain' → QUEUE; never applies to KERNEL-mediated kinds; accepted-vs-overridden tracked in north-star metrics. |
| 26 | P1 | S | Durable HITL pause: pending approval serialized into the checkpoint and re-emitted on restore with idempotent delivery ids; approval TTL per risk tier with fail-closed auto-deny and resumable checkpoint; durable delivery ledger with redelivery after restart | Microsoft Agent Framework (pending requests in checkpoints, RequestInfoEvent re-emitted); OpenAI RunState.to_json(); Hermes v0.19.0 durable delivery ledger and 300 s fail-closed approval timeout; Copilot Studio request expiry; OpenClaw ask fallback deny | Night-shift and multi-day missions straddle restarts; today a pending decision card can be lost or duplicated (double push eats the ≤4/day budget) and an approval waiting forever tempts 'skip' modes. | Store pending_request (task id, card hash, delivery id) in checkpoint.py CheckpointManager; on boot autonomy_coordinator re-emits unresolved requests via inbox.py with the same delivery id so InterruptBudget is not charged twice; TTL per tier with auto-deny on expiry and checkpoint for later resume; pending authority bound to the run id; ledger table keyed by delivery id in the SQLite task queue; session export reuses the audit redaction module. |
| 27 | P1 | L | Session-as-event-log with stateless worker: wake(mission_id) rebuilds step state from the append-only mission log; sandbox/container death surfaces as a retryable tool error | Claude Managed Agents architecture; OpenHands per-event JSON persistence; Temporal critique of in-process LangGraph checkpoints | Turns the night shift into a crash-safe 24/7 runtime without Temporal; in-process state dies with the process. | Mission event log becomes source of truth; AutonomyWorker gains wake(mission_id) replaying events; docker target death = retryable tool error with bounded retries (BudgetLedger); non-idempotent EXTERNAL/IRREVERSIBLE steps never auto-replayed — on resume they become ASK cards. |
| 28 | P1 | M | Progress ledger + stall-triggered replan with owner plan review for higher tiers, and cloud-on-stall escalation policy | Microsoft Magentic (is_request_satisfied / is_in_loop / is_progress_being_made, MagenticPlanReviewRequest); Devin Fusion dual-model routing (60% cost cut) | A company-mode manager needs explicit per-round progress judgment and bounded replans; the governed version of Fusion is local-by-default with per-agent cloud escalation only on measured stall. | Mission loop emits a progress_ledger event each round (local model, structured JSON); stall → replan requires owner approval when max tier ≥ EXTERNAL or resets > 2; plan review default ON for tier ≥ 2; mission escalation_policy {local_only \| cloud_on_stall} where cloud requires the agent's existing opt-in and is logged as a cortex decision record with cost; %-local metric tracks the effect. |
| 29 | P1 | L | Long-build harness template (planner → generator → evaluator with feature-list JSON, progress file, session-open verification, evaluator using the browser seam, sprint contracts) and run-status-vs-outcome grading (pass^k; 'green ≠ success') | Anthropic harness posts (2025-11, 2026-03); Devin 2.2 self-verify with computer use; Claude routines docs; Anthropic 'Demystifying evals' | The demonstrably working shape of multi-hour autonomous work; the morning brief must report verified outcomes, not exits, or the north-star becomes misleading. | Ship a 'company-mode build' mission template: feature list + progress file as mission artifacts, generator in docker target, evaluator via owner-enabled browser seam in reversible mode, sprint contract = Mission contract, gates = tests; capability_verification.py gains per-task-kind outcome graders; digest.py separates ran / verified / unverified; north_star.py adds pass^k over last k runs of each recurring task. |
| 30 | P1 | S | One-command migration importers: `nerva import --from hermes\|openclaw\|claude-code` (skills, SOUL/USER persona files, memory, config, channel tokens) | Hermes import-agent (Claude Code/Codex) and wizard detection of ~/.openclaw; Cursor VS Code-fork carry-over (secondary) | The 600k+ combined OpenClaw/Hermes base is the audience that wants governance after ClawHavoc and the exposed-gateway wave; switching cost is the barrier and Nerva's importer only handles skills. | Extend skills/importer.py with detectors for ~/.openclaw, ~/.hermes, ~/.claude; map SOUL.md/USER.md → persona layer, MEMORY.md → ingestion pipeline as tainted facts with read-only preview, channel tokens → SecretStore, skills → quarantined pinned import; every memory write is kernel kind memory.ingest at reversible tier with a dry-run diff and one QUEUE approval; offer shown in the wizard intro step. |
| 31 | P1 | M | Package-manager and store presence: Homebrew cask, winget manifest, signed apt/dnf repo, GHCR image, signed Tauri installers; stable/latest channels with auto-update and pre-upgrade snapshot; real-OS CI lanes for the operator drivers | Claude Code (brew cask stable + @latest, winget, signed apt/dnf/apk, notarized, minimumVersion); Ollama cask (84.5k installs/365d) + winget; LM Studio; Hermes macOS/Windows CI lanes (v0.21.0) | Free, measurable distribution surfaces where technical early adopters already look; release.yml has SBOM/checksums but no cask, manifest, image or signed desktop build. | release.yml builds a GHCR image from docker-compose.yml, generates cask formula + winget manifest pointing at the signed tarball, publishes both; stable (≥1 week soak) and latest channels; update path takes the Gate-2 🚧5 pre-upgrade snapshot; code-signing certs are owner tasks in docs/OWNER_TASKS.md; matrix CI for Windows/macOS/Linux drivers. |
| 32 | P1 | M | Accessibility-snapshot-first browser observation (Playwright aria_snapshot(boxes=True)) fused with screen_grounding before any vision click | Playwright MCP default snapshot mode + --snapshot-boxes; Playwright Python 1.60+ aria snapshots; Anthropic read_page refs | Makes the governed browser a STRUCTURED_UI route (deterministic, token-cheap, no VLM) matching operator_router rank order. | observe_snapshot() in browser_playwright.py using page.locator('body').aria_snapshot(boxes=True) bounded by max_extract_chars, feeding fuse_with_a11y; registered in operator_router as STRUCTURED_UI for browser tasks; JARVIS_PLAYWRIGHT_HOST stays default-off, isolated contexts, existing origin allowlist. |
| 33 | P1 | M | Pinned local grounding-model presets + coordinate-convention parsers (Qwen3-VL-4B/8B default, UI-TARS-1.5-7B, Holo-3.1-35B-A3B Q4 GGUF, Qwen3.8-27B; all Apache-2.0) | LM Studio/Ollama Qwen3-VL and Qwen3.8 catalogs; Hcompany Holo-3.1 GGUF; Agent S3 (UI-TARS grounder); OS-Atlas 0–1000 normalized vs UI-TARS absolute-on-resized conventions | llm/vlm.py refuses to guess a model and screen_grounding only parses '(x, y)'; different open grounders emit different coordinate conventions, so a preset table is the difference between a working visual route and silent mis-clicks, all strict-local on a 16–24 GB box. | VLM_PRESETS registry keyed by JARVIS_VLM_PRESET (still requires explicit JARVIS_VLM_MODEL pin; presets validate/annotate) plus screen_grounding.normalize_coords(convention, image_size, resized_size); visual route stays rank 3; every VLM-derived click is a mutating step under GovernedDesktop approval with the screenshot hash in the audit record; cloud VLMs remain per-agent opt-in. |
| 34 | P1 | S | Owner-granted, revocable input-consent persistence modelled as a kernel capability (Wayland portal restore_token; macOS TCC prompt decision) | XDG RemoteDesktop portal v2 persist_mode/restore_token; python-libei 0.3.0+; macOS TCC per-app grants with Sequoia re-prompts | Night shift cannot pop a consent dialog at 3 a.m., but silently persisting consent would violate graduated autonomy; an explicit audited revocable grant reconciles both. | Action kind/contract desktop.input.persist (external/reversible tier) → QUEUE by default; on GRANT store restore_token in the secrets store, log to the Merkle chain, expose in HUD; kill-switch/revoke deletes the token and closes the RemoteDesktopSession; macOS analogue is the decision to call CGRequestScreenCaptureAccess / prompt=True; night shift uses persisted consent only for reversible-only steps. |
| 35 | P1 | S | Windows driver hardening: `uiautomation` (Apache-2.0, comtypes-only) as second UIA backend, UIPI/elevated-window honest refusal, DPI-aware screenshots | Windows-MCP (uiautomation); UFO²; Microsoft UIA security overview | pywinauto has had no release since 2025-01-06; a non-elevated Nerva cannot see or click elevated windows and today that surfaces as generic failures. | _UiautomationBackend next to _PywinautoBackend in desktop_host.py selected by JARVIS_DESKTOP_BACKEND (default pywinauto), same element dicts so fuse_with_a11y is unchanged; probe TokenIntegrityLevel of the target HWND and refuse mutating steps on high-IL targets with a stable reason; never ship a uiAccess manifest; SetProcessDpiAwarenessContext before screenshots. |
| 36 | P2 | M | Fully local voice loop: faster-whisper STT, on-device open-vocabulary wake word, clause-streaming TTS with barge-in, voice bubbles on Telegram/Discord | Hermes voice mode (v0.20.0 Herald: barge-in, wake words, NeuTTS/Edge TTS, 26-phrase hallucination filter) | Adoption ('iPhone-like') and hands-free house control, all local, matching the non-negotiable. | Extend the docs/VOICE.md pipeline with faster-whisper + openWakeWord-style detector and NeuTTS/Edge fallback; wake-word audio never leaves the device; voice-initiated actions carry origin=inbound taint so anything above read-only goes to the decision inbox. |
| 37 | P2 | M | OpenAI-compatible API server per profile (chat/completions, models, responses with server-side state, tool-progress SSE) with mandatory bearer key even on loopback; MCP server exposure of the governed operator; consume Windows agent connectors / WebMCP as API-route implementations | Hermes API server (port 8642) and hermes mcp serve; Windows agent connectors + MCP registry; Chrome WebMCP origin trial; UFO³ MCP | Zero-cost adoption channel (any OpenAI-format client becomes a Nerva front end), substrate for Nerva-to-Nerva peers, and a way to plug into the OS-level connector ecosystem instead of reimplementing every app. | FastAPI router /v1/* mapped to the orchestrator; requests are origin=inbound (tainted) so all mutating kinds QUEUE; API key required; default-off NERVA_API_SERVER=0; bind 127.0.0.1; MCP server in agents/core/mcp exposing snapshot/locate (read-only) and invoke/type (kernel-mediated) with the same action kinds, MCP callers treated as untrusted and passing the permission ledger; consumed connectors/WebMCP tools registered as OperatorRoute.API so they outrank UI/visual. |
| 38 | P2 | S | Home Assistant event-driven observer: subscribe to state_changed over WebSocket and wake watchers on filtered events | Hermes HA gateway adapter | House brain is pull-based; events make observers reactive (door opened, camera event) with zero polling cost. | HA WebSocket observer in observers/watchers producing monitor.alert kernel events; device actions still route through the HA plugin risk policy (locks/security never above the queue); default-off filter list. |
| 39 | P2 | S | Phone→desktop 'dispatch' run type on an always-on local host with the decision inbox as approval channel | Claude Cowork Dispatch; Perplexity Personal Computer (Mac mini + iPhone approve); Gemini Spark 24/7 background agent | Nerva already has the Telegram inbox and Expo app; framing runs as dispatched jobs on an always-on box is the company-mode story and the adoption hook. | Dispatch run type created from mobile/Telegram, executed on the designated local host (target local or local-isolated), decision inbox for approvals, run summary card at completion; night-shift rules apply outside presence hours. |
| 40 | P2 | S | Audit-chain head anchoring to an owner-held copy (mobile app/Telegram) and tiered operator kill switch (pause input, kill browser context, lock operator session, disable seam flag) | windows-mcp-server (hash chain optionally anchored off-box, tiered containment); Perplexity 'kill switch' (unverified) | Nerva has the Merkle chain and estop; the operator-specific gap is anchoring (tamper on the host becomes detectable) and per-driver containment. | Periodically push the chain head to the Expo app/Telegram; extend estop with operator tiers; anchoring is a read-only kind, containment actions are KERNEL-mediated and always allowed. |
| 41 | P2 | M | Reviewable memory consolidation that never mutates the live store (nightly 'dream' → candidate namespace → owner or earned-autonomy promotion) | Claude Managed Agents Dreams | DailyReflector already consolidates living memory nightly; a candidate store keeps a 24/7 learner from silently corrupting the digital twin/persona layer. | DailyReflector._consolidate_living_memory writes to a candidate namespace; promotion is a REVERSIBLE-tier decision (auto under earned autonomy, else inbox card); diff visible in HUD; night window only. |
| 42 | P2 | M | Hybrid API/CLI-before-GUI adapters per OS (PowerShell/COM allowlist, osascript/JXA + Shortcuts CLI, D-Bus/gdbus + xdg-open) registered as API/CLI routes | UFO² hybrid GUI + API actions; Windows-MCP App/Shortcut/Clipboard tools; Apple Shortcuts CLI; dogtail/GNOME D-Bus | operator_router ranks API and CLI above UI and visual but few concrete OS-level adapters exist; native surfaces are cheaper and more reliable than pixels. | Extend desktop_control.py allowlist keys with per-OS launchers resolved by the host (never caller-supplied paths); each adapter is a GovernedDesktop step (mutating → approval) reporting which route executed for operator_reality metrics. |
| 43 | P2 | L | Local GUI-agent regression harness in the research lab (OSWorld-Verified subset in the docker target, WindowsAgentArena, MacArena/macOSWorld samples internal-only) reporting success at 15/50 steps per VLM preset | xlang-ai/OSWorld Docker provider; WindowsAgentArena; MacArena (CC BY-NC-SA); trycua sandboxes | Nerva 2.0 research-lab benchmarks exist contract-first; without a runnable harness drivers and presets cannot be compared honestly, and leaderboard rows are self-reported. | Lab runner launching OSWorld's docker provider under the docker execution target, driving GovernedDesktop with auto-GRANT scoped to the sandbox only; results into the benchmark contract with preset, step budget, commit hash; MacArena task definitions internal-only; publish only own reproducible numbers with step budgets stated. |
| 44 | P2 | M | Shareable 'receipt' cards (redacted signed audit excerpt) and signed starter packs (Design Partner, Night Shift, House Brain, Inbox Triage, Research Lab) from a Command Center gallery | Cursor/ChatGPT screenshot-native spread; viral CLAUDE.md template (220k stars); Vercel skills.sh Packs; Hermes official optional skills | Nerva's differentiator is invisible in a chat screenshot; a receipt makes 'it acted while I slept, here is the proof' a shareable unit; packs get strangers to value without configuring 18 agents (product_posture.py POSTURES and /api/packs verification already exist). | /api/audit/{id}/receipt renders a card from the Merkle entry (kind, tier, approver, timestamp, chain hash prefix, local-model badge) through existing PII/secret scanners; posting is external-tier share.receipt → QUEUE unless pre-granted; packs as signed bundles under packs/ verified by knowledge_packs.verify_pack, declaring posture flags, reversible-only watchers, signed skills; install = kernel kind pack.install → QUEUE with plain-language diff; GET /api/packs/gallery. |
| 45 | P3 | S | Launch sequencing and co-distribution: Show HN + r/LocalLLaMA + r/selfhosted same day with runnable no-signup artifact and receipt GIF; Ollama/LM Studio integration docs PRs; NemoClaw/OpenShell profile | OpenClaw/Hermes GitHub-native launches; Show HN rules; Hermes listed in Ollama integrations and NemoClaw README; NVIDIA NemoClaw one-command stack | docs/launch drafts cite stale numbers and launching before the installer/first-action loop would repeat the 0-partner outcome; local-first users discover agents from the runtime they already run. | Owner-gated per GAP-0 ordering: refresh drafts to v1.0.0 / 18 agents / current test count, gate on ≥N activated design partners, position as 'the governed local agent'; write Ollama/LM Studio integration docs and submit PRs; publish an OpenShell profile running the docker target inside OpenShell with the kernel unchanged; no product changes, no telemetry. |
| 46 | P3 | M | Optional set-of-marks pre-processor (OmniParser icon_detect_v3, MIT YOLOv9-E) behind a heavy [grounding] extra for weak local VLMs | microsoft/OmniParser v3 detector; Hermes SoM capture; WindowsAgentArena Navi | Small local VLMs ground poorly on dense UIs (Qwen3-VL-2B 11.4% on MacArena); numbered marks plus the a11y tree raise reliability without cloud. | Lazy import of ultralytics/torch only when JARVIS_GROUNDING_SOM=1; run on the driver's screenshot, emit boxes into fuse_with_a11y as source='som'; weights pinned by hash in the marketplace manifest; no network; disabled by default; never pull AGPL YOLOv8 detectors. |

## 3. Do not copy

- **YOLO / approvals.mode: off / unattended_mode: approve / cron_mode: approve (Hermes), Cowork 'Skip', ChatGPT 'Full access', Manus 'Skip confirmations', Claude routines 'no approval prompts during a run' with all write-capable connectors included** — Nerva's kernel must have no global bypass; autonomy is earned per capability with money/locks/security pinned to the queue. Sources: https://hermes-agent.nousresearch.com/docs/user-guide/security, https://support.claude.com/en/articles/13345190-get-started-with-claude-cowork, https://learn.chatgpt.com/docs/sandboxing/auto-review, https://manus.im/blog/manus-schedules, https://code.claude.com/docs/en/routines
- **Skipping dangerous-command checks on container/cloud backends ('container is boundary')** — The audit chain, budgets and taint semantics are the product, not the sandbox; kernel mediation stays on every target. Source: https://hermes-agent.nousresearch.com/docs/user-guide/security
- **An auxiliary-LLM or reviewer agent as the sole gate (Hermes smart approvals, OpenAI Auto-review, Copilot Studio human-supervision trigger)** — Vendors themselves say it is probabilistic ('not a deterministic security guarantee'; 'Don't rely on human review… as a fail-safe'; auto mode misses 17% of overeager incidents). Model judgment may reduce interrupts for reversible tiers, never decide external/irreversible kinds. Sources: https://learn.chatgpt.com/docs/sandboxing/auto-review, https://learn.microsoft.com/en-us/microsoft-copilot-studio/human-supervision-computer-use, https://www.anthropic.com/engineering/claude-code-auto-mode
- **Judge fail-open semantics ('broken judges default to continue') and unbounded defaults (Devin 'if unset, runs without limits', OpenAI max_turns=None, Hermes 10 concurrent / 250 iterations without ledgered budgets)** — 24/7 local operation needs fail-closed pauses and bounded defaults; children tie to BudgetLedger/LoopDetector. Sources: https://hermes-agent.nousresearch.com/docs/user-guide/features/goals, https://docs.devin.ai/product-guides/automations, https://openai.github.io/openai-agents-python/human_in_the_loop/, https://github.com/NousResearch/hermes-agent/releases/tag/v2026.8.31
- **Skill and memory self-writes defaulting to no approval (Hermes write_approval flags opt-in), plugins/skills executed in-process as trusted code, and open-upload registries moderated after the fact (ClawHub)** — ClawHavoc: 341 of 2,857 skills malicious in weeks; Unit 42 found scanner evasion by README padding. Keep CDX-8 quarantine, signing, curator and trust tiers as defaults. Sources: https://thehackernews.com/2026/02/researchers-find-341-malicious-clawhub.html, https://unit42.paloaltonetworks.com/openclaw-ai-supply-chain-risk/, https://docs.openclaw.ai/tools/exec-approvals
- **OpenClaw trusted-operator defaults (security=full, ask=off, sandbox off) and gateways reachable from the internet after a VPS install** — 40,214 exposed instances, 63% vulnerable, plus infostealer waves; Nerva stays loopback-only and the installer must never change that. Sources: https://www.infosecurity-magazine.com/news/researchers-40000-exposed-openclaw/, https://www.intel471.com/blog/openclaw-a-viral-ai-assistant-and-a-magnet-for-infostealer-malware-and-clickfix-trickery
- **Cloud-only or cloud-default execution and perception: virtual computers (ChatGPT agent, Perplexity Computer, Copilot Studio hosted browser/Cloud PC), cloud browser providers (Browserbase/Browser Use Cloud/Firecrawl), Nous Portal/Tool Gateway as golden path, Agent S3 grounding on HF Inference Endpoints, cloud-only always-on paths (routines, Managed Agents, Lindy Autopilot)** — Violates local-first; Nerva offers isolation locally and cloud strictly opt-in per agent; the 24/7 runtime is the local supervisor. Sources: https://trustvector.guard0.ai/agents/chatgpt-agent, https://learn.microsoft.com/en-us/microsoft-copilot-studio/computer-use, https://hermes-agent.nousresearch.com/docs/user-guide/features/browser, https://github.com/simular-ai/Agent-S
- **Screenshot-only perception as the main loop (Anthropic computer toolset, OpenAI CUA) and pyautogui as the sole actuation layer** — 1,000–1,800 tokens per frame, brittle, hard for local models; pyautogui unmaintained since 2023-05, primary-monitor only, X11-only. Use a11y/API routes first, pixels strictly as fallback. Sources: https://platform.claude.com/docs/en/docs/agents-and-tools/tool-use/computer-use-tool, https://github.com/asweigart/pyautogui
- **Private-API and consent-bypassing input paths: macOS SkyLight SPIs as primary path (Hermes), KWin private EIS 'bypass the XDG RemoteDesktop portal' (kwin-mcp), ydotool/raw /dev/uinput, GNOME-private Mutter APIs (dogtail), Windows uiAccess=true manifests** — They remove the human consent gate Nerva's governance depends on, need root/signing, or break on OS updates. Use public AX/AT-SPI/portal APIs; refuse elevated targets. Sources: https://hermes-agent.nousresearch.com/docs/user-guide/features/computer-use, https://github.com/isac322/kwin-mcp, https://github.com/ReimuNotMoe/ydotool, https://learn.microsoft.com/en-us/windows/win32/winauto/uiauto-securityoverview
- **Foreground-only desktop control that hijacks the owner's mouse (ChatGPT Computer Use on Windows) as the long-term design** — Acceptable only as the presence-gated watch mode; 24/7 requires isolated session/background input. Source: https://learn.chatgpt.com/docs/computer-use
- **Letting the model read or store credentials, subdomain-wide origin allowlists or first-party-CDN message passing (ShadowPrompt), Playwright MCP --extension attach to the owner's live Chrome, --allow-unrestricted-file-access defaults** — Every vendor added takeover modes for this reason; the Claude extension XSS chain came from *.claude.ai trust; ambient credentials contradict the isolated-context rule. Sources: https://thehackernews.com/2026/03/claude-extension-flaw-enabled-zero.html, https://github.com/microsoft/playwright-mcp
- **Email-based approval loops with long expiry (Copilot Studio Outlook reviewer) and sharing an agent with maker credentials so others act with the author's access** — Too slow and phishable; Nerva is single-owner and approvals live in the authenticated decision inbox with short TTLs. Source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/human-supervision-computer-use
- **Continuous screen/audio capture as the value proposition (Windows Recall style, Rewind/Limitless)** — Recall's post-authentication threat model is disputed; Limitless was shut down in EU/UK on consent grounds. Living memory ingests structured, explicit sources with forget/purge, not ambient recording. Sources: https://support.microsoft.com/en-us/windows/privacy/privacy-and-control-over-your-recall-experience, https://winbuzzer.com/2025/12/05/meta-acquires-ai-wearables-startup-limitless-kills-pendant-sales-and-sunsets-rewind-app-xcxwbn/
- **Unofficial WhatsApp (Baileys) as a first-class channel and chasing 27-platform gateway breadth as a target** — OpenClaw users' personal accounts were banned; most Hermes adapters are enterprise/China chat. Add only channels the owner uses; WhatsApp only via official Cloud API. Sources: https://github.com/openclaw/openclaw/issues/4376, https://hermes-agent.nousresearch.com/docs/user-guide/messaging/
- **Generic dynamic manager (Magentic-One style) as the default company brain, persona-only 'AI software company' role-play (MetaGPT/ChatDev), Agent Swarms cloning agents for hundreds of outbound actions, and 'AI employee, no supervision' marketing** — Microsoft calls Magentic 'untested' outside its design; TheAgentCompany best agent ≤30%; METR >16 h unreliable; mass EXTERNAL-tier actions violate non-negotiables; roles must map to capabilities, gates and budgets. Sources: https://learn.microsoft.com/en-us/agent-framework/workflows/orchestrations/magentic, https://arxiv.org/html/2412.14161v3, https://metr.org/time-horizons/, https://sintra.ai/
- **Treating a green run/exit as success in the morning brief and auto-granting plan approvals from a lead agent for tier ≥ EXTERNAL** — Claude routines docs: green 'does not mean the task in your prompt succeeded'; agent teams auto-grant plans without human review. Report verified outcomes. Sources: https://code.claude.com/docs/en/routines, https://code.claude.com/docs/en/agent-teams
- **In-process checkpoints as 'durability' (LangGraph style)** — A run dies with the process; the mission event log must be process-independent and replayable. Source: https://temporal.io/blog/temporal-langgraph-plugin-durable-execution
- **Electron desktop shell, raw model-callable PowerShell/Registry/FileSystem MCP tools with telemetry beacons (Windows-MCP posthog), GPL/NC-licensed building blocks (atomacos, macapptree, dogtail, OmniParser YOLOv8, Holo2-30B/235B, Holo1.5)** — Nerva already has Tauri; raw OS tools must route through CLI/ToolRPC allowlists and the kernel with zero analytics; GPLv2/CC-BY-NC imports conflict with the Apache-2.0 product. Sources: https://github.com/CursorTouch/Windows-MCP, https://daveenguyen.github.io/atomacos/readme.html, https://github.com/MacPaw/macapptree, https://github.com/microsoft/OmniParser, https://huggingface.co/Hcompany/Holo2-8B
- **Star velocity / GitHub Trending as north-star, hardware-first products (Humane, Rabbit R1), companion-first positioning (Dot), generic 'AI' Product Hunt launches with landing pages, requiring a paid cloud account to start (Claude Code) or silently reusing another tool's cloud login (OpenClaw Quick Start)** — Trending is gameable and coexisted with mass exposure; Humane returns exceeded sales, Rabbit had ~5k daily users of 130k sold, Dot reached 24.5k downloads; Show HN rejects sign-up walls; Nerva's first-30-minutes loop must be zero-key and local, measured in accepted governed actions per active user. Sources: https://aiforautomation.io/news/2026-05-13-ai-agents-github-trending-star-ranking-broken, https://techcrunch.com/2025/02/18/humanes-ai-pin-is-dead-as-hp-buys-startups-assets-for-116m, https://en.wikipedia.org/wiki/Rabbit_r1, https://techcrunch.com/2025/09/05/personalized-ai-companion-app-dot-is-shutting-down/, https://news.ycombinator.com/showhn.html, https://code.claude.com/docs/en/setup
- **Marketing 'auditable and reversible' / 'kill switch' without a verifiable chain and defined undo semantics (Perplexity), or publishing self-reported leaderboard numbers as product claims** — Ship the mechanism before the word; OSWorld rows are self-reported and harness-dependent; publish only own reproducible docker-harness numbers with step budgets. Sources: https://www.macrumors.com/2026/04/16/perplexity-personal-computer-for-mac/, https://leaderboard.steel.dev/leaderboards/osworld/
- **Regressing on the things Hermes explicitly lacks: no spend cap (issue #26382 open), no tamper-evident action audit (issue #487 closed 'not planned'), no kill switch, no interrupt budget, no house risk tiering** — These are Nerva's verified differentiators; do not trade them for parity speed and say so in marketing. Sources: https://github.com/NousResearch/hermes-agent/issues/26382, https://github.com/NousResearch/hermes-agent/issues/487, https://hermes-agent.nousresearch.com/docs/user-guide/messaging/homeassistant

## 4. Bets nobody in the field ships

- **Proof-of-Action receipts: every accepted autonomous action becomes a redacted, Merkle-anchored, owner-verifiable receipt whose chain head is mirrored to the owner's phone — 'it acted while I slept and here is the proof' as the shareable unit and the trust primitive for delegation** — why nobody: Cloud vendors have no user-verifiable log (the log lives with them) and open agents optimise for speed, not evidence; the feature only makes sense when the chain, the approval queue and a local host are all owned by the user. · why Nerva can: Nerva already has the tamper-evident Merkle chain, verify_chain, cortex decision records, PII/secret scanners, the Telegram inbox and an Expo app; only rendering, anchoring and a share kind are missing. Hermes closed the hash-chained action log as 'not planned' (issue #487); windows-mcp-server has anchoring but no agent; Perplexity's 'auditable' is a claim without a chain. Sources: https://github.com/NousResearch/hermes-agent/issues/487, https://github.com/deploymenttheory/windows-mcp-server, https://www.macrumors.com/2026/04/16/perplexity-personal-computer-for-mac/ · first slice: GET /api/audit/{id}/receipt + 'Share receipt' button after the wizard's first accepted morning brief; hourly chain-head push to the owner's Telegram; share.receipt kernel kind at external tier (QUEUE).
- **One consent ledger across the whole machine: the OS consent surfaces (XDG portal restore_token, macOS TCC grants, Windows integrity/agent accounts, browser origins, HA devices, MCP servers) modelled as a single kernel-governed, per-app/per-site, earned-and-revocable permission ledger with presence gating — 'the settings app for what your AI may touch'** — why nobody: Cloud agents cannot see OS consent state and OS vendors (Apple, Microsoft) only expose their own surface; a local-first kernel is the only place where desktop, browser, house and tool consent meet. · why Nerva can: The graduated-autonomy ladder, taint escalation, preference learning (EARNED_MIN_SAMPLES) and HA per-device risk policy already exist; the ledger is a schema plus adapters. Every 2026 operator ships a per-app prompt (Cowork, ChatGPT, Windows agent workspace, Manus, OpenClaw) but each covers one surface and none unifies desktop + browser + house + tools under one revocable audit. Sources: https://support.claude.com/en/articles/14128542-let-claude-use-your-computer-in-cowork, https://support.microsoft.com/en-us/windows/ai/ai-features/experimental-agentic-features, https://flatpak.github.io/xdg-desktop-portal/docs/doc-org.freedesktop.portal.RemoteDesktop.html, https://docs.openclaw.ai/tools/exec-approvals · first slice: DesktopAppPolicy + BrowserPolicy grants in the autonomy SQLite with {once, session, always, never}; HUD 'Permissions' panel listing every grant with revoke; desktop.input.persist as the first OS-consent-backed entry.
- **Chores that graduate to autopilot: governed record-then-replay — successful supervised runs become deterministic, zero-token operator scripts with post-conditions, executed only in the night-shift reversible window under the original approval scope, promoted by pass^k track record and demoted on drift, with the replay lineage in the audit chain** — why nobody: Vendors are paid per token/credit and have no incentive to make recurring work free; open agents lack an approval-scope object to bind a replay to. · why Nerva can: Episodes, Synapse skills SDK, night shift, preference learning and the north-star pass metric are the exact substrate; docker + a11y drivers make replay local. Skyvern has code caching for cloud web workflows only; no agent ties replay to an approval scope, an autonomy ladder or an audit lineage. Sources: https://www.skyvern.com/docs/developers/features/code-caching, https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents, https://code.claude.com/docs/en/routines · first slice: Persist the step list of a successful governed browser run as an episode artifact; run_with=replay through the same GovernedBrowser path verifying post-conditions; any new domain/app re-queues; report ran/verified in the morning brief.
- **The verified-outcome company: a fully local 'company mode' where every goal carries a contract (outcome/verification/constraints/boundaries/stop_when), deterministic gates, a fail-closed judge, a per-round progress ledger, bounded per-run budgets and breakers, and a morning brief that reports only verified outcomes with pass^k — the first 24/7 agent runtime whose success claims are evidence-backed by construction** — why nobody: 'AI employee' vendors sell the promise of no supervision; admitting unverified outcomes undercuts the pitch, and cloud runtimes cannot run project-local verification gates on the owner's machine. · why Nerva can: Mission, iteration_budget, BudgetLedger/LoopDetector, cortex decision records, digest/retro, the docker target and 18 specialists already exist; only the contract/judge/ledger layer and outcome graders are missing. Hermes /goal is fail-open and single-session; Claude routines warn green ≠ success; Managed Agents bound budgets but run in the cloud; Devin caps ACUs but is code-only cloud. Sources: https://hermes-agent.nousresearch.com/docs/user-guide/features/goals, https://code.claude.com/docs/en/routines, https://platform.claude.com/docs/en/managed-agents/scheduled-deployments, https://learn.microsoft.com/en-us/agent-framework/workflows/orchestrations/magentic, https://arxiv.org/html/2412.14161v3 · first slice: Mission.contract + gates + local judge (fail-closed for tier ≥ EXTERNAL) with mission.gate kernel kind; digest.py splits ran/verified/unverified; north_star.py pass^k per recurring task.
- **Physical-context-gated autonomy: the house brain (HA events, Frigate detections, presence, time-of-day) becomes an input to the Action Kernel's risk policy for digital actions — presence from sensors satisfies 'watch mode', a door/camera event wakes watchers at zero tokens, night window is inferred from the home state, and locks/security stay above the queue — a world model that governs the computer, not just the lights** — why nobody: It requires local access to home sensors, cameras and the computer simultaneously plus a deterministic risk policy that can consume physical context; cloud agents lack the data and open agents lack the kernel. · why Nerva can: Nerva uniquely owns both halves: an HA house brain with per-device risk tiers, Frigate read-only cameras, a world-model contract in Nerva 2.0, and a kernel with requires_presence-style hooks. Hermes has four generic HA tools and an event adapter; no vendor ships a house model, and cloud operators cannot see the home at all. Sources: https://hermes-agent.nousresearch.com/docs/user-guide/messaging/homeassistant, https://trustvector.guard0.ai/agents/chatgpt-agent, https://blog.google/innovation-and-ai/products/gemini-app/next-evolution-gemini-app/, https://arxiv.org/abs/2606.05342 · first slice: HA WebSocket observer emitting monitor.alert events (default-off filter list); a presence signal from HA feeding the requires_presence check for the first presence-gated desktop kind; night-window inference recorded as a cortex decision.

## 5. Hermes 2026 release cadence (primary sources)

Hermes Agent (Nous Research, MIT) 2026 cadence from https://github.com/NousResearch/hermes-agent/releases (read 2026-09-05): public launch ~2026-02-25 (secondary); v2026.6.5 'Surface' (Jun 5: native desktop app macOS/Linux/Windows, self-update, /undo); v2026.6.19 (Jun 19: iMessage via Photon, async subagents, Automation Blueprints, WhatsApp Cloud API, atomic memory batch ops); v0.18.0/v2026.7.1 'Judgment' (Jul 1: MoA as model, /goal completion contracts, /learn, /journey, background fan-out, scale-to-zero gateway, Vertex AI); v0.19.0/v2026.7.20 'Quicksilver' (Jul 20: ~80% TTFT cut, LLM-reviewer smart approvals, Bitwarden/1Password SecretSource, subagent transcript tail, durable delivery ledger, multi-profile gateway, cron execution audit history, session export --redact); v0.19.1/v2026.7.30 (1,000+ PRs); v0.20.0/v2026.8.3 'Herald' (Aug 3: streaming TTS with barge-in, on-device wake words, A2A v1.0, HMAC outbound webhooks, desktop Artifacts + Plugin SDK, hermes import-agent, cold start 14s→1.8s, per-model compression thresholds, Office skills, MCP lazy startup); v0.20.3 (Aug 16: MCP 2.x SDK, bundled Bot Mode, Cua Driver 0.20 contracts, subprocess isolation, plugin install scanning, cron self-healing); v0.20.4 (Aug 18: NVIDIA SkillEvaluator, native OS notifications); v0.20.5 (Aug 19: keyless web tier, persistent cron memory, multi-question clarify); v0.20.6 (Aug 27: consent-gated browser profiles, 50+ vendor MCP catalog, TTL result caching, OS-keychain secrets); v0.21.0/v2026.8.31 'Pantheon' (Aug 31: ~5,800 commits / ~2,475 PRs / 760+ contributors; Bot Mode default-on with named agents and group chats, hermes peer, cron continuity + monitor-mode skipping the LLM, delegate_task list/steer/stop with JSON-schema outputs and defaults raised to 250 iterations / 10 concurrent, desktop browser driving, MCP command center, 6 new providers, protected agent-instruction files require write approval, Windows destructive-command approvals, macOS TCC signing, real-OS CI lanes). Scale: 241,911 stars / 49,683 forks on 2026-09-05 (https://api.github.com/repos/NousResearch/hermes-agent). Explicit non-features: no hash-chained action audit (issue #487 closed not planned), no spend cap (issue #26382 open), no kill switch or interrupt budget, no house model (4 HA tools + event adapter), no official mobile app (issue #11911).

## 6. Unverified or contradictory claims

- Contradiction not reconciled: Hermes delegation docs say 3 concurrent subagents / 50 iterations by default while v0.21.0 release notes say defaults raised to 10 concurrent / 250 iterations; docs page likely lags (https://hermes-agent.nousresearch.com/docs/user-guide/features/delegation vs https://github.com/NousResearch/hermes-agent/releases/tag/v2026.8.31).
- Hermes public launch date 2026-02-25 and intermediate star milestones (57.2k, 99k in 8 weeks, 188k, 214k) are secondary only (Dealroom page returned 403 through the proxy); 241,911 stars on 2026-09-05 is primary.
- Hermes skills-hub composition: primary hub page shows only 'Fetching 88k+ skills'; the 90,700 / 82 built-in / 117 official / 90,501 community / 11 registries breakdown is from agent37.com (secondary, 2026-08-26).
- Exact defaults of Hermes skills.write_approval and memory.write_approval are phrased as opt-in in docs but no explicit 'default: false' was read.
- Whether cua-driver (trycua/cua) is Nous-maintained or third-party; cua-driver 0.23.2 on PyPI 2026-08-31 was confirmed but the org relationship was not.
- Hermes issue #35164 (dangerous-command approval bypass in batch_runner.py) seen only in search results; status unknown. Raw CHANGELOG.md returned 404; release pages used instead. Hermes docs pages carry no last-updated dates; dates inferred from the introducing release.
- Anthropic GA date 2026-08-19 for computer_toolset_20260801 / browser_toolset_20260801 is from secondary posts; the docs are undated.
- OpenAI claims (ChatGPT agent watch mode/takeover wording, Auto-review red-team figures, Atlas sunset dates 2026-07-09/08-09, GPT-5.6 / ChatGPT Work details, Agents SDK April 2026 harness launch, 'Background Mode', Codex self-scheduling): openai.com and help.openai.com returned 403; relied on learn.chatgpt.com, TechCrunch, ppc.land and a third-party evaluation.
- Google 'User Alignment Critic' four-layer auto-browse security model and Google threat-intel injection figures: secondary only.
- Microsoft Build 2026 specifics (Windows MCP registry, Foundry Local), Copilot Studio computer-use GA announcement date, and the Windows Central 'rethinking Recall' report: pages failed to load or 403; inferred from Learn docs and snippets.
- Unit 42 broader ClawHub scan numbers (49,943 skills, 80% mismatch, 5% multi-stage), Bitdefender ~17%, VirusTotal partnership, Vidar stealing ~/.openclaw/openclaw.json (2026-02-13): secondary; the fetched Unit 42 post documents only five evasive skills.
- OpenClaw marketing stats ('3.2M MAU, 92% retention, 500K instances, 44K ClawHub skills') from openclawvps.io are unverified; the same site mislabels Steinberger joining OpenAI as an 'acquisition' — primary sources say foundation + sponsorship.
- Perplexity Computer launch details (2026-02-25, 19 models, Opus 4.6 orchestrator) and Personal Computer 'auditable and reversible' / 'kill switch' claims: perplexity.ai returned 403; MacRumors/TechCrunch only. Manus pricing tiers and Meta operational split by 2026-06-11: secondary. Meta's $2B Manus price: secondary.
- Benchmark inconsistencies: OSWorld-Verified rows (Qwen3.8-Max 86.1%, Claude Mythos/Fable 85%, Holo3-35B-A3B 82.6% on benchlm vs 77.8–78.85% on H's own pages) are self-reported and harness-dependent; OSWorld 2.0 official site shows Claude Opus 4.8 20.6% binary while secondary trackers list Claude Opus 5 31.43% and GPT-5.6 27.34%; ClawBench 33.3% and browser-use Playwright→CDP migration are community-sourced; UI-TARS-2 weights are absent from Hugging Face despite blog claims; WindowsAgentArena has no 2026 leaderboard found.
- macOS specifics: CGPreflightScreenCaptureAccess/CGRequestScreenCaptureAccess behaviour and Sequoia re-prompt cadence are from secondary blogs (Apple pages returned empty descriptions); Hammerspoon license/macOS 26 compatibility not read on primary; fazm.ai macOS AX failure-mode page returned 404.
- Linux specifics: xdg-desktop-portal release dates rendered inconsistently; RemoteDesktop persistence in 1.21.0 rests on Peter Hutterer's 2026-07-22 post; python-libei GNOME 50.4 end-to-end test seen only in a snippet; KDE portal behaviour with python-libei and kwin-mcp's portal-bypass claim not verified on a real session; mss Wayland unsupport documented only via GitHub issues; pynput limitations page is the 1.7.6 build though 1.8.2 is current; Windows-MCP Python 3.13+ (README) vs >=3.12 (PyPI) untested.
- Long-horizon/eval claims: Claude Code Routines launch date 2026-04-14 (secondary); METR Opus 4.6 horizon figures (secondary; METR says >16 h unreliable); GPT-5.5 GDPval 84.9% (secondary); '$47,000 / 11-day' and '$6,531 DN42' runaway incidents appear only in secondary blogs (the fetched dev.to $47 example is hypothetical); LangGraph durability modes (docs page loaded as stub); OpenHands default max_iterations=500 (search summary); TheAgentCompany post-2025 leaderboard (site empty); Lindy Agent Swarms mechanics and '400K+ users'; Cognosys→Ottogrid→Cohere; Devin 'July 2026 blitz'; Anthropic auto-mode 83%/17% figures are internal-only.
- Adoption figures from aggregators only: Ollama '52M monthly downloads' and '~9M users / $65M'; Claude Code run-rate and npm download figures; Telegram >1B MAU (Durov post not fetched); Meta AI MAU and WhatsApp share; Discord MAU/bot counts; skills.sh ~669K skills and install counts; Adjust D7 13%; GitHub Trending May 2026 and Product Hunt H1-2026 figures; Humane 100,000-unit plan.
- Repo-side: Nerva claims in the matrix come from the task context plus a quick read-only confirmation of agents/core/environments/execution.py (docker-only, explicit not-implemented refusals for local/ssh), the JARVIS_DESKTOP_HOST / JARVIS_PLAYWRIGHT_HOST / JARVIS_VLM_BACKEND / JARVIS_CHANNEL_PAIRING seams, and the kernel module layout (registry/syscalls/budget/binding); specific module names cited in the 'how' fields (e.g. context_compressor.py, checkpoint.py, autonomy/nl_schedule.py, product_posture.py) are as reported by the researchers and were not individually re-opened in this synthesis.

## 7. Source ledger (per researcher)


### ANGLE 1 — Hermes Agent (Nous Research) as of 2026-09-05: full feature inventory from primary sources (README, releases v2026.6.5→v2026.8.31, docs site), explici

| Topic | Claim | Source | Date | Confidence |
|---|---|---|---|---|
| Scale & community (primary) | GitHub API on 2026-09-05: 241,911 stars, 49,683 forks, 39,971 open issues, MIT license, repo created 2025-07-22, last push 2026-09-05T18:13Z (main is actively pushed daily). README badge shows 242k stars. v0.21.0 notes cite 760+ contributors since v0.20.0. | <https://api.github.com/repos/NousResearch/hermes-agent> | 2026-09-05 | high |
| Star trajectory (secondary) | Public launch reported as 2026-02-25; ~57.2k stars mid-April, ~99k after 8 weeks, 100k late April, 188k mid-June, 214k by late summer, 241.9k on 2026-09-05 (last figure primary). Average ~15k stars/week in Q2. | <https://app.dealroom.co/news/note/hermes-agent-hits-99k-github-stars-in-8-weeks-fastest-growing-open-source-agent-framework-of-2026> | 2026-04 | medium |
| Release cadence 2026-06→2026-09 (primary) | Releases: v2026.6.5 'Surface' (Jun 5: native desktop app macOS/Linux/Windows, in-app self-update, /undo); v2026.6.19 (Jun 19: iMessage via Photon, background async subagents, Automation Blueprints, WhatsApp Business Cloud API, atomic memory batch ops); v0.18.0/v2026.7.1 'Judgment' (Jul 1: MoA as selectable model, completion contracts via /goal, /learn, /journey, background subagent fan-out, scale-to-zero gateway, Vertex AI); v2026.7.7(.2) patches; v0.19.0/v2026.7.20 'Quicksilver' (Jul 20: ~80% TTFT cut, LLM-reviewer smart approvals, Bitwarden/1Password SecretSource, tail -f subagent transcripts, durable delivery ledger, multi-profile gateway, cron execution audit history, session export --redact); v0.19.1/v2026.7.30 (1,000+ PRs); v0.20.0/v2026.8.3 'Herald' (Aug 3); v0.20.1–v0.20.6 (Aug 13–27 patches); v0.21.0/v2026.8.31 'Pantheon' (Aug 31). No release found after Aug 31 as of Sept 5. | <https://github.com/NousResearch/hermes-agent/releases> | 2026-08-31 | high |
| v0.21.0 Pantheon (primary) | ~5,800 commits, ~2,475 PRs, ~2,100 issues closed since v0.20.0. Ships: Bot Mode 'bundled, default-on part of the desktop app' with named agents and Discord-style group chats; `hermes peer` bot-to-bot DMs; cron jobs with persistent memory ('continuity=true carries each run's output into the next'; 'Monitor-mode jobs skip the LLM entirely when nothing changed'); delegate_task live orchestration (list/steer/stop children, optional JSON-schema validation of child outputs, per-delegation cost, 'Raised defaults (250 iterations, 10 concurrent children)'); 'The agent drives the desktop's browser'; MCP command center with background health checks and 30-day usage overlay; Ctrl+P palette, /status shows pending approvals + context usage; 6 new providers; security: 'Protected agent-instruction files (AGENTS.md, skills, memory stores) now always require write approval', deep redaction sweep, Windows destructive-command approvals, stable macOS TCC signing; real-OS CI lanes for macOS and Windows. | <https://github.com/NousResearch/hermes-agent/releases/tag/v2026.8.31> | 2026-08-31 | high |
| v0.20.x patch train (primary) | v0.20.3 (Aug 16): MCP 2.x SDK migration + stateless protocol 2026-07-28, bundled Bot Mode plugin, 'Cua Driver 0.20 runtime contracts for computer use', PYTHONHOME/PYTHONPATH subprocess isolation, plugin install security scanning, cron self-healing. v0.20.4 (Aug 18): NVIDIA SkillEvaluator security scanning, native OS notifications. v0.20.5 (Aug 19): keyless web tier with free rotation, persistent memory for cron, multi-question clarify. v0.20.6 (Aug 27): consent-gated browser profile access, expanded MCP catalog (50+ vendors), TTL result caching, multi-query tool search, OS-keychain encryption for secrets. | <https://github.com/NousResearch/hermes-agent/releases/tag/v2026.8.16.2> | 2026-08-16 | high |
| v0.20.0 Herald (primary) | ~3,650 commits / ~1,400 PRs / 650+ contributors. 'Streaming, conversational TTS with barge-in across all surfaces'; on-device open-vocabulary wake words; hands-free 'stop'; configurable STT; A2A v1.0 plugin ('discover, talk to, and be driven by other A2A-compatible agents'); outbound webhooks 'Push signed lifecycle events to external endpoints' (HMAC); grounded-citations skill with fact-check; desktop Artifacts with sandboxed live preview + Plugin SDK (Kanban founding plugin) + global-hotkey quick entry; CLI `!cmd`, /init, /diff, /context, /focus, `hermes import-agent` (migrate Claude Code/Codex setups); cold start ~14s→~1.8s; per-model compression thresholds; strict redaction at compaction boundaries; Office skills bundled (docx/xlsx/pdf/pptx); MCP lazy server startup. | <https://github.com/NousResearch/hermes-agent/releases/tag/v2026.8.3> | 2026-08-03 | high |
| Computer use — mechanism (primary) | 'Hermes Agent can drive your desktop — clicking, typing, scrolling, dragging — in the background on macOS, Windows, and Linux.' Built-in `computer_use` toolset speaks 'MCP over stdio to cua-driver' (open-source, linked as trycua/cua). Per-OS stacks: macOS 'AX (private SkyLight SPIs)' + 'SLPSPostEventRecordTo — pid-scoped, no cursor warp'; Windows 'UIAutomation' + 'SendInput + PostMessage — no focus steal'; Linux 'AT-SPI (X11 + Wayland)' + 'XTest (X11) / virtual-keyboard (Wayland)'. NOT xdotool/AppleScript. Actions: capture (modes 'som' numbered overlay and 'ax' tree), click, type, key, drag, scroll, focus_app; target app by name (app="Mail"); pixel coordinates are the fallback when trees are sparse. | <https://hermes-agent.nousresearch.com/docs/user-guide/features/computer-use> | 2026-08 (Cua Driver 0.20 contracts shipped 2026-08-16) | high |
| Computer use — safety, enablement, limits (primary) | Off by default: enable via `hermes tools` → Computer Use or `hermes computer-use install` (installer pre-stages the driver). 'Destructive actions (click, type, drag, scroll, key, focus_app) require approval — either interactively via the CLI dialog or via the messaging-platform approval buttons.' Hard-blocked key combos: empty trash, force delete, lock screen, log out; blocked typed shell commands and password entry ('No keyboard password entry'). Limits: background events ~5–20 ms on macOS; Windows 'Medium-integrity process ... cannot enumerate the UIA tree of, or inject mouse input into, a window owned by a High-integrity (Administrator) process'; Wayland capture needs XWayland; macOS relies on private SPIs 'Apple can change them in any OS update'. | <https://github.com/NousResearch/hermes-agent/blob/main/website/docs/user-guide/features/computer-use.md> | 2026-08 | high |
| Browser automation stack (primary) | Multiple backends: 'Browser Use CLI 3.0' default driver; Lightpanda (Zig headless); local Chromium via `agent-browser`; Camofox (Firefox, fingerprint spoofing); cloud Browserbase / Browser Use Cloud / Firecrawl / Nous subscription gateway. Hybrid routing: 'Hermes auto-spawns a local Chromium sidecar for URLs that resolve to a private/loopback/LAN address' while public URLs use the cloud provider. Tools: browser_navigate, _click, _type, _scroll, _press, _back, _snapshot (a11y tree), _vision (screenshot+AI), _console (JS eval), _cdp (raw CDP passthrough), _dialog. Real-profile browsing is opt-in (`browser.use_real_profile: true`), copies a snapshot of the profile — 'Your live browser profile is never opened directly'. Installer runs `playwright install-deps chromium` (Playwright present). Desktop app can drive its in-app browser (v0.21.0). | <https://hermes-agent.nousresearch.com/docs/user-guide/features/browser> | 2026-08 | high |
| Terminal backends (primary) | Seven backends: local, docker, ssh, modal, daytona, vercel_sandbox, singularity (README: 'Run it on a $5 VPS, a GPU cluster, or serverless infrastructure'; 'Serverless persistence on Daytona/Modal (hibernates when idle)'). Security table: dangerous-command check runs on local and ssh, is '❌ Skipped (container is boundary)' on docker/modal/daytona/vercel_sandbox. Docker hardening: --cap-drop ALL (+DAC_OVERRIDE,CHOWN,FOWNER), no-new-privileges, pids-limit 256, tmpfs; container_cpu/memory/disk/persistent config. SSH requires TERMINAL_SSH_HOST + TERMINAL_SSH_USER. | <https://hermes-agent.nousresearch.com/docs/user-guide/security> | 2026-08 | high |
| Skills system (primary) | SKILL.md (name/description/version required; optional platforms, metadata.hermes.tags/category/config, required_environment_variables), 'compatible with the agentskills.io open standard'. Progressive disclosure: 'Level 0: skills_list() ... (~3k tokens)', 'Level 1: skill_view(name)', 'Level 2: skill_view(name, path)'. Skills live in ~/.hermes/skills/ plus project-local .hermes/skills/ and .agents/skills/ and configurable external_dirs. Agent self-authors via `skill_manage` with actions create / patch ('preferred for token efficiency') / edit / delete / write_file / remove_file; system prompt nudges to 'record a non-trivial workflow with skill_manage'; a 'background self-improvement review can suggest skill changes after sessions'. `/learn` distills SDK dirs, doc URLs or corpora into 'knowledge-base skills' (lean SKILL.md + index + references/). Optional gate: 'When skills.write_approval: true, every skill write is staged under ~/.hermes/pending/skills/'. | <https://hermes-agent.nousresearch.com/docs/user-guide/features/skills> | 2026-08 | high |
| Skills hub / ecosystem size | Hub has 8 integrated sources: official, skills.sh (Vercel), well-known (/.well-known/skills/index.json), direct GitHub taps (default taps incl. OpenAI, Anthropic, HuggingFace, NVIDIA, Gstack), ClawHub, LobeHub, browse.sh (200+ site-specific browser skills), direct URL; users publish 'taps'. All hub installs pass a 'security scanner that checks for data exfiltration, prompt injection, destructive commands, supply-chain signals'; trust levels builtin/official/trusted/community; optional NVIDIA SkillEvaluator (PII, unicode smuggling, lint, license, SkillSpector); project-local skills get 'scan-time quarantine'. Official hub page loads with 'Fetching 88k+ skills across every registry' (primary, 2026-09-05); secondary breakdown as of 2026-08-26: 90,700 = 82 built-in + 117 official optional + 90,501 community across 11 registries. | <https://hermes-agent.nousresearch.com/docs/skills/> | 2026-09-05 | medium |
| Skill self-evolution (official side repo) | NousResearch/hermes-agent-self-evolution: 'DSPy + GEPA (Genetic-Pareto Prompt Evolution) to automatically evolve and optimize Hermes Agent's skills, tool descriptions, system prompts, and code'. Phase 1 (skill files) complete; phases 2–5 (tool descriptions, system prompts, code, continuous pipeline) 'Planned'. 'No GPU training', '~$2-10 per optimization run', variants must pass tests, skills ≤15KB, human review before merge. 5.3k stars / 621 forks. | <https://github.com/NousResearch/hermes-agent-self-evolution> | 2026-09-05 | high |
| Built-in memory (primary) | Two files in ~/.hermes/memories/: MEMORY.md (2,200 chars ≈800 tokens) and USER.md (1,375 chars ≈500 tokens), 'injected into the system prompt as a frozen snapshot at session start' (prefix-cache friendly); `memory` tool add/replace/remove, overflow 'returns an error instead of silently dropping entries'; `session_search` = 'FTS5 full-text search' over ~/.hermes/state.db returning raw messages; `memory.write_approval: true` stages writes for `/memory pending`. '8 external memory provider plugins — including Honcho, OpenViking, Mem0, Hindsight, Holographic, RetainDB, ByteRover, and Supermemory' run 'alongside built-in memory (never replacing it)', one active at a time. /journey timeline + desktop memory graph (v0.18.0). | <https://hermes-agent.nousresearch.com/docs/user-guide/features/memory> | 2026-08 | high |
| Honcho (primary) | Optional skill: `hermes skills install official/autonomous-ai-agents/honcho`; cloud (app.honcho.dev key) or self-hosted base URL. 'AI-native cross-session user modeling'; user peer + per-profile AI peer; auto-injects session summary + user representation + AI peer card before every turn; recall modes hybrid/context/tools; dialectic reasoning tuned by cadence (default 2), depth (1–3), level (minimal→max); tools honcho_profile/search/context/reasoning/conclude. | <https://hermes-agent.nousresearch.com/docs/user-guide/skills/optional/autonomous-ai-agents/autonomous-ai-agents-honcho> | 2026-08 | high |
| Cron / scheduling (primary) | Single `cronjob` tool: create/list/update/pause/resume/run/remove. Schedules: 'in 30m', 'every 2h', 'every monday 9am', cron expressions, ISO timestamps. Delivery to Telegram/Discord/Slack/WhatsApp/Signal/Matrix/Mattermost/Email/SMS/Home Assistant/DingTalk/Feishu/WeCom/WeChat/BlueBubbles/QQ/bot-chat or files. `context_from` chains outputs; `continuity=true` re-injects last output; `no_agent=True` script-only ('No tokens, no model'); pre-run script `{"wakeAgent": false}` gate at $0. 'The gateway ticks the scheduler every 60 seconds, running any due jobs in isolated agent sessions'; history in ~/.hermes/cron/executions.db (claimed/running/completed/failed/unknown); 'Cron-run sessions cannot recursively create more cron jobs'; cron.max_parallel_jobs; approvals `cron_mode: deny\|approve`. | <https://hermes-agent.nousresearch.com/docs/user-guide/features/cron> | 2026-08 | high |
| Gateway / messaging platforms (primary) | 27 platforms documented: Telegram, Discord, Slack, Google Chat, WhatsApp, WhatsApp Cloud API, Signal, SMS, Email, Home Assistant, Mattermost, Matrix, DingTalk, Feishu/Lark, WeCom, WeCom Callback, Weixin, BlueBubbles (iMessage), Photon (iMessage), QQ, Yuanbao, Microsoft Teams, LINE, ntfy, SimpleX Chat, IRC, Buzz. Gateway = 'a single background process that connects to all your configured platforms, handles sessions, runs cron jobs, and delivers voice messages'. Auth: allowlists (recommended) or DM pairing ('8-char from 32-char unambiguous alphabet', 1h TTL, 1 req/10 min, 5 failures → 1h lockout, chmod 0600). Approvals via chat reply (yes/no) or native buttons; approval timeout 300 s 'denied by default (fail-closed)'. Multi-profile routing per guild/channel/thread; scale-to-zero 'dormant-quiesce'. | <https://hermes-agent.nousresearch.com/docs/user-guide/messaging/> | 2026-08 | high |
| Subagents / delegation (primary) | `delegate_task`: 'Subagents start with a completely fresh conversation' (inherit AGENTS.md/CLAUDE.md/.cursorrules), 'Up to 3 concurrent subagents by default' (docs) — release notes v0.21.0 say defaults raised to 10 concurrent / 250 iterations (docs page may lag); per-child `max_iterations` (docs default 50); orchestration actions list/steer/stop; leaf children cannot use 'delegate_task, clarify, memory, send_message, cronjob'; structured summary return; background fan-out with one consolidated return; 'frontier planner, inexpensive workers' pattern; tail -f transcripts. | <https://hermes-agent.nousresearch.com/docs/user-guide/features/delegation> | 2026-08 | high |
| Context compression & caching (primary) | Auto-compression at '50% of context (default)' with gateway safety net at '85% of context length'; 'System prompt + first exchange always preserved' plus `protect_last_n: 20` tail; `target_ratio` 0.20; modes 'lean' (~2.5% of window with recovery pointers) vs 'legacy' (verbatim tail); per-model overrides ('longest matching key wins'); summary model 'must have a context window at least as large as the main agent model's'; iterative re-summarization (previous summary updated, not regenerated); sessions rewritten in place with parent_session_id lineage; pluggable 'ContextEngine ABC' (e.g. Lossless Context Management); Anthropic prompt caching 'system_and_3'; /compress forces, /context shows breakdown, desktop context-usage meter. | <https://hermes-agent.nousresearch.com/docs/developer-guide/context-compression-and-caching/> | 2026-08 | high |
| MCP (primary) | Native MCP client: stdio subprocesses, HTTP endpoints, and OAuth HTTP ('Hermes handles discovery, client identification, PKCE, token exchange, refresh, and step-up auth'). `hermes mcp serve` exposes Hermes as a stdio MCP server to Claude Code/Cursor/Codex. Curated catalog 'disabled by default — install only what you actually want', entries added 'by merging a PR' (50+ vendors per v0.20.6). Stdio env isolation: only PATH/HOME/USER/LANG/LC_ALL/TERM/SHELL/TMPDIR/XDG_* + explicit env — 'All other environment variables (API keys, tokens, secrets) are stripped'. include/exclude globs; sampling & elicitation 'enabled by default' with rate limits and approval gates; tools prefixed mcp_<server>_<tool>; 'Invisible Unicode TAG characters are stripped'; supports notifications/tools/list_changed; MCP 2.x SDK. | <https://hermes-agent.nousresearch.com/docs/user-guide/features/mcp> | 2026-08 | high |
| Model providers incl. local (primary) | ~50 named providers (Nous Portal, OpenAI Codex, GitHub Copilot, Anthropic, OpenRouter, Fireworks, DeepSeek, Google/Gemini, Vertex, Azure AI Foundry, AWS Bedrock, NVIDIA Build, xAI, Kimi, MiniMax, Qwen, Meta Model API, etc.). Local/self-hosted: 'Ollama — Local Models, Zero Config', vLLM (`--enable-auto-tool-choice --tool-call-parser hermes`), SGLang, llama.cpp/llama-server (`--jinja`), LM Studio, LiteLLM proxy, ClawRouter, 'any OpenAI-compatible API endpoint'; custom endpoints 'queries your endpoint's /models API and auto-selects the model if exactly one is loaded'; named custom providers (`/model custom:local:qwen-2.5`); auxiliary model for vision/summaries/MoA; fallback provider chains; `/model --once`; MoA presets as models; `model_overrides` for context/pricing. Nous Portal '300+ models' (README) / 400+ (v0.19 era) + Tool Gateway (web search, image gen, TTS, browser) for subscribers. | <https://github.com/NousResearch/hermes-agent/blob/main/website/docs/integrations/providers.md> | 2026-08 | high |
| Approval / safety model (primary) | `approvals.mode`: smart (default — 'Use an auxiliary LLM to assess risk. Low-risk commands are auto-approved ... Uncertain cases escalate'), manual, off ('equivalent to running with --yolo'); unattended contexts `cron_mode`, `single_query_mode`, `unattended_mode` each deny\|approve. YOLO via --yolo, /yolo, HERMES_YOLO_MODE=1 (red banner). 'Hardline blocklist' (rm -rf /, fork bombs, mkfs on root, dd to /dev/sd*) 'execute nowhere—not even under --yolo'. `approvals.deny` fnmatch rules evaluated before yolo; protected paths hard-blocked (~/.ssh, ~/.aws, ~/.kube, /etc/sudoers, auth.json, .env*); HERMES_WRITE_SAFE_ROOT sandbox; CLI prompt '[o]nce \| [s]ession \| [a]lways \| [d]eny'; `hermes approvals suggest` mines history but excludes destructive classes; context-file injection scanning (AGENTS.md/SOUL.md/.cursorrules); SSRF guard (RFC1918, link-local, CGNAT, metadata hosts) with `security.allow_private_urls`; tirith pre-exec scanner (default on, fail-open); website_blocklist; supply-chain advisory checks at startup/doctor; lazy-install allowlist; credential redaction in errors. | <https://hermes-agent.nousresearch.com/docs/user-guide/security> | 2026-08 | high |
| What Hermes does NOT do — audit chain (primary) | No tamper-evident action audit. Issue #487 'Cryptographic Audit Trail — SHA-256 Hash-Chained Action Log' (opened 2026-03-06) is 'Closed as not planned'; it documents current state as 'SQLite database with sessions and messages tables', 'Simple text log, no chaining' via append_audit_log() for skill ops only, 'No structured action log', no 'cryptographic chaining or tamper detection'. Only cron has an executions.db history (v0.19.0 'Durable execution audit history'). Security docs mention no Merkle/hash chain. | <https://github.com/NousResearch/hermes-agent/issues/487> | 2026-03-06 | high |
| What Hermes does NOT do — spend caps / budgets / kill switch (primary) | Issue #26382 'Configurable daily/monthly hard budget cap for unattended operation' (opened 2026-05-15) is OPEN: 'Running Hermes unattended has no mechanism to cap LLM spending'; only /usage, /insights and a rate guard exist. No kill-switch, interrupt budget, or graduated autonomy ladder appears in security/cron/bot-mode docs. Community fills the gap with third-party plugins (agentguard-spend-hermes on PyPI with Ed25519-signed audit logs; nujovich/hermes-telemetry budget enforcement). | <https://github.com/NousResearch/hermes-agent/issues/26382> | 2026-05-15 | high |
| What Hermes does NOT do — house model / physical (primary) | Home Assistant integration = 4 tools (ha_list_entities, ha_get_state, ha_list_services, ha_call_service) auto-enabled when HASS_TOKEN is set, plus a gateway adapter that subscribes to `state_changed` over WebSocket and forwards filtered events as messages. No world/house model, no camera/Frigate integration, no media director, no risk tiering of device actions (ha_call_service is a generic tool subject only to dangerous-command patterns). | <https://hermes-agent.nousresearch.com/docs/user-guide/messaging/homeassistant> | 2026-08 | high |
| What Hermes does NOT do — governance primitives (inferred from primary docs) | Hermes has no action-kind contract layer (approval is string-pattern + LLM-reviewer on shell commands and a fixed destructive list for computer_use), no taint escalation from untrusted sources (only context-file injection scanning), no money/locks/security 'never above approval queue' invariant, and dangerous-command checks are skipped entirely on container/cloud backends. Governance is per-tool and opt-in (write_approval flags default off; smart mode auto-approves). | <https://hermes-agent.nousresearch.com/docs/user-guide/security> | 2026-08 | medium |
| Install path & platform tiers (primary) | Recommended: desktop installers for macOS/Windows from the site; CLI: `curl -fsSL https://hermes-agent.nousresearch.com/install.sh \| bash` (Linux/macOS/WSL2/Termux) and `iex (irm https://hermes-agent.nousresearch.com/install.ps1)` (Windows). Installer manages Python 3.11, Node v26, ripgrep, ffmpeg. Docker image nousresearch/hermes-agent ('Docker installs do not support hermes update'). Tier 1: macOS Apple Silicon, Windows 10/11 x86_64+aarch64, Linux/WSL2, Docker; Tier 2: Android/Termux, Nix; unsupported: Intel macOS, and 'Installs via AUR, PyPI, or Homebrew'. Post-install: `hermes`, `hermes model`, `hermes tools`, `hermes setup --portal`, `hermes doctor`. | <https://hermes-agent.nousresearch.com/docs/getting-started/installation> | 2026-08 | high |
| Voice (primary) | STT: local 'pip install faster-whisper' (base ~150 MB, small, large-v3; 'voice mode works with zero API keys for STT'), or Groq/OpenAI/Mistral/xAI cloud. TTS: Edge TTS free (322 voices/74 languages), NeuTTS local, ElevenLabs, OpenAI. Barge-in 'interrupt the agent at ANY point'; on-device open-vocabulary wake words (separate wake-word page); CLI Ctrl+B; Telegram/Discord voice bubbles (Opus/OGG); bot can join Discord voice channels; 26-phrase hallucination filter. Fully local voice is possible. | <https://hermes-agent.nousresearch.com/docs/user-guide/features/voice-mode> | 2026-08 | high |
| Desktop app (primary) | Hermes Desktop = Electron shell + React chat surface over a headless Python `hermes serve` backend ('reuses the agent runtime rather than embedding hermes --tui'), macOS/Windows/Linux, 'same config, same API keys, same sessions, same skills, same memory' as CLI/gateway. Features: streaming with live tool activity, drag-drop files, preview rail with in-app browser + comment mode (CSS selectors), Artifacts gallery, Bot Mode roster + Routines, Agents/Command Center, MCP server management, context-usage meter, cache-hit/tokens-per-sec status, plugin SDK ($HERMES_HOME/desktop-plugins/), quick-entry global hotkey, native notifications, translucency UI. | <https://hermes-agent.nousresearch.com/docs/user-guide/desktop> | 2026-08 | high |
| Mobile | No official Nous Research iOS/Android app: installation/platform docs list only desktop + CLI + Termux; feature request issue #11911 'Native Mobile App (iOS & Android) with Voice Calling' remains a request. Third-party clients exist (Hermex on iOS App Store since June 2026, 'Hermes Agent Client - OnDev' and others on Google Play) that connect to a self-hosted Hermes/hermes-webui API. | <https://github.com/NousResearch/hermes-agent/issues/11911> | 2026-06 | medium |
| Bot Mode / multi-agent 'company' (primary) | 'a Bot is a Hermes profile' (~/.hermes/profiles/<name>/) with own role, model, memory, skills, SOUL.md, avatar and 'Per-skill, per-toolset, and per-MCP-server enablement'. `message_agent(target=..., message=...)` fire-and-forget bot-to-bot with roster+roles in every system prompt; group chats run 'up to three serial rounds', hard caps '10 messages per send, 3 rounds', members may pass; Routines = cron jobs namespaced '[bot:<name>]'; cross-machine via desktop Connections relay or `hermes peer add <name> --url http://host:8377 --key <API_SERVER_KEY>` / `hermes peer dm`; machine-readable failure reason codes (provider_auth_or_access, context_overflow, runtime_offline); retry at most once; CLI parity `hermes -p <bot> chat`. | <https://hermes-agent.nousresearch.com/docs/user-guide/bot-mode> | 2026-08-31 | high |
| API server / A2A / webhooks (primary) | OpenAI-compatible API server inside `hermes gateway` (API_SERVER_ENABLED=true, bearer API_SERVER_KEY 'required for every deployment, including the default loopback bind', port 8642, host 127.0.0.1): POST /v1/chat/completions, GET /v1/models (advertises profile name), POST /v1/responses with previous_response_id; SSE 'event: hermes.tool.progress'; per-profile servers on separate ports. A2A v1.0 plugin and HMAC-signed outbound lifecycle webhooks shipped in v0.20.0 (release notes; not on the API-server page). | <https://hermes-agent.nousresearch.com/docs/user-guide/features/api-server> | 2026-08 | high |
| Architecture (primary) | Synchronous `AIAgent` loop handling 'provider selection, prompt construction, tool execution, retries, fallback, callbacks, compression, and persistence'; 'central tool registry (tools/registry.py) with 70+ registered tools across ~28 toolsets' (docs homepage says '60+ built-in tools', '20+ platforms'); SQLite FTS5 sessions with lineage; ShareGPT trajectory export for training; gateway with '25+ platform adapters'; plugins from ~/.hermes/plugins, project .hermes/plugins, pip entry points; memory-provider and context-engine plugin types, one active each. Tools list: web_search, web_extract, x_search, terminal, process, read_file, patch, browser_*, vision_analyze, image_generate, text_to_speech, todo, clarify, execute_code (Python RPC tool calling), delegate_task, memory, session_search, cronjob, skill_manage, computer_use, ha_*, mcp_*. | <https://hermes-agent.nousresearch.com/docs/developer-guide/architecture> | 2026-08 | high |
| Completion contracts / work verification (primary) | v0.18.0: 'the agent verifies its own work — done means proven, not claimed'; agents 'decide it's finished by actually running your project's checks'; '/goal gained completion contracts: you state what done looks like'. Also 'post-turn self-improvement fork now routes to an auxiliary model'. | <https://github.com/NousResearch/hermes-agent/releases/tag/v2026.7.1> | 2026-07-01 | high |
| S1 bar — execution breadth Nerva must match/exceed | Hermes: background, accessibility-first computer use on macOS (AX), Windows (UIA), Linux (AT-SPI, X11+Wayland) with SoM/ax capture and pixel fallback, pid-scoped input that does not steal cursor/focus; 11 browser tools incl. raw CDP and consent-gated real-profile snapshot, 5+ browser engines with LAN-local routing; 70+ tools; 27 chat platforms; local voice (faster-whisper + on-device wake word + barge-in); HA tools + event gateway. Nerva today: Windows-only pywinauto/UIA driver (owner seam), Playwright governed browser, Telegram/Discord/Slack/email/voice. Gap: macOS + Linux drivers, background/pid-scoped input, SoM capture, CDP passthrough, event-driven HA, wake word. | <https://hermes-agent.nousresearch.com/docs/user-guide/features/computer-use> | 2026-09-05 | high |
| S2 bar — skill acquisition Nerva must match/exceed | Hermes: agent-authored skills via skill_manage with token-cheap `patch`, prompt-level nudges + post-session self-improvement review (auxiliary model), /learn ingestion into knowledge-base skills with references/, progressive disclosure (L0 ≈3k tokens), 8-source hub with trust tiers + scanner + NVIDIA SkillEvaluator + quarantine, 88k+ indexed skills, official GEPA/DSPy evolution pipeline (phase 1). Nerva today: gap→research→generate→sandbox→approve→registry loop, 78 pinned Hermes skills, signing, marketplace, CDX-8 PENDING_REVIEW quarantine. Gap: patch-level refinement loop, /learn-style corpus→skill distillation, progressive disclosure, multi-source hub + scanner, evolutionary optimisation with benchmarks. | <https://hermes-agent.nousresearch.com/docs/user-guide/features/skills> | 2026-09-05 | high |
| S3 bar — multi-target execution Nerva must match/exceed | Hermes: 7 real backends (local/docker/ssh/modal/daytona/vercel_sandbox/singularity), serverless hibernate/wake, per-backend env passthrough policy, docker capability hardening, Bot Mode cross-machine peers over authenticated API (port 8377), A2A v1.0. Nerva today (agents/core/environments/execution.py): 'The only wired backend is docker'; 'local and ssh return explicit not-implemented refusals' though the policy plane (targets.py) already has allow/approval_required/deny + hash-chained audit. Gap: ship ssh + local transports under that policy plane, then optional serverless target, and a peer protocol for multi-node Nerva. | <https://hermes-agent.nousresearch.com/docs/user-guide/security> | 2026-09-05 | high |
| S4 bar — context endurance Nerva must match/exceed | Hermes: 50%/85% two-tier compression, protected head + protect_last_n tail, iterative re-summarization, session lineage, pluggable context engines (incl. lossless), per-model thresholds, prompt-cache-preserving frozen memory snapshot, /context meter, cron continuity + notepad, durable delivery ledger with redelivery on reboot, delegate_task with 250-iteration children. Nerva: conversation + vector store + bi-temporal KG, no documented compaction policy. Gap: explicit compaction engine with lineage + cache-aware protected head, continuity for night-shift/observer runs, delivery ledger. | <https://hermes-agent.nousresearch.com/docs/developer-guide/context-compression-and-caching/> | 2026-09-05 | high |

### ANGLE 2 — Computer-use and desktop-agent products in 2026 (Anthropic, OpenAI, Google, Microsoft, Apple, Manus, Perplexity, OpenClaw, browser-use, Skyvern, MCP d

| Topic | Claim | Source | Date | Confidence |
|---|---|---|---|---|
| Anthropic computer use tool — current version and action set | Production toolset is `computer_toolset_20260801` (no beta header): 17 member tools (screenshot, zoom, left/right/middle/double/triple_click, left_click_drag, mouse_move, left_mouse_down/up, cursor_position, type, key, hold_key, scroll, wait). Supported models: claude-fable-5-1, claude-mythos-5-1, claude-fable-5, claude-mythos-5, claude-opus-5, claude-sonnet-5, claude-opus-4-8; older `computer_20251124` still serves Opus 4.7/4.6/4.5, Sonnet 4.6. Perception is screenshots + pixel coordinates only (no accessibility tree); `zoom` inspects a region at full resolution. Available on Claude API and Vertex (full), Bedrock/Foundry (beta), not in Claude Managed Agents; ZDR-eligible. | <https://platform.claude.com/docs/en/docs/agents-and-tools/tool-use/computer-use-tool> | undated doc, read 2026-09-05; GA reported 2026-08-19 by secondary sources | high |
| Anthropic computer use — batch actions semantics | Claude may emit several member tool_use blocks in one turn (e.g. left_click → type → screenshot). Executor must run them sequentially, stop at the first failure, and answer every skipped block with is_error=true and the exact text 'Not executed: an earlier computer action in this turn failed.' The official computer-use-demo quickstart implements the same sequential-stop-on-failure semantics. | <https://github.com/anthropics/claude-quickstarts/tree/main/computer-use-demo> | read 2026-09-05 | high |
| Anthropic computer use — cost/latency numbers | A typical screenshot costs 1,000–1,800 input tokens; modern models accept up to 2576 px on the long edge / ~4784 visual tokens (~3.75 MP) per image, older models 1568 px / 1.15 MP. Recommended: prompt-cache breakpoints after system+tools and on the last tool_result, prune old screenshots in batches (every ~25 turns) rather than one per turn; once >20 images are in a request stricter per-side limits apply. Secondary sources put declaration overhead at ~4,500 input tokens for computer use and ~6,600 for browser use. | <https://platform.claude.com/docs/en/docs/agents-and-tools/tool-use/computer-use-tool> | read 2026-09-05 | high |
| Anthropic computer use — safety model | Docs require: dedicated VM/container with minimal privileges; no credentials/PII in the environment; internet limited to an allowlist of domains; human confirmation for consequential actions (financial transactions, accepting ToS/cookies, anything with real-world consequences). Anthropic runs classifiers that flag prompt injections in screenshots and automatically steer the model to ask the user for confirmation before proceeding; this can be disabled via support. The demo README states plainly that 'Claude will follow commands found in content even if it conflicts with the user's instructions'. | <https://platform.claude.com/docs/en/docs/agents-and-tools/tool-use/computer-use-tool> | read 2026-09-05 | high |
| Anthropic browser use tool — a11y-first hybrid perception | `browser_toolset_20260801` is a separate client toolset with 31 members. It perceives pages 'both through its structure (the accessibility tree, elements, forms, and tabs) and through pixels (screenshots and viewport coordinates)'. `read_page` returns an accessibility tree with element refs like [ref_2]; `find` does natural-language element search (≤20 matches); `form_input` sets values directly; targets can be {type:'ref'} (survives layout shifts) or {type:'coordinate'}. Tab state is returned as a full `browser_state` inventory (≤100 tabs, ≤200 state changes). `javascript_exec`, `file_upload`, `read_console`, `read_network` are disabled by default. The executor runs in the developer's environment, not Anthropic's. | <https://platform.claude.com/docs/en/docs/agents-and-tools/tool-use/browser-use-tool> | read 2026-09-05 | high |
| Anthropic browser use tool — mandated safety controls | Docs mandate: run the executor in a dedicated container/VM with a fresh profile and no stored credentials; enforce a domain allowlist at the network layer AND re-check in the navigate handler after redirects; block loopback/link-local/private ranges; refuse any scheme other than http/https (javascript:, file:, data:, chrome:); treat all page content as untrusted; use low-privilege accounts for logged-in sessions; redact credentials from console/network logs; truncate suspicious tab titles/URLs; require human confirmation for purchases, account modifications, messaging, terms acceptance. | <https://platform.claude.com/docs/en/docs/agents-and-tools/tool-use/browser-use-tool> | read 2026-09-05 | high |
| Claude Cowork — platforms and approval modes | Cowork runs in Claude Desktop (macOS, Windows), web, mobile (iOS/Android) and the Chrome side panel, on paid plans (Pro, Max, Team, Enterprise). It accesses local files via folder permissions, a browser (built-in or Claude in Chrome), authorized connectors/MCPs, and web search. Three approval modes: Manual ('Claude pauses and asks for approval for actions'), Auto (works independently but runs safety checks and 'automatically block[s] anything it determines to be unsafe'), Skip (no checks). Explicit permission is always required before permanently deleting files. | <https://support.claude.com/en/articles/13345190-get-started-with-claude-cowork> | help article, 'updated this week' as of read 2026-09-05 | high |
| Claude Cowork computer use — tool hierarchy and per-app permission | Announced 2026-03-23: in Cowork and Claude Code, Claude can 'point, click, and navigate what's on your screen'. Tool preference order is connectors first (Slack, Calendar), then browser, then direct control of 'browser, mouse, keyboard, and screen'. Claude 'will always request permission before accessing new applications'; some apps are off-limits by default (help center names investment platforms and cryptocurrency) and users can add apps to a blocklist. Available to Pro and Max only (not Team/Enterprise), macOS and Windows; the desktop must be awake and the app open. On macOS 15+ Claude works in background windows by default, with optional full screen control. Perception is screenshot-based; macOS requires Accessibility and Screen Recording permissions. | <https://claude.com/blog/dispatch-and-computer-use> | 2026-03-23 | high |
| Claude Cowork built-in browser — isolated profile pattern | Published 2026-08-26: Cowork gets its own browser inside the desktop app; 'Claude never sees your tabs, bookmarks, or passwords'. Logins can be imported site-by-site from Chrome/Edge/Firefox, with banking and SSO sites excluded by default. Runtime checks 'review Claude's actions against what you asked for' plus prompt-injection defenses that 'can't eliminate' the risk. macOS, Windows, Linux (beta); Pro/Max/Team, Enterprise when an owner enables it; enabled by default on rollout. | <https://claude.com/blog/cowork-built-in-browser> | 2026-08-26 | high |
| Claude in Chrome GA — per-action classifier and injection numbers | GA on every paid plan 2026-08-26. Claude can now act autonomously in the browser without per-action approval; 'a safety classifier validates each action before it's performed', and users can switch back to manual approval. Reported prompt-injection attack success: Sonnet 5 / Opus 5 'no attacks succeeded'; Fable 5 0.3% with full safeguards; Opus 4.5 baseline 17.6% without safeguards. Help center adds: <0.08% ASR in internal testing; adult and pirated sites blocked; financial sites require prior permission; Team/Enterprise admins can set allow/blocklists; 'Manually approve' mode available. | <https://claude.com/blog/claude-in-chrome-generally-available> | 2026-08-26 | high |
| Incident — ShadowPrompt zero-click injection in Claude Chrome extension | Reported 2026-03-26: Koi Security researcher Oren Yomtov (disclosed 2025-12-27) chained an over-permissive origin allowlist (any `*.claude.ai` subdomain) with a DOM-based XSS in an Arkose Labs CAPTCHA component on a-cdn.claude.ai; a hidden iframe + postMessage let any website inject prompts 'as if the user wrote them'. Patched in extension v1.0.41 with tightened origin validation. | <https://thehackernews.com/2026/03/claude-extension-flaw-enabled-zero.html> | 2026-03-26 | medium |
| OpenAI — Atlas retired, computer use moved into ChatGPT desktop/Codex | OpenAI began sunsetting the standalone Atlas browser on 2026-07-09 (launched 2025-10-21) and folded agentic browsing into ChatGPT Work (GPT-5.6) and a unified desktop app with Codex; a 'Computer Use' feature lets ChatGPT 'operate a person's computer directly... clicking, typing, and moving files'. 'Auto-review' has advanced models review significant actions before execution. Rollout began for Pro/Enterprise/Edu with Plus/Business following; usage scales with task complexity. | <https://ppc.land/openai-kills-atlas-browser-folds-it-into-new-chatgpt-work-agent/> | 2026-07-09 | medium |
| OpenAI Computer Use (desktop) — perception, permissions, limits | ChatGPT Computer Use can 'see and operate graphical user interfaces on macOS or Windows' for tasks where CLI tools or integrations are not enough. It perceives via screenshots (macOS Screen Recording permission) and acts via Accessibility permission. Per-app permission prompts with 'Always allow'; grants revocable in settings. It cannot automate terminal apps or ChatGPT itself, cannot authenticate as administrator; on Windows it runs in the foreground only (cannot work in the background of the same session); macOS offers optional 'locked use' to keep working after screen lock. Available in supported regions with ChatGPT Work and Codex. | <https://learn.chatgpt.com/docs/computer-use> | read 2026-09-05 (undated doc) | high |
| OpenAI Auto-review and permission modes — reviewer-agent pattern | Permission modes: 'Ask for approval' (default; read/edit workspace files and routine local commands, pause beyond that or for internet), 'Approve for me' (Auto-review: a separate reviewer agent decides boundary-crossing requests — escalated shell calls, blocked network, edits outside allowed dirs, MCP/app tools, Computer Use access to new domains), and 'Full access'. The sandbox (which files/network) is independent from the approval path; switching to Auto-review does not widen the sandbox. 'Computer Use is a separate case. App approvals for Computer Use still surface directly to the user'. Docs state Auto-review 'is not a deterministic security guarantee'. | <https://learn.chatgpt.com/docs/sandboxing/auto-review> | read 2026-09-05 (undated doc) | high |
| OpenAI ChatGPT agent (cloud virtual computer) — watch mode / takeover | The agent runs in a virtual computer 'exclusively in OpenAI's infrastructure' (browser, limited-network terminal, file tools never touch the device). Safety controls: 'watch mode requiring active user supervision on sensitive sites (email, banking) that pauses when the user goes inactive'; 'takeover mode lets users enter credentials themselves without the model seeing or storing them'; explicit confirmation before consequential actions (purchases, sending email, editing files); refusal of high-risk tasks such as bank transfers; prompt injection called 'a core unsolved risk'. (openai.com and help.openai.com returned 403 via proxy; claims taken from a third-party evaluation quoting the system card and from OpenAI help-center snippets.) | <https://trustvector.guard0.ai/agents/chatgpt-agent> | evaluation dated 2026-07-09 | medium |
| Google — Project Mariner discontinued | Project Mariner (research-prototype Chrome extension announced 2024-12-11, rolled to AI Ultra US 2025-05-20, ran tasks in cloud VMs) was discontinued 2026-05-04; Google points users to Gemini Agent, AI Mode in Search and Chrome auto browse. | <https://en.wikipedia.org/wiki/Project_Mariner> | 2026-05-04 (shutdown) | medium |
| Google Chrome auto browse — approval and takeover model | Chrome auto browse (Gemini 3) launched 2026-01-28 in preview for AI Pro and Ultra subscribers in the U.S. on Windows, macOS and Chromebook Plus. It scrolls, clicks and enters text in a new tab while showing every step; it is 'designed to pause and explicitly ask for your confirmation or prompt you to complete some tasks like making a purchase or posting on social media', with a 'Take over task' control; it can use Google Password Manager for sign-ins only after the user authorizes it. Secondary coverage describes a four-layer security model including a 'User Alignment Critic' that vetoes risky actions. At I/O (2026-05-19) Google added WebMCP (origin trial in Chrome 149) for sites to expose tools, and Chrome DevTools for agents (console, network, accessibility trees). | <https://blog.google/products-and-platforms/products/chrome/gemini-3-auto-browse/> | 2026-01-28 (auto browse); 2026-05-19 (I/O) | high |
| Google Gemini Spark / Gemini Agent — ask-first for high-stakes actions | Announced 2026-05-19: Gemini Spark is 'a 24/7 personal AI agent' with recurring tasks/triggers and teachable skills that 'keeps working in the background even when you close your laptop'. It is 'designed to ask you first before performing high-stakes actions like spending money or sending emails'; users choose which apps it connects to. Beta for U.S. Google AI Ultra; a macOS app 'later this summer' will let it operate the local browser and local files. Gemini Agent similarly seeks confirmation before critical actions and can be paused or taken over at any time. | <https://blog.google/innovation-and-ai/products/gemini-app/next-evolution-gemini-app/> | 2026-05-19 | high |
| Microsoft Copilot Studio computer use — scope, models, pricing | Computer use lets an agent operate 'a Windows computer... websites and desktop apps' with a virtual mouse/keyboard, powered by CUA-style vision+reasoning models: OpenAI CUA and Claude Sonnet 4.5 (GA), Claude Sonnet 4.6 and Opus 4.6 (experimental). Billing: 5 Copilot Credits per step (15 for premium Opus 4.6); a 4-step timesheet example costs 20 credits. Runs on: hosted browser (Windows 365 for Agents, preview, not for production, may be throttled), Cloud PC pool (preview, Entra-joined/Intune), or bring-your-own Windows machine via Power Automate for desktop ≥2.61.132.25266 with runs queued per machine. | <https://learn.microsoft.com/en-us/microsoft-copilot-studio/computer-use> | ms.date 2026-07-03, updated 2026-08-03 | high |
| Microsoft Copilot Studio computer use — access control, credentials, human supervision | Optional 'Access control' restricts actions to an allowlist of websites (wildcards) and desktop apps by process name — it 'only prevents the model from taking actions on websites or applications that aren't in the allow list. It doesn't stop the model from opening them'. 'Enforce HTTPS' toggle. Stored credentials (internal encrypted store or Azure Key Vault) are auto-filled at sign-in prompts so the model does not receive passwords. Human supervision routes clarification/confirmation requests to a reviewer by Outlook email or an inline activity-panel card, with a response time limit after which the run stops; docs warn the trigger is probabilistic — 'Don't rely on human review or clarification requests as a fail-safe' — and removing reviewers makes the session fail when the agent pauses rather than making it autonomous. Sharing an agent with maker credentials lets anyone act with the author's access. | <https://learn.microsoft.com/en-us/microsoft-copilot-studio/human-supervision-computer-use> | ms.date 2026-06-29 | high |
| Microsoft Windows — agent workspace, agent accounts, agent connectors | Windows 'experimental agentic features': an agent workspace is 'a separate, contained space in Windows' running 'in a separate Windows session' so agents work in parallel to the user; each agent runs under its own agent account distinct from the user; agent connectors are MCP servers bridging agents to Windows apps/system tools; default file scope is six known folders (Documents, Downloads, Desktop, Music, Pictures, Videos) with 'Allow Always' / 'Ask every time' / 'Never allow'; least privilege, never exceeding the initiating user; only a device administrator can enable it and it applies to all users. Private preview for Windows Insiders (support page dated 2025-12-05). Build 2026 (early June) coverage says Windows is being positioned as an agent runtime with an MCP registry and local models, but the primary blog could not be fetched. | <https://support.microsoft.com/en-us/windows/ai/ai-features/experimental-agentic-features> | 2025-12-05 (page); Build 2026 secondary 2026-06-02 | high |
| Microsoft Research UFO² / UFO³ — hybrid UIA+vision desktop AgentOS | UFO² (April 2025, LTS) is a Windows 'Desktop AgentOS' with deep OS integration (UIA, Win32, WinCOM), a 'hybrid control detection pipeline [that] fuses Windows UI Automation (UIA) with vision-based parsing', 'hybrid actions' mixing GUI clicks with API calls, a Picture-in-Picture isolated virtual desktop so 'agents and users... operate concurrently without interference', and 'speculative multi-action' planning that cuts LLM calls by 51%; evaluated on 20+ real Windows apps. UFO³ Galaxy (Nov 2025) adds DAG task decomposition, an Agent Interaction Protocol for cross-device orchestration (Windows, Linux, Android) and MCP integration. MIT license. | <https://github.com/microsoft/UFO> | README read 2026-09-05; paper arXiv 2504.14603 (2025-04-20) | high |
| Microsoft Windows Recall — 2026 status and dispute | Recall is opt-in ('off by default'), requires a Windows Hello biometric, stores snapshots on-device, encrypts with TPM-protected keys inside a VBS Enclave with just-in-time decryption, filters passwords/IDs/card numbers by default, excludes private browsing and filtered apps, and can be paused/deleted/uninstalled. On 2026-04-18 Microsoft rejected researcher 'xaitax'/Alexander Hagenah's claim (TotalRecall Reloaded, 2026-04-09) that decrypted content reaches an unprotected AIXHost.exe process after sign-in, saying it 'operates within the current, documented security design'. A Windows Central report that Microsoft is scaling back Copilot integrations and 'rethinking Recall' could not be loaded and is unverified. | <https://support.microsoft.com/en-us/windows/privacy/privacy-and-control-over-your-recall-experience> | page undated; dispute 2026-04-18 | high |
| Apple — Siri AI and App Intents (iOS/macOS 27) | Apple announced 'Siri AI' on 2026-06-08 for iOS/iPadOS/macOS/watchOS/visionOS 27 with onscreen awareness, in-app actions via App Intents (e.g. draft an email, edit and share photos) and personal context, running on-device Apple Foundation Models plus Private Cloud Compute; developer testing immediately, user beta 'later this year'. No third-party model partner is named in the release. WWDC26 session 343 shows Siri 'auto-confirms intents with meaningful side effects', a new OwnershipProvidingEntity protocol (public/shared entities), view-annotation APIs (.appEntityIdentifier) for on-screen references, and interaction donations. Apple's model is API/intent-first (no pixel-level GUI control exposed). | <https://www.apple.com/newsroom/2026/06/apple-introduces-siri-ai-a-profoundly-more-capable-and-personal-assistant/> | 2026-06-08 | high |
| Manus — My Computer desktop app (local CLI agent) | On 2026-03-16 Manus launched the Desktop app with 'My Computer' for macOS and Windows: the agent executes terminal commands locally to read/edit files and launch/control apps (CLI-based, not GUI pixel control). 'Every terminal command requires your explicit approval before execution' with 'Always Allow' or 'Allow Once'; users authorize specific local folders; cloud intelligence combined with local compute (can use the local GPU). | <https://manus.im/blog/manus-my-computer-desktop> | 2026-03-16 | high |
| Manus — Meta acquisition blocked by China | Meta announced the ~$2B acquisition in December 2025; on 2026-04-27 China's NDRC 'made a decision to prohibit foreign investment in the Manus project... and has required the parties involved to withdraw the acquisition transaction' under foreign-investment security review; ~100 Manus employees had already moved to Meta's Singapore offices. Secondary sources report Meta completed an operational split by 2026-06-11 and current plans of $20/$40/$200 per month with credits. | <https://techcrunch.com/2026/04/27/china-vetoes-metas-2b-manus-deal-after-months-long-probe/> | 2026-04-27 | medium |
| Perplexity Computer and Personal Computer for Mac | Perplexity Computer launched 2026-02-25 as a cloud agent orchestrating ~19 models (Claude Opus 4.6 as orchestrator) for Max ($200/mo) with credit/per-token billing (secondary; perplexity.ai returned 403). 'Personal Computer' for Mac (2026-04-16) runs on any Mac with macOS 14+, gets file-system and native-app access, activates with both Command keys, recommends an always-on Mac mini, can be initiated/approved from iPhone, and is described as sandboxed, 'auditable and reversible', with 'a kill switch'; Max-only at launch, opened to Pro/Enterprise 2026-05-07. Perplexity positions it as safer than OpenClaw's 'elevated permissions' while still using 'a secure development environment on Perplexity's servers'. | <https://www.macrumors.com/2026/04/16/perplexity-personal-computer-for-mac/> | 2026-04-16 / 2026-05-07 | medium |
| OpenClaw — what it is (2026) | OpenClaw ('The AI that really does things. Any OS. Any Platform.'), MIT © OpenClaw Foundation, ~389k GitHub stars: a local Gateway control plane for sessions/tools/events, messaging channels (WhatsApp, Telegram, Slack, Discord, Google Chat, Signal, iMessage…), skills/plugins, and companion apps that 'add voice, Canvas, camera, screen, and device-local actions'; macOS/Linux/Windows/WSL2; works with hosted and local model providers. README: 'Treat inbound messages as untrusted input'; DM channels pair unknown senders by default. | <https://github.com/openclaw/openclaw> | read 2026-09-05 | high |
| OpenClaw — security defaults and exec approvals | Security docs assume 'one trusted boundary per gateway'; gateway binds loopback by default; pairing codes expire after 1 hour; Ed25519 device identities. Sandboxing and exec approvals are opt-in — trusted-operator default is `security=full`, `ask=off`. Exec-approval policy levels: deny, allowlist, ask, auto (native auto-reviewer before human), full; 'Commands run only when policy + allowlist + (optional) user approval all agree'; allowlists are per agent; approvals surface in the macOS companion app ('Allow Once' / 'Always Allow Here') or Control UI — 'chat channels never receive automation approvals'; if no UI can be reached the 'ask fallback (default: deny)' applies. `security.installPolicy` can run a local command returning allow/warn/block for skill installs; plugins run in-process as trusted code. Docs cite model-dependent prompt-injection ASR: 0.5% Claude Opus 4.5, 1.0% Sonnet 4.5 … 8.5% Gemini 2.5 Pro. | <https://docs.openclaw.ai/tools/exec-approvals> | read 2026-09-05 (undated docs; egress allowlist noted Aug 2026) | high |
| Incident — OpenClaw infostealer wave and ClickFix lures | Intel 471 (2026-03-12): OpenClaw reached ~265k stars by 2026-03-05 and became a malware magnet. Campaign 1 (found 2026-03-02): fake site app-clawbot.org used ClickFix (copy/paste a terminal command) to deliver Stealc_v2. Campaign 2 (2026-03-03): 'Clearl_AI' lures delivered AMOS on macOS and a novel Electron-based JS stealer on Windows. Trend Micro counted 2,200+ malicious OpenClaw skills on GitHub as of 2026-02-23. Earlier (2026-02-13, reported by The Hacker News, secondary) a Vidar variant exfiltrated ~/.openclaw/openclaw.json including the gateway auth token. Mitigations: isolated VMs, low-privilege short-lived identities, explicit skill-install review, block look-alike domains, alert on browser-to-shell chains. | <https://www.intel471.com/blog/openclaw-a-viral-ai-assistant-and-a-magnet-for-infostealer-malware-and-clickfix-trickery> | 2026-03-12 | high |
| Incident — ClawHub supply chain (Unit 42) | Unit 42 (2026-06-23) found five malicious skills that evaded ClawHub screening Feb–May 2026: two macOS infostealers with C2, one scanner-evasion skill ('omnicogg') padding its README with 22 MB of filler to exceed content-analysis size limits, and two 'agentic' abuse skills (runtime affiliate-link injection, front-running). Secondary reports of a larger Unit 42 scan claim 49,943 skills scanned, 80% with declared-vs-actual behavior mismatch and 5% (2,490) with multi-stage attack chains; Bitdefender reportedly found ~17% malicious in ClawHub's first weeks. OpenClaw announced a VirusTotal scanning partnership (secondary). | <https://unit42.paloaltonetworks.com/openclaw-ai-supply-chain-risk/> | 2026-06-23 | high |
| browser-use — architecture, perception controls, local models | browser-use (MIT, 112.4k stars) supports OpenAI/Anthropic/Google and local models via Ollama plus its own ChatBrowserUse model. Agent parameter `use_vision` = 'auto' (screenshot tool available, used only when requested) / True / False; `sensitive_data` dict; `max_actions_per_step` default 4; browser parameters `allowed_domains` (patterns like 'example.com', '*.example.com'), `prohibited_domains` (allowed takes precedence), `cross_origin_iframes` default False, `user_data_dir`/`storage_state` for profiles. A May 2026 community write-up says it 'migrated to direct CDP with their own event-driven watchdog architecture in early 2026' (dropping Playwright), reports Online-Mind2Web 97% / WebVoyager 89.1%, and notes 'most frontier agents only manage 30%' on realistic tasks (ClawBench best 33.3%). | <https://docs.browser-use.com/customize/browser/all-parameters> | docs read 2026-09-05; blog 2026-05-04 | high |
| Skyvern — vision-first agent with record-then-replay code caching | Skyvern (AGPL-3.0; PyPI 1.0.48 released 2026-08-05; Python 3.11–3.13) uses vision LLMs plus a planner/agent/validator swarm; reports 85.8% WebVoyager and 64.4% WebBench; supports Ollama/OpenRouter/OpenAI-compatible endpoints and self-hosting with SQLite by default. Code caching: run once with run_with='agent' to record successful actions as generated code; later run_with='code' replays with 'No screenshots, no LLM reasoning'; if the cached code hits a layout change or missing element it 'falls back to the agent automatically and regenerates the cache'; caching is per block for workflows, while conditional/wait/code blocks always run live. | <https://www.skyvern.com/docs/developers/features/code-caching> | read 2026-09-05; PyPI 2026-08-05 | high |
| Windows MCP servers — accessibility-tree-first desktop control | deploymenttheory/windows-mcp-server (Go, MIT, MCP rev 2026-07-28): 35 tools in 13 toolsets; 'No computer-vision model is required: the agent perceives the UI through the Windows accessibility tree' (Snapshot = foreground window + labeled interactive elements); guidance 'Prefer Invoke over Click/Type' (UIA patterns instead of synthetic input); device policy engine evaluating every call against live device signals; loopback egress proxy that 'admits only the domains you declare'; 'hash-chained audit log (optionally keyed and anchored off-box)'; kill switch with tiered containment. CursorTouch/Windows-MCP (MIT, PyPI): UIAutomation-based, 'Vision Optional', DOM mode for browsers, 'typical latency between actions... 0.2 to 0.5 secs'. QwenLM/open-computer-use (MIT): MCP service controlling macOS/Linux/Windows via accessibility APIs with 9 tools and 'post-action screenshots attached to every action tool' plus a `doctor` permissions check. | <https://github.com/deploymenttheory/windows-mcp-server> | read 2026-09-05 | high |
| Anthropic MCP computer-use server | No official Anthropic MCP computer-use server was found; Anthropic's reference remains the `computer-use-demo` quickstart (Docker Linux desktop with X11+VNC, Streamlit loop, defaults to claude-opus-4-8) and the API toolsets. Community MCP servers (Windows-MCP, MacOS-MCP, zavora-ai/computer-use-mcp in Rust, Qwen open-computer-use) fill the gap. | <https://github.com/anthropics/claude-quickstarts/tree/main/computer-use-demo> | read 2026-09-05 | medium |
| OSWorld benchmark state (Sept 2026) | Aggregator leaderboard: Qwen3.8-Max 86.1% on OSWorld-Verified (Aug 2026), Claude Mythos Preview 85.4%, Claude Mythos 5 and Fable 5 85.0% (June 2026), versus the 72.36% human baseline; most rows use pass@1 over 361 tasks with 100 max steps, but scores are not directly comparable across harnesses ('self-reported rows can use different max steps, OS images, and tool permissions'). Frontier models now exceed the human baseline on this benchmark while real-world task suites (ClawBench) remain far lower. | <https://leaderboard.steel.dev/leaderboards/osworld/> | read 2026-09-05 | medium |
| Cross-cutting pattern — consequential-action confirmation is universal | Every 2026 operator converges on the same gate: purchases, payments, sending messages/email, account changes, terms acceptance, posting on social media, and file deletion require explicit user confirmation (Anthropic docs, Cowork delete rule, ChatGPT agent, Chrome auto browse, Gemini Spark, Copilot Studio human supervision, Manus per-command approval). Banking/finance and crypto are commonly blocked or watch-only by default (Cowork app blocklist, Claude in Chrome financial-site permission, ChatGPT refuses bank transfers, Cowork browser excludes banking/SSO login import). | <https://support.claude.com/en/articles/14128542-let-claude-use-your-computer-in-cowork> | read 2026-09-05 | high |
| Cross-cutting pattern — session isolation for the operator | Vendors isolate the agent from the human's live session rather than sharing it: Cowork's built-in browser uses an isolated profile; Windows agent workspace runs agents in a separate Windows session under an agent account; UFO² uses a Picture-in-Picture virtual desktop; Anthropic/Microsoft docs tell you to dedicate a VM/machine; ChatGPT's Windows Computer Use is foreground-only precisely because it lacks such isolation; macOS variants (Cowork, ChatGPT 'locked use') work in background windows. | <https://support.microsoft.com/en-us/windows/ai/ai-features/experimental-agentic-features> | read 2026-09-05 | high |

### ANGLE 3 — Open-source building blocks for local-first, governed computer control on Windows/macOS/Linux that Nerva (Python 3.12, Apache-2.0, FastAPI) can adopt 

| Topic | Claim | Source | Date | Confidence |
|---|---|---|---|---|
| input-layer/pyautogui | pyautogui latest release is 0.9.54 (2023-05-24), BSD-3-Clause; no release in over three years. Per README it needs pyobjc on macOS and python3-xlib on Linux (X11 only), uses pyscreeze/pymsgbox/pillow, and 'only works on the primary monitor'. It is the default action executor of the OSWorld harness and of Agent S3, so it remains the de-facto benchmark actuation layer despite being unmaintained. | <https://github.com/asweigart/pyautogui> | 2023-05-24 (PyPI 0.9.54); README fetched 2026-09-05 | high |
| input-layer/pynput | pynput 1.8.2 released 2026-05-12, LGPLv3; deps: pyobjc-framework-ApplicationServices + Quartz on macOS, evdev + python-xlib on Linux. Official limitations: Linux needs an X server and $DISPLAY (Wayland only via Xwayland with limited reach); the uinput backend requires root; macOS keyboard monitoring requires root or the process to be trusted under Accessibility (exposes an IS_TRUSTED attribute); on Windows synthetic key presses do not propagate as physically held keys. | <https://pynput.readthedocs.io/en/latest/limitations.html> | 2026-05-12 (PyPI 1.8.2); limitations page is versioned 1.7.6 | high |
| screenshot-layer/mss | mss 10.2.0 (2026-04-23), MIT, pure-ctypes, Python>=3.9; backends: Windows GDI BitBlt, macOS CoreGraphics CGWindowListCreateImage/ScreenCaptureKit (needs Screen Recording permission), Linux Xlib XGetImage/XShm. Wayland is not supported: XGetImage()/XDefaultRootWindow fail on GNOME Wayland (issues #155, #324). | <https://github.com/BoboTiG/python-mss/issues/155> | 2026-04-23 (PyPI 10.2.0) | high |
| screenshot-layer/Pillow ImageGrab | Pillow 12.3.0 (2026-07-01, Python>=3.10). ImageGrab.grab(): Windows native GDI with include_layered_windows/all_screens; macOS captures at 2x on Retina by default with scale_down=True added in 12.3.0 and per-window capture via CGWindowID (12.1.0); Linux uses X11 via xdisplay and, when the default X display fails, falls back to gnome-screenshot, grim or spectacle if installed (xdisplay="" disables the fallback; PIL.features.check_feature('xcb') reports X11 support). Nerva's desktop_host.py already uses PIL.ImageGrab, so on Wayland it silently shells out to those tools. | <https://pillow.readthedocs.io/en/stable/reference/ImageGrab.html> | 2026-07-01 (PyPI 12.3.0) | high |
| windows/pywinauto | pywinauto 0.6.9 released 2025-01-06 ('the last Python 2.7 compatible release with pinned dependencies'), BSD-3-Clause since 0.6.0, deps comtypes + pywin32 (+six; python-xlib on Linux), backends win32 (default) and uia. No newer release; repo shows ~505 open issues. This is the backend Nerva's desktop_host.py already lazy-imports (Desktop(backend='uia')). | <https://github.com/pywinauto/pywinauto/releases> | 2025-01-06 | high |
| windows/uiautomation (yinkaisheng) | uiautomation 2.0.29 (2025-08-05), Apache-2.0, single dependency comtypes>=1.2.1, binds Microsoft UIA COM directly (GetRootControl, control-tree walk, Click, SendKeys, CaptureToImage, pattern access). README caveats: admin privileges needed for some targets, Electron apps need --force-renderer-accessibility, DirectUI/custom controls without a UIA provider are invisible. Windows-MCP is built on it. | <https://github.com/yinkaisheng/Python-UIAutomation-for-Windows> | 2025-08-05 (PyPI 2.0.29) | high |
| windows/Windows-MCP | Windows-MCP 0.8.5 (2026-08-01), MIT, requires_python>=3.12 (README says 3.13+), deps include comtypes, dxcam, pillow, pywin32, fastmcp>=3.0 and posthog (telemetry). Exposes 20+ tools including Screenshot, Snapshot (UIA tree), Click, Type, Scroll, Shortcut, App, PowerShell, FileSystem, Clipboard, Process, Registry; 'works with any LLM'. | <https://github.com/CursorTouch/Windows-MCP> | 2026-08-01 (PyPI 0.8.5) | high |
| windows/UIA security model (UIPI, UIAccess) | Microsoft Learn (updated 2026-02-18): a medium-integrity process cannot access UI of elevated (high-IL) processes; to cross the boundary an app must set uiAccess=true in its manifest, be Authenticode-signed and installed in a secure location (e.g. Program Files), and still cannot touch system-IL UI (UAC secure desktop, logon). Windows shows no consent prompt for accessibility clients — the limit is integrity level, not TCC-style consent. | <https://learn.microsoft.com/en-us/windows/win32/winauto/uiauto-securityoverview> | 2026-02-18 | high |
| windows/Microsoft UFO | Microsoft UFO / UFO² (Desktop AgentOS, 2025) / UFO³ Galaxy (Nov 2025), MIT; deep Windows integration via UIA + Win32 + WinCOM with 'hybrid GUI + API actions'. No 2026 news entries on the README; local-model support not documented. | <https://github.com/microsoft/UFO> | 2025-11 | high |
| macos/pyobjc | pyobjc-framework-ApplicationServices and -Quartz 12.2.2 released 2026-08-11, MIT, Python>=3.10. pyobjc notes say HIServices (the AX API: AXUIElement*, AXObserver) is 'fully supported' except AXValueGetValue, which 'requires a manual wrapper'. | <https://pyobjc.readthedocs.io/en/latest/apinotes/ApplicationServices.html> | 2026-08-11 (PyPI 12.2.2) | high |
| macos/TCC accessibility check API | AXIsProcessTrustedWithOptions (macOS 10.9+) 'returns whether the current process is a trusted accessibility client'; passing kAXTrustedCheckOptionPrompt=true informs the user asynchronously without affecting the return value — i.e. a driver can preflight silently (prompt=false) and refuse honestly. | <https://developer.apple.com/documentation/applicationservices/1459186-axisprocesstrustedwithoptions> | Apple docs JSON fetched 2026-09-05 | high |
| macos/TCC screen recording check API | CGPreflightScreenCaptureAccess() and CGRequestScreenCaptureAccess() are available since macOS 10.15 (Apple docs). Secondary sources: Preflight only checks and never adds the app to the Screen Recording list; Request triggers the prompt and registers the app. macOS Sequoia added recurring re-authorization prompts for screen capture (weekly in early betas, monthly from beta 6; 15.1 reduced them for frequently used apps); the 'Persistent Content Capture' entitlement is reserved for VNC-type apps by request. | <https://mjtsai.com/blog/2024/08/08/sequoia-screen-recording-prompts-and-the-persistent-content-capture-entitlement/> | 2024-08-08 (updated through 2024-10) | medium |
| macos/Automation (Apple Events) TCC | osascript/JXA calls that control another app trigger a one-time per source/target-app Automation consent prompt; if denied, AppleScript returns error -1743 (prompt is attributed to the host process, e.g. Terminal or the Nerva binary). | <https://scriptingosx.com/2020/09/avoiding-applescript-security-and-privacy-requests/> | 2020-09 | medium |
| macos/atomacos | atomacos 3.3.0 (2021-05-24) is GPLv2 (PyPI classifier), pyobjc-based, and pins stale deps (pyautogui<0.9.42, pyscreeze<0.1.20, pygetwindow<0.0.5); raises AXErrorAPIDisabled when Accessibility is not granted. GPLv2 import is incompatible with Nerva's Apache-2.0 codebase. | <https://daveenguyen.github.io/atomacos/readme.html> | 2021-05-24 | high |
| macos/macapptree | macapptree 0.0.2 (2025-03-12), MIT, returns the app accessibility tree as JSON plus a cropped PIL screenshot and a segmented image; but requires_dist pins atomacos==3.3.0 (GPLv2) and pyobjc==10.3.1 exactly. MacArena uses it as the optional a11y observation source. | <https://github.com/MacPaw/macapptree> | 2025-03-12 | high |
| macos/pyax | pyax 0.3.4 (2026-02-28), MIT, requires pyobjc-framework-applicationservices>=10.1 (+typer); 'Pythonifies AXUIElement and AXObserver' (get_application_by_name etc.). Early-stage (about 15 stars) but the only actively released MIT AX wrapper found. | <https://github.com/eeejay/pyax> | 2026-02-28 | high |
| macos/Hammerspoon | Hammerspoon latest release 1.1.1 (2025-02-26), minimum macOS 13; Lua bridge with hs.axuielement, hs.eventtap, hs.mouse; no 2026 release listed. Usable only as an external CLI/IPC dependency, not a Python import. | <https://github.com/Hammerspoon/hammerspoon/releases> | 2025-02-26 | high |
| macos/cliclick | cliclick 5.1, BSD-3-Clause, macOS 10.15+, CLI for click/type/key/move; requires Accessibility permission for the calling terminal/app and since 5.1 writes a warning to stderr when permission is missing — a precedent for honest refusal. | <https://github.com/BlueM/cliclick> | README fetched 2026-09-05 | high |
| macos/Shortcuts CLI | The `shortcuts` CLI supports run/list/view/sign, input via -i/--input-path, output via -o/--output-path and --output-type (UTI), exit code 0/1 — a native, user-visible API/CLI route for macOS automation before pixels. | <https://support.apple.com/guide/shortcuts-mac/run-shortcuts-from-the-command-line-apd455c82f02/mac> | Apple support page fetched 2026-09-05 | high |
| linux/xdotool | xdotool (BSD-3-Clause) is built on X11 XTEST/Xlib; README: 'If you are using Wayland, please be aware this software will not work correctly' (typing, window search fail) and points to ydotool/dotool. | <https://github.com/jordansissel/xdotool> | README fetched 2026-09-05 | high |
| linux/ydotool | ydotool (AGPLv3, C99) emulates input via /dev/uinput so it works on X11, Wayland and consoles, but requires the ydotoold daemon and root or a udev rule on /dev/uinput; commands type/key/click/mousemove/stdin; no window management, and it bypasses compositor consent entirely. | <https://github.com/ReimuNotMoe/ydotool> | README fetched 2026-09-05 | high |
| linux/python-libei | python-libei 0.4.0 (2026-09-03; first release 0.1.0 on 2026-08-28), MIT (Dennis K. Paulsen 2026), Python>=3.10, pure ctypes over system libei/libeis/liboeffis 1.6, optional extra [portal] needs PyGObject>=3.42. 0.3.0 (2026-08-31) added direct D-Bus negotiation of org.freedesktop.portal.RemoteDesktop with persist_mode/restore_token and a RemoteDesktopSession context manager; 0.4.0 moved status to Beta. README: 'Not a way around user consent — a real session goes through the portal's consent dialog'; keyboard is evdev keycodes, text_utf8 needs libei 1.6 TEXT device; input only (pair with ScreenCast portal + PipeWire for pixels); API not frozen before 1.0. | <https://pypi.org/project/python-libei/> | 2026-09-03 | high |
| linux/XDG RemoteDesktop portal | org.freedesktop.portal.RemoteDesktop: CreateSession → SelectDevices (bitmask KEYBOARD=1, POINTER=2, TOUCHSCREEN=4; restore_token/persist_mode since v2) → Start ('typically results in the portal presenting a dialog letting the user select what to share') → NotifyPointerMotion/NotifyKeyboardKeycode or ConnectToEIS (v2, libei fd). persist_mode: 0 none (default), 1 while app runs, 2 until explicitly revoked; restore_token is single-use. | <https://flatpak.github.io/xdg-desktop-portal/docs/doc-org.freedesktop.portal.RemoteDesktop.html> | docs fetched 2026-09-05 | high |
| linux/libei + portals (Peter Hutterer, 2026-07-22) | libei is the transport for the RemoteDesktop and InputCapture portals since xdg-desktop-portal 1.17 (mid-2023) and works with 'any compositor that implements the portal'; session persistence (no repeated consent) landed in portals 1.21.0 and is rolling out to major compositors; Xwayland ≥23.2 routes XTEST through libei so legacy X tools keep working under Xwayland. | <http://who-t.blogspot.com/2026/07/libei-integrations-in-xdg-remotedesktop.html> | 2026-07-22 | high |
| linux/wdotool (Wayland xdotool successor) | wdotool (Rust, MIT OR Apache-2.0, 'early but usable') uses libei via the RemoteDesktop portal on GNOME/KDE, wlr virtual-keyboard/pointer + foreign-toplevel on Sway/Hyprland/river/Wayfire, KWin D-Bus scripting and a GNOME Shell extension for window ops, with uinput as last-resort fallback; README admits GNOME/KDE backends are 'unverified on actual sessions', Unicode typing is best-effort outside wlr, and modifier state cannot be observed on Wayland. | <https://github.com/cushycush/wdotool> | README fetched 2026-09-05 | high |
| linux/kwin-mcp | kwin-mcp 0.7.0 (2026-03-29), MIT, Python>=3.12 (mcp, pygobject, dbus-python, pillow); 30 MCP tools for KDE Plasma 6 Wayland. Injects input through KWin's private EIS D-Bus interface explicitly to 'bypass the XDG RemoteDesktop portal' and its authorization dialogs; screenshots via org.kde.KWin.ScreenShot2 (~30–70 ms). | <https://github.com/isac322/kwin-mcp> | 2026-03-29 (PyPI 0.7.0) | high |
| linux/AT-SPI2 python stack | pyatspi2 is not on PyPI (404); it is a distro package (python3-pyatspi) and a hand-written wrapper preserving Orca's old API over the GObject-Introspection API gi.repository.Atspi of libatspi; reading the tree works over D-Bus regardless of display server. Input synthesis does NOT: Atspi.generate_keyboard_event() 'does not work on Wayland' (GNOME Shell issue #2958, open) and at-spi2-core issue #14 documents wrong-coordinate generated clicks on Wayland. | <https://gitlab.gnome.org/GNOME/gnome-shell/-/issues/2958> | GNOME devel docs + issue fetched 2026-09-05 | high |
| linux/dogtail | dogtail 2.1.0 (2026-08-21), GPLv2+, Python>=3.9, AT-SPI2-based GUI test framework; requires `gsettings set org.gnome.desktop.interface toolkit-accessibility true` (apps restarted); on GNOME Wayland input goes through gnome-ponytail-daemon (Screen Cast, Remote Desktop, Shell Introspection APIs) and headless via Mutter's RemoteDesktop D-Bus API — GNOME-specific and GPL, so subprocess-only for Nerva. | <https://gitlab.com/dogtail/dogtail/-/raw/master/README.md> | 2026-08-21 (PyPI 2.1.0) | high |
| browser/Playwright Python | playwright 1.62.0 on PyPI 2026-07-31 (Python>=3.10; deps pyee, greenlet) bundling Chromium 151 / Firefox 153 / WebKit 26.5. Agent-relevant: 1.59 Screencast API + page.pick_locator(); 1.60 aria snapshots with `boxes` option (element coordinates) + HAR-in-trace + Drop API; 1.61 Web Storage API + virtual passkeys; 1.62 locator.wait_for_function(), WebP screenshots, clipboard isolation in headless. | <https://playwright.dev/python/docs/release-notes> | 2026-07-31 | high |
| browser/Playwright MCP | microsoft/playwright-mcp (Apache-2.0, Node server) v0.0.80 on 2026-09-01 (recording tools), v0.0.79 on 2026-08-06 (WebP, Python/Java/C# codegen, settle delays), v0.0.78 on 2026-07-09 (browser_find, less-verbose snapshots, mobile emulation). Default mode is accessibility snapshots ('deterministic', no vision model); `--caps vision` adds browser_mouse_click_xy/drag_xy; flags include --allowed-origins/--blocked-origins/--allowed-hosts, --isolated, --storage-state, --headless, --extension (attach to the user's running Chrome/Edge), --allow-unrestricted-file-access, --snapshot-boxes. | <https://github.com/microsoft/playwright-mcp> | 2026-09-01 | high |
| models/UI-TARS-1.5-7B | ByteDance-Seed/UI-TARS-1.5-7B: Apache-2.0, Qwen2.5-VL-based (~8B tensors), reports OSWorld 42.5 (100 steps), WindowsAgentArena 42.1 (50 steps), ScreenSpot-Pro 61.6, Online-Mind2Web 75.8, AndroidWorld 64.2; card lists 13 community quantizations for llama.cpp/Ollama/LM Studio (GGUF + mmproj, e.g. Mungert/UI-TARS-1.5-7B-GGUF). Repo note: Qwen2.5-VL-based models output absolute pixel coordinates on the model-resized image. | <https://huggingface.co/ByteDance-Seed/UI-TARS-1.5-7B> | 2025-04-18 (last modified) | high |
| models/UI-TARS-2 (no open weights) | UI-TARS-2 technical report (arXiv 2509.02544, Sept 2025) reports Online-Mind2Web 88.2, OSWorld 47.5, WindowsAgentArena 50.6, AndroidWorld 73.3, but the Hugging Face API listing for ByteDance-Seed on 2026-09-05 contains only UI-TARS-1.5-7B and the 2B/7B/72B SFT/DPO v1 checkpoints — no UI-TARS-2 weights; GitHub issue #213 requesting them is still open. Blog posts claiming 'UI-TARS-2 weights available' are wrong. | <https://github.com/bytedance/UI-TARS/issues/213> | 2025-09-04 (issue); HF API checked 2026-09-05 | high |
| models/Qwen3-VL | Qwen3-VL family is Apache-2.0: dense 2B/4B/8B/32B and MoE 30B-A3B/235B-A22B, Instruct and Thinking, with official Qwen GGUFs (2B/4B/8B/32B, Nov 2025). Ollama library tags: 2b 1.9 GB, 4b 3.3 GB, 8b 6.1 GB, 30b 20 GB, 32b 21 GB, 235b 143 GB, requires Ollama ≥0.12.7; LM Studio lists 2b/4b/8b/30b/32b in GGUF and MLX (3–20 GB). Model card advertises GUI-agent use ('operates PC/mobile GUIs — recognizes elements, understands functions, invokes tools'). | <https://ollama.com/library/qwen3-vl> | 2025-11 (HF); Ollama page fetched 2026-09-05 | high |
| models/Qwen3.8-27B | Qwen/Qwen3.8-27B: Apache-2.0, 27B dense native VLM (text/image/video), released 2026-08-14, 262k context; model card self-reports OSWorld-Verified 84.3, AndroidWorld 81.9, WebArena-Verified 64.8. Available locally: Ollama qwen3.8:27b (18 GB, plus 27b-mlx tag), LM Studio qwen/qwen3.8-27b 16.1 GB GGUF/MLX (min 16 GB RAM). | <https://huggingface.co/Qwen/Qwen3.8-27B> | 2026-08-14 | high |
| models/Holo3 and Holo3.1 (H Company) | Holo3-35B-A3B released 2026-03-31 under Apache-2.0 (Qwen3.5-family MoE, 3B active); H's page reports 78.85% OSWorld-Verified for the API-only Holo3-122B-A10B. Holo3.1 (2026-06-01) ships 0.8B/4B/9B/35B-A3B on HF under Apache-2.0 (HF API tags), with FP8/NVFP4/Q4 GGUF checkpoints; Hcompany/Holo-3.1-35B-A3B-GGUF (Apache-2.0) offers Q4_K_M 21.3 GB and BF16 69.4 GB and had ~176k downloads/month; H reports FP8/NVFP4 within ~2 OSWorld points of BF16, AndroidWorld 79.3 (35B) and step time cut 6.8s→3.3s. | <https://hcompany.ai/holo3.1> | 2026-06-01 | high |
| models/Holo2 | Holo2 (2025-11-13): 4B and 8B under Apache-2.0, 30B-A3B (and 235B-A22B, Holo1.5-72B) under CC-BY-NC-4.0 per HF tags; built on Qwen3-VL (Holo2-8B fine-tuned from Qwen3-VL-8B-Thinking). Holo2-8B: ScreenSpot-Pro 71.4 (agentic), OSWorld-G 70.1, WebArena 42.2, AndroidWorld 60.4; Holo2-30B-A3B: ScreenSpot-Pro 66.1, OSWorld-G 76.1. | <https://huggingface.co/Hcompany/Holo2-8B> | 2025-11-13 | high |
| models/OmniParser | microsoft/OmniParser: repo CC-BY-4.0; V2 (Feb 2025) reports ScreenSpot-Pro 39.5 with GPT-4o and 60% lower latency than V1; news entry '[2026/7] We add a YOLOv9-E interactive region detector' (icon_detect_v3) which is MIT, while earlier YOLOv8 detectors 'retain their original AGPL license'; caption model (Florence) is MIT; HF microsoft/OmniParser-v2.0 tagged MIT. Requires torch — heavy for a lazy extra. | <https://github.com/microsoft/OmniParser> | 2026-07 | high |
| models/ShowUI, OS-Atlas, GUI-Actor | ShowUI-2B (Nov 2024, Qwen2-VL base): GitHub says Apache-2.0, HF card tagged MIT; scored 1.2% on macOSWorld. OS-Atlas (ICLR 2025): Base-4B (InternVL2) and Base-7B (Qwen2-VL), Apache-2.0, emits 0–1000 normalized coordinates; ~84.1 ScreenSpot-v2. GUI-Actor (June 2025, MIT): 2B/3B/7B on Qwen2-VL / Qwen2.5-VL, coordinate-free attention head + Verifier-2B; ScreenSpot-Pro 7B 40.7 (44.2 with verifier), ScreenSpot-v2 7B 92.1. | <https://github.com/microsoft/GUI-Actor> | 2025-06 | high |
| models/OpenCUA | xlangai/OpenCUA-7B/32B/72B (MIT; Qwen-based with Kimi-VL tokenizer, needs trust_remote_code; vLLM ≥0.12 since Jan 2026) report OSWorld-Verified 26.6 / 34.8 / 45.0 at 100 steps (24.3 / 29.7 / 39.0 at 15 steps). | <https://github.com/xlang-ai/OpenCUA> | 2026-01 | high |
| agents/Agent S3 | simular-ai/Agent-S (Apache-2.0; pip `gui-agents` 0.3.2 on 2025-12-16, requires_python <=3.12) supports Windows/macOS/Linux using pyautogui plus pywinauto/pywin32 on Windows and pyobjc on macOS; grounding via UI-TARS-1.5-7B on HF Inference Endpoints (1920x1080) or UI-TARS-72B; OSWorld-Verified 66% alone and 72.6% with Behavior Best-of-N at 100 steps (Dec 15, 2025; TMLR 2026). | <https://github.com/simular-ai/Agent-S> | 2025-12-15 | high |
| agents/trycua cua | trycua/cua (MIT; Python>=3.11; cua-driver 0.23.2 on 2026-08-31) provides sandboxes for macOS (Lume on Apple Virtualization, Apple Silicon), Windows Sandbox, Linux containers/QEMU, with an async SDK (sb.mouse.click, sb.keyboard.type, sb.screenshot). | <https://github.com/trycua/cua> | 2026-08-31 | high |
| benchmarks/OSWorld-Verified definition | OSWorld-Verified announced 2025-07-28 (xlang-ai/OSWorld, Apache-2.0): 369 tasks (361 excluding Google Drive), step budgets 15/50/100, AWS parallel evaluation under 1 hour, human baseline 72.36%; actions executed in the VM via pyautogui by default. News 2026-06-26: OSWorld 2.0 released. | <https://github.com/xlang-ai/OSWorld> | 2025-07-28 | high |
| benchmarks/OSWorld-Verified SOTA (Sept 2026) | Leaderboard mirrors updated 2026-09-04: Qwen3.8 Max 86.1%, Claude Fable 5 85.0%, Claude Mythos 5 85.0% (steel lists Claude Mythos Preview 85.4%), Qwen3.8-27B 84.3% (open weights), Claude Opus 4.8 83.4%, Gemini 3.6 Flash 83.0%, Holo3-35B-A3B 82.6% (open weights), Claude Sonnet 5 81.2%, Muse Spark 1.1 80.8%, Holo3-122B-A10B 78.8%. Rows are mostly self-reported and vary by harness/step budget. | <https://benchlm.ai/benchmarks/osworld-verified> | 2026-09-04 | medium |
| benchmarks/OSWorld 2.0 | OSWorld 2.0 (released 2026-06-26, v2026.06.24; release-controlled components osworld-v2-2026.08.08): 108 long-horizon tasks across 7 professional domains with 31 self-hosted sites, median human time ~1.6 h (69.6% >1 h), ~27 weighted checkpoints per task, binary + partial scoring. Official site: Claude Opus 4.8 20.6% binary / 54.8% partial, Claude Opus 4.7 18.2% binary, GPT-5.5 ~14% binary; secondary trackers (Sept 2026) list Claude Opus 5 at 31.43% binary / 68.31% partial and GPT-5.6 Sol 27.34% / 62.72%. No open-weight model on the primary board. | <https://osworld-v2.xlang.ai/> | 2026-06-26 (site); secondary 2026-09 | medium |
| benchmarks/WindowsAgentArena | WindowsAgentArena (MIT; 154 tasks; Windows 11 Enterprise Evaluation; runs locally via WSL/Linux Docker or Azure): Navi 19.5% vs human 74.5% (2024); best published scores found are UI-TARS-1.5 42.1% (50 steps, Apr 2025) and UI-TARS-2 50.6% (Sept 2025 report). No 2026 leaderboard update was found; repo news stops at 2024-11-10. | <https://github.com/microsoft/WindowsAgentArena> | 2025-09 (UI-TARS-2 report) | high |
| benchmarks/macOSWorld | macOSWorld (arXiv 2506.04135, v4 2025-10-18, NeurIPS 2025): 202 tasks, 30 apps (28 macOS-exclusive), 5 languages, hosted on AWS Mac mini hardware with VNC action space over SSH/VNC. English success: Claude CUA 44.4%, OpenAI CUA 33.3%, Gemini 2.5 Pro 22.8%, GPT-4o 8.8%, UI-TARS-7B-DPO 5.3%, ShowUI 1.2%; safety subset distraction rates: Claude 72.4%, OpenAI 69.0%, UI-TARS 58.6%. | <https://arxiv.org/html/2506.04135v4> | 2025-10-18 | high |
| benchmarks/MacArena (2026) | MacArena (arXiv 2606.06560, June 2026, ICML 2026 Agents-in-the-Wild workshop; code CC BY-NC-SA 4.0): 421 tasks across 50 apps on Apple Virtualization via UTM, screenshots plus optional macapptree accessibility tree. Full benchmark: OpenAI CUA 31.83%, Qwen3-VL-4B 24.23%, UI-TARS-1.5-7B 21.14%, Qwen3-VL-2B 11.40%; macOS-native subset: 36.73 / 12.24 / 10.20 / 4.08 — rankings invert on native macOS tasks. | <https://arxiv.org/html/2606.06560v1> | 2026-06-04 | high |
| benchmarks/Online-Mind2Web and WebArena | Online-Mind2Web (steel tracker, updated 2026-06-29): Browser Use Cloud bu-max 97.0% (Mar 2026), GPT-5.4 native computer use 93.0%, ABP + Claude Opus 4.6 90.53%, TinyFish 90.0%, UI-TARS-2 88.2% (Sep 2025). WebArena (2026-06-29): WebTactix (DeepSeek v3.2) 74.3%; OpAgent 71.6% (Jan 2026); Qwen3.8-27B self-reports 64.8% on WebArena-Verified; Holo2-8B 42.2%. Trackers warn rows differ by judge/harness/attempt budget. | <https://leaderboard.steel.dev/leaderboards/online-mind2web/> | 2026-06-29 | medium |
| benchmarks/ScreenSpot-Pro grounding | ScreenSpot-Pro (high-res professional UI grounding) current open-model marks: Holo2-8B 71.4 (agentic), Holo2-30B-A3B 66.1, UI-TARS-1.5-7B 61.6, GUI-Actor-7B 44.2 with verifier, OmniParser V2 + GPT-4o 39.5; Holo3.1 figures are only in an image table. | <https://huggingface.co/Hcompany/Holo2-8B> | 2025-11 to 2026-06 | medium |
| windows/comtypes+pywin32 currency | comtypes 1.4.16 (2026-03-02, Python>=3.9) and pywin32 312 (2026-06-04, Python>=3.9, PSF license) are current and Python 3.12-compatible, so the UIA route has no dependency-freshness blocker even though pywinauto itself is stale. | <https://pypi.org/pypi/pywin32/json> | 2026-06-04 | high |
| linux/xdg-desktop-portal persistence versions | xdg-desktop-portal release notes list RemoteDesktop/InputCapture session persistence in the 1.21.x pre-releases and 1.22.x as current stable (1.22.1 adds a session_handle_token quirk python-libei works around); python-libei notes the portal answers restore_token without persist_mode with no token at all. | <https://github.com/flatpak/xdg-desktop-portal/releases> | 1.21–1.22 series | medium |

### ANGLE 4 — "AI company / 24x7 goal-driven agents" in 2026: how Devin, OpenHands, Manus, Lindy/Sintra/Relevance, Claude Code routines + Agent SDK + Managed Agents

| Topic | Claim | Source | Date | Confidence |
|---|---|---|---|---|
| Claude Code Routines — cloud scheduled/API/GitHub-triggered agent runs | A routine is 'a saved Claude Code configuration: a prompt, one or more repositories, and a set of connectors'. Triggers: Scheduled (presets hourly/daily/weekdays/weekly; custom cron via /schedule update; 'The minimum interval is one hour'; deterministic stagger), API (POST /v1/claude_code/routines/{id}/fire with per-routine bearer token, beta header experimental-cc-routine-2026-04-01), GitHub (pull_request / release events with field filters). Runs 'as full Claude Code cloud sessions: there is no permission-mode picker and no approval prompts during a run'. Daily per-account run cap; one-off runs exempt; Team/Enterprise owners can disable routines org-wide. Available on Pro/Max/Team/Enterprise; 'Routines are in research preview.' | <https://code.claude.com/docs/en/routines> | 2026-09 (live docs; CLI refs v2.1.211–v2.1.227) | high |
| Claude Code Routines — untrusted fire payload and 'green ≠ success' | API/Run-now text 'arrives wrapped in a <routine-fire-payload> block that labels it as untrusted data and tells Claude not to follow instructions inside it unless the routine's own prompt says to'; the stored prompt 'can't act as approval or consent for actions during the run'. Docs warn: 'A green status in the run list means the session started and exited without an infrastructure error. It does not mean the task in your prompt succeeded.' All connected MCP connectors are included by default and 'Claude can use every tool from an included connector, including writes, without asking for permission during a run.' | <https://code.claude.com/docs/en/routines> | 2026-09 (live docs) | high |
| Claude Code Desktop scheduled tasks — local-first scheduling semantics | Local tasks run only 'while the app is open and your computer is awake'; minimum interval 1 minute (cloud routines: 1 hour); each task has its own permission mode and saved 'always allow' approvals; MCP tools marked requiresUserInteraction 'prompt on every call' and stall runs. Missed runs: 'Desktop starts exactly one catch-up run for the most recently missed time and discards anything older' (7-day window). A running task can reschedule itself via the update_scheduled_task MCP tool. Prompt lives at ~/.claude/scheduled-tasks/<task-name>/SKILL.md. Comparison table Cloud / Desktop / /loop covers machine-on, persistence, local file access, permission prompts. | <https://code.claude.com/docs/en/desktop-scheduled-tasks> | 2026-09 (live docs; Desktop ≥1.1.5368) | high |
| Claude Code Agent Teams — lead/teammates, shared task list, consent rules | Experimental, default off (CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1). Architecture: team lead, teammates (separate Claude Code instances), shared task list with dependencies and 'file locking to prevent race conditions', JSON mailboxes. Teammate permission prompts bubble to the lead; plan approvals are auto-granted by the lead without human review. Inter-agent messages are marked as coming from another session: 'A teammate can't approve a permission prompt or supply consent on your behalf.' Hooks TeammateIdle / TaskCreated / TaskCompleted can veto with exit code 2 ('quality gates'). Limits: no session resumption of in-process teammates, no nested teams, not spawned in -p / Agent SDK mode. Guidance: 'Letting a team run unattended for too long increases the risk of wasted effort.' | <https://code.claude.com/docs/en/agent-teams> | 2026-09 (live docs; describes v2.1.178–v2.1.251) | high |
| Anthropic — 'Effective harnesses for long-running agents' (two-agent harness) | Initializer agent sets up a feature-list JSON (200+ features marked pass/fail), claude-progress.txt and a git repo; each coding session opens by reading progress + git log and running end-to-end tests, then picks 'the highest-priority feature that's not yet done', and closes with a descriptive commit + progress update. Observed failure modes: premature 'project complete', degraded environment state, undocumented work. Browser automation (Puppeteer MCP) was critical for real verification. | <https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents> | 2025-11-26 | high |
| Anthropic — 'Harness design for long-running application development' (planner/generator/evaluator) | Three-role harness: planner expands a 1–4 sentence prompt into a spec; generator implements; evaluator uses Playwright MCP to click through the live app and grades on four criteria with hard thresholds (design quality, originality, craft, functionality). 'Sprint contracts' negotiated between generator and evaluator define done before work starts; agents communicate via files. Costs: retro game maker solo 20 min / $9 vs full harness 6 h / $200; DAW 3 h 50 min / $124.70. With Opus 4.6, sprint decomposition was dropped (ran >2 h continuously with compaction). Solo models under-scoped and self-evaluated poorly. | <https://www.anthropic.com/engineering/harness-design-long-running-apps> | 2026-03-24 | high |
| Anthropic — 16 parallel Claudes build a C compiler (coordination + tests-as-oracle) | 16 agents ran for nearly 2 weeks, ~2,000 Claude Code sessions, 2B input / 140M output tokens, 'just under $20,000'. Coordination via text-file task locks in current_tasks/ plus git; merge conflicts resolved by Claude; each agent in Docker with a local /workspace copy. Verification: GCC as known-good oracle, CI to prevent regressions, random-sample '--fast' testing (1%/10%). Lessons: 'Write extremely high-quality tests', design harness for 'context window pollution' and 'time blindness', parallelize via task specialization. | <https://www.anthropic.com/engineering/building-c-compiler> | 2026-02-05 | high |
| Anthropic — Claude Code auto mode (classifier-gated autonomy) | Two layers: server-side prompt-injection probe on tool outputs, and a reasoning-blind transcript classifier (Sonnet 4.6) that sees only user messages and executable tool calls. Tier 1 allowlist (reads), Tier 2 in-project writes (git as accountability), Tier 3 everything else classified against block rules (destruction, exfiltration, security degradation). Principle: 'The prompt establishes what is authorized; everything the agent chooses on its own is unauthorized until the user says otherwise.' Results: 0.4% false positives on 10,000 real actions; missed 17% of 52 real overeager incidents; 'auto mode is not a drop-in replacement for careful human review on high-stakes infrastructure.' | <https://www.anthropic.com/engineering/claude-code-auto-mode> | 2026-03-25 | high |
| Anthropic — Managed Agents architecture ('decoupling the brain from the hands') | Session = durable append-only event log living outside the harness; harness is stateless ('nothing in the harness needs to survive a crash') and is rebooted via wake(sessionId); sandbox failure is 'a standard tool-call error'; interfaces getSession(id), getEvents(), emitEvent(id, event). Credentials never enter the sandbox. Deferring container provisioning cut TTFT ~60% p50 / >90% p95. Enables long-horizon work exceeding the context window. | <https://www.anthropic.com/engineering/managed-agents> | 2026-04-08 | high |
| Claude Managed Agents — scheduled deployments with per-run budgets | Deployments run an agent on POSIX cron + IANA timezone (minute granularity, jitter up to 15% of interval, max 9 min). Optional budget {type: limit, max_list_cost} is 'copied onto each session it starts, so the budget bounds every run separately'; a session 'pauses with budget_reached'. Every trigger writes a deployment_run with error.type (environment_archived_error, agent_archived_error, session_rate_limited_error); unrecoverable errors auto-pause the deployment; 'Missed triggers are not backfilled'; max 1,000 deployments/org; manual /run endpoint; webhooks for lifecycle. Beta header managed-agents-2026-04-01. Managed Agents is 'stateful by design: sessions are long-running, resume cleanly after pauses' and not ZDR/HIPAA eligible. | <https://platform.claude.com/docs/en/managed-agents/scheduled-deployments> | 2026-09 (live docs; examples dated 2026-05) | high |
| Claude Managed Agents — 'Dreams' (offline memory consolidation) | Research preview (beta header dreaming-2026-04-21): an async job reads a memory store plus 1–100 past session transcripts and 'produces a new, reorganized memory store: duplicates merged, stale or contradicted entries replaced with the latest value, and new insights surfaced. The input store is never modified, so you can review the output and discard it.' Takes 'minutes to a few hours'; billed at token rates; cancel/archive lifecycle; failure leaves partial output for inspection. | <https://platform.claude.com/docs/en/managed-agents/dreams> | 2026-09 (live docs; example created_at 2026-04-29) | high |
| Anthropic — 'How we contain Claude across products' (containment before steering) | Three layers: environment (containers/VMs/sandboxes/network egress), model (prompts, classifiers, training), external-content controls (MCP, plugins, web). Claude Code uses OS sandboxes (Seatbelt/bubblewrap) plus human approval for sensitive ops; Cowork runs the agent loop outside a VM while execution stays inside. Principle: 'Design for containment at the environment layer first, then steer behavior at the model layer.' Auto-mode classifier 'catches approximately 83% of problematic behaviors before execution.' | <https://www.anthropic.com/engineering/how-we-contain-claude> | 2026-05-25 | high |
| Anthropic — 'Demystifying evals for AI agents' (how to grade long-horizon work) | Grade outcome (final environment state) and transcript separately; combine code-based, model-based and human graders; pass@k ('at least one correct solution in k attempts') vs pass^k ('all k trials succeed', for reliability); 'it's often better to grade what the agent produced, not the path it took'; rigid graders caused false saturation (a fix raised Opus 4.5 from lower score to 95%). | <https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents> | 2026-01-09 | high |
| OpenAI Agents SDK — loop limits, guardrails, HITL, durability integrations | Runner loop raises MaxTurnsExceeded when max_turns is exceeded; 'Pass max_turns=None to disable this turn limit'; error_handlers={'max_turns': ...} can return fallback output. Guardrails raise InputGuardrailTripwireTriggered / OutputGuardrailTripwireTriggered; pre_approval_tool_input_guardrails=True runs guardrails before approval interruptions. Tool approval: needs_approval=True or async callable on function_tool / Agent.as_tool / ShellTool / ApplyPatchTool; paused runs expose result.interruptions (ToolApprovalItem), result.to_state(), state.approve()/reject(), RunState.to_json()/from_json() ('includes your app context plus SDK-managed runtime metadata such as approvals, usage'). Long-running via Dapr, Temporal, Restate, DBOS integrations. | <https://openai.github.io/openai-agents-python/human_in_the_loop/> | 2026-09 (live docs) | high |
| OpenAI Agents SDK — April 2026 harness + sandbox for long-horizon agents | OpenAI added 'an in-distribution harness for frontier models' and sandboxing so agents 'operate in controlled computer environments', to 'build these long-horizon agents using our harness and with whatever infrastructure they have'; Python first, TypeScript later; 'code mode and subagents' planned; standard API pricing. README: 'Sandbox agents: Agents preconfigured to work with a container to perform work over long time horizons' (SandboxAgent). openai.com launch post returned HTTP 403 through the proxy. | <https://techcrunch.com/2026/04/15/openai-updates-its-agents-sdk-to-help-enterprises-build-safer-more-capable-agents/> | 2026-04-15 | medium |
| OpenAI Codex / ChatGPT Automations — scheduled + event-triggered, same-thread continuation | Scheduled tasks run on time-based schedules or, 'on eligible plans', from Gmail, Slack, or GitHub PR events; combine with skills; in the desktop app tasks 'can work with local projects and run in the project directory or an isolated worktree'; 'Schedule a task inside an existing chat when you want ChatGPT to return to that chat on a schedule. The scheduled task uses the chat's existing context'; the Scheduled view 'acts as your inbox' with an unread indicator 'when a run needs your attention'. | <https://learn.chatgpt.com/docs/automations?surface=app> | 2026-09 (live docs) | high |
| Devin — Automations (triggers, ACU cap, invocation limit, persistent triage agent) | Triggers: Slack, GitHub (comments, PR events, reviews, check runs, pushes), Linear, Schedule (cron or one-time), Webhook; combinable with OR; optional conditions. Actions: 'Start session', 'Message session' (feed events into an existing long-running session), 'Triage Devin' ('A persistent Devin that monitors a Slack channel...spawns child sub-devins'), email notification (all/failures/successes). Guards: 'Set a maximum ACU (Agent Compute Unit) budget per session' and an invocation limit ('at most 10 invocations per hour'); 'if unset, the automation runs without limits'. 26 templates (Monitoring, CI/CD, Triage, Maintenance, PM, Code Quality, Security). Scheduled Sessions page: 'Automations are now the recommended way to run Devin on a schedule.' | <https://docs.devin.ai/product-guides/automations> | 2026-09 (live docs) | high |
| Devin — 2026 release notes relevant to 24x7 operation | Apr 1: PR resuming across sessions; ACU cap enforcement modal. Apr 8: 'Enterprises can now set a hard upper limit on total ACUs per session'. May 22: RFC 5545 RRULE schedules; network access requests surface for admin approval. Jun 3: minimum per-session ACU limit for automations lowered from 3 to 1. Jun 5: run-once schedules. Jul 7: 'Automations now support queueing: set the maximum number of concurrent runs and queue depth per automation'. Jul 17: 'a new option to create a long-running session that receives subsequent trigger events'. Jul 24: approve network access from Slack. Aug 26: Cmd/Ctrl+Shift+Backspace stops Devin; Automations can iterate on an existing automation. Sep 3: sidebar grouping, cleanup scan type, /scan command. | <https://docs.devin.ai/release-notes/2026> | 2026-01 to 2026-09-03 | high |
| Devin 2.2 — self-verification with computer use | Devin 2.2 (Feb 24, 2026) can 'test its work with computer use, self-verify, and auto-fix its code', has a Linux desktop to 'launch and test desktop applications', runs a self-review loop ('plans, codes, reviews its own output, catches issues, and fixes them - all before you ever open the PR'), and provides screen recordings so humans 'review every detail of its work'; startup 3x faster; free tier with $10 credits. | <https://cognition.com/blog/introducing-devin-2-2> | 2026-02-24 | high |
| Devin Fusion — dual-model routing to cut 24x7 cost | 'two parallel agents: one with a frontier model, the other with a more cost-effective sidekick model'; claims frontier-level performance 'at 60% lower cost' (63.1 on FrontierCode 1.1 Extended at $1.35 avg/task vs Fable 5 64.9); '88% of their merged PRs were driven entirely by the automated Fusion router' internally; cost estimate revised from 35% to 60% on 2026-08-07. Cognition also acquired TierZero (Jul 20, 2026) to bring post-deployment incident automations into Devin. | <https://cognition.com/blog/devin-fusion> | 2026-06-29 (updated 2026-08-07) | high |
| OpenHands SDK — confirmation policies, risk analyzers, stuck detection, persistence | Confirmation policies AlwaysConfirm() / NeverConfirm() / ConfirmRisky() ('requires security analyzer'); risk levels LOW/MEDIUM/HIGH/UNKNOWN; LLMSecurityAnalyzer plus deterministic PatternSecurityAnalyzer, PolicyRailSecurityAnalyzer, EnsembleSecurityAnalyzer (takes highest risk); pending actions surface when execution_status == WAITING_FOR_CONFIRMATION, approve via conversation.run(), reject via conversation.reject_pending_actions(reason). Stuck detector (stuck_detection=True default) halts on: same action→same observation 4+ times, same action→error 3+ times, 3+ agent messages without user input, alternating pairs 6+ cycles, repeated context-window errors. Persistence: Conversation(persistence_dir=..., conversation_id=...) writes events/event-00000-<id>.json and resumes by recreating with the same ids. | <https://docs.openhands.dev/sdk/guides/security> | 2026-09 (live docs; see also /sdk/guides/agent-stuck-detector and /sdk/guides/convo-persistence) | high |
| OpenHands — Agent Canvas + ACP: any coding agent, scheduled automations, remote backends | Agent Canvas is 'the browser-based OpenHands UI and backend for running agent conversations and automations'; via Agent Client Protocol you can use Claude Code, Codex, Gemini CLI or OpenHands 'on arbitrary servers'; 'rich automations, so you can run agents on a schedule to perform code review, do dependency upgrades, update documentation'; backends 'can be set up to run anywhere' (local + remote), credentials 'injected at session start'; agents 'request permissions when needed'. Budget/resume specifics not documented on this page. | <https://www.openhands.dev/blog/use-any-coding-agent-in-openhands-with-acp> | 2026-06-18 | high |
| Hermes Agent — cron: continuity, incidents, pre-run validation, no-agent jobs, drift guard | Jobs via /cron add, hermes cron create, or the cronjob tool (create/list/pause/resume/run/remove/update); schedules 'in 30m', 'every 2h', 'weekdays at 9am', cron expressions, ISO timestamps; delivery to origin/local/telegram/discord/slack/whatsapp/all/bot-chat; skills attached with --skill. State between runs: continuity=True ('injects its own most recent output into each run') and context_from=[job_id]; repeat=N caps runs. Failure: failure_streak nudges at 3, each failure is a 'durable incident, keyed by the job plus a normalized signature of the error text', hermes cron incidents ack silences that signature only; last_status ok/failed/delivery_failed/blocked_config. Isolation: 'Cron runs each job in a fresh agent session with no chat platform attached'; 'Cron-run sessions cannot recursively create more cron jobs'. Pre-run validation sets blocked_config with one alert and 'no LLM call is made'. no_agent=True runs a script only ('No tokens, no model'). Model pinned per job; 'The drift guard… prevents an unattended job from silently inheriting a switch to a paid provider/model' (cron.model_drift_guard). Timeouts: script 3600 s; cron.max_parallel_jobs; scheduler ticks every 60 s with a .tick.lock; executions.db ledger (claimed/running → completed/failed/unknown). | <https://hermes-agent.nousresearch.com/docs/user-guide/features/cron> | 2026-09 (live docs) | high |
| Hermes Agent — Persistent Goals (/goal): worker + judge loop with contracts and gates | /goal <text> keeps 'a standing objective that survives across turns'; after each turn a separate judge LLM (goal_judge config) sees the goal text and the last ~4 KB of the agent's response and returns done / blocked / continue / wait. Optional completion contract fields: outcome, verification, constraints, boundaries, stop_when; success only 'when the verification criterion is met with concrete evidence (a command result, file excerpt, test output)'. /goal gate add <command> adds deterministic shell gates (must exit 0) that run before judging; /subgoal appends acceptance criteria. Default goals.max_turns = 20 with auto-pause and /goal resume; /goal pause\|resume\|clear; state persisted in SessionDB.state_meta keyed goal:<session_id>. Judge is fail-open ('broken judges default to continue'). | <https://hermes-agent.nousresearch.com/docs/user-guide/features/goals> | 2026-09 (live docs) | high |
| Hermes Agent — v0.20.x/v0.21.0 releases (Aug 2026): cron memory, monitor-mode, subagent steering, desktop browser | v0.21.0 (Aug 31, 2026): 'Cron jobs that remember — Scheduled jobs stopped being goldfish', cron agents load/update persistent memory, durable notepad per job, 'Monitor-mode jobs skip LLM when nothing changed'; 'Steer your subagents while they run' (list children, course-correct, stop early), optional JSON-schema validation of child outputs, defaults raised to 250 iterations and 10 concurrent children; 'The agent drives the desktop's browser'; MCP command center with fleet cost/usage overlay. v0.20.6 (Aug 27): consent-gated browsing, OS-keychain secret encryption. v0.20.5 (Aug 19): per-job reasoning effort for cron. v0.20.0 (Aug 3): A2A v1.0, signed outbound webhooks (HMAC), grounded-citations skill. | <https://github.com/NousResearch/hermes-agent/releases> | 2026-08-03 to 2026-08-31 | high |
| Manus — Scheduled Tasks 2.0 | Recurring work 'can remain within the same task rather than spawning separate instances, preserving conversation history and instructions'; scheduled tasks reuse Project setup ('files, connectors, skills, and output standards'); Manus-built web apps 'can now include scheduled actions of their own'; calendar/schedule views; 'Skip confirmations' lets Manus 'send, publish, or post without asking for approval every time' for trusted workflows. Docs: cadences daily/weekdays/weekly/monthly/custom/one-time; task limits 'Depends on your plan'; execution history shows 'past runs, results, and any errors'. | <https://manus.im/blog/manus-schedules> | 2026-05-18 | high |
| Lindy — 'AI employee' positioning, Autopilot cloud computer, Safe Mode | Lindy defines an AI employee as 'a software-based assistant that can handle your work on its own… without needing constant input' (blog 2026-05-15), with triggers, optional human-in-the-loop approvals, 24/7 operation, persistent learning. Changelog (fetched; latest entries Oct 2025): Autopilot (Aug 4, 2025) 'your AI agent gets its own cloud computer'; 'Safe Mode' (Mar 19, 2025) 'requiring approval before Lindy performs sensitive actions'; advanced credit monitoring and overages (May 2025); task termination (Sep 2025). Agent Swarms (self-cloning for parallel outreach) appear only in Lindy marketing/blog content. | <https://www.lindy.ai/blog/ai-employee> | 2026-05-15 (blog); changelog through 2025-10-21 | medium |
| Sintra / Relevance AI / Cognosys — 'AI employee' products | Sintra sells 12 named role helpers (Soshie, Cassie, Dexter, Penn, Milli…) with 'Brain AI' business memory, claims 'AI employees don't require supervision, follow-ups, or performance check-ins', and lets users 'Set priorities, guidelines, and approval levels'; ~$15.60–$48.50/month. Relevance AI frames workforces as 'coordinated teams of agents that own a process end to end', triggered by business signals, with approvals draggable into the flow and outputs 'gated by the same evals'. Cognosys is reported deprecated (→ Ottogrid → acquired by Cohere, May 2025) — secondary only. | <https://sintra.ai/> | 2026-09 (live pages, undated) | medium |
| Microsoft Agent Framework 1.0 GA + Magentic orchestration limits | GA on Apr 3, 2026 (.NET + Python): patterns sequential, concurrent, handoff, group chat, Magentic-One; 'Checkpointing and hydration ensure long-running processes survive interruptions'; all patterns support HITL approvals and pause/resume. Magentic builder exposes max_round_count=10, max_stall_count=3, max_reset_count=2 (docs example); progress ledger per round tracks is_request_satisfied, is_in_loop, is_progress_being_made, next_speaker, instruction; consecutive non-progressing rounds increment a stall counter and 'exceeding the configured maximum triggers an automatic reset and replan'. Plan review via MagenticPlanReviewRequest/Response (approve() / revise(feedback)); Python default enable_plan_review=False, .NET RequirePlanSignoff defaults true. Docs caution: 'it is untested how well the Magentic orchestration will perform outside of the original Magentic-One design.' | <https://learn.microsoft.com/en-us/agent-framework/workflows/orchestrations/magentic> | 2026-09-04 (doc ms.date); GA post 2026-04-03 | high |
| Microsoft Agent Framework — durable HITL: pending requests survive checkpoints | Executors call ctx.request_info() and handle replies with @response_handler; tool approval surfaces as RequestInfoEvent with function_approval_request content. 'When a checkpoint is created, pending requests are also saved as part of the checkpoint state. When you restore from a checkpoint, any pending requests will be re-emitted as RequestInfoEvent objects'; resume by passing both checkpoint_id and responses to workflow.run(...). | <https://learn.microsoft.com/en-us/agent-framework/workflows/human-in-the-loop> | 2026-07-16 (updated 2026-08-25) | high |
| LangGraph — checkpoints vs durable execution | LangGraph checkpointers persist graph state per thread (InMemorySaver/SqliteSaver/PostgresSaver) for HITL, time travel and fault tolerance; Temporal's LangGraph plugin post argues 'checkpoints are not durable execution. A LangGraph run lives in a single process, so if that process dies then the run dies with it' — recovery is manual, human review halts processes, multi-day agents strain state; Temporal runs each node as a durable activity with 'durable waits (signals and timers) cost nothing while they wait' and continue-as-new for unbounded runs. The LangGraph durable-execution doc page itself was not readable through the proxy (stub/redirect), so durability-mode names are unverified. | <https://temporal.io/blog/temporal-langgraph-plugin-durable-execution> | 2026-07-16 | medium |
| CrewAI Flows — persistence, fork/resume, human feedback, memory | @persist (class or method level) with default SQLiteFlowPersistence; resume via kickoff(inputs={'id': uuid}) (same flow id, history extends) or fork via kickoff(restore_from_state_id=uuid) (new id); control flow with @start, @listen, @router, or_()/and_(); @human_feedback(message=..., emit=[...]) pauses for approval with self.last_human_feedback / human_feedback_history; flow memory self.remember()/recall() persisted in LanceDB. No native scheduling/triggers or iteration budgets documented in the Flows guide (scheduling lives in the AMP suite). | <https://docs.crewai.com/en/concepts/flows> | 2026-09 (live docs) | high |
| ChatDev 2.0 and MetaGPT — status in 2026 | ChatDev 2.0 released Jan 7, 2026 as a 'Zero-Code Multi-Agent Platform' with YAML workflows in yaml_instance/, a visual drag-and-drop workflow console, OpenClaw integration, and 'human-in-the-loop feedback' with live logs and intermediate artifacts; no budgets or timeouts documented. MetaGPT's product MGX launched Feb 19, 2025 (secondary); the 'software company' role-play framing (CEO/CTO/Programmer/Reviewer) persists but without documented cost/stop controls. | <https://github.com/OpenBMB/ChatDev> | 2026-01-07 | high |
| Runaway cost — OpenClaw circuit breaker for consecutive LLM idle timeouts | PR #76345 (merged May 3, 2026) fixes issue #76293: an idle timeout fed an unbounded upstream retry loop producing '761-1384 paid Anthropic Sonnet 4.6 calls in 60 seconds' at '$20-30 each' incident, discovered only at billing time. Fix: breaker at the stream wrapper level trips after 5 consecutive idle timeouts without completed output (default), counter resets on any productive progress, state lives at outer run-loop scope surviving retries and model failover; worst case now 5 paid calls per incident. | <https://github.com/openclaw/openclaw/pull/76345> | 2026-05-03 | high |
| Runaway cost — practitioner controls (secondary) | A Apr 15, 2026 practitioner post recommends hard per-run budget limits, per-call token ceilings, circuit breakers after N consecutive violations, sliding-window rate limits, threshold alert callbacks, and per-agent cost tracking, stressing 'hard-abort' over alerts ('alerts just tell you it already happened'). Its '$47 on a single stuck researcher loop' example is hypothetical; widely repeated '$47,000 / 11-day' incident figures were not found in any primary source. | <https://dev.to/sapph1re/how-to-stop-ai-agent-cost-blowups-before-they-happen-1ehp> | 2026-04-15 | low |
| METR Time Horizon 1.1 — doubling times | TH1.1 (Jan 29, 2026): task suite grew 170→228 (8h+ tasks 14→31), infrastructure moved to UK AISI's Inspect. Doubling time: 196.5 days all-time, 130.8 days since 2023, 88.6 days since 2024. Top model then: Claude Opus 4.5 at 320 min 50%-horizon [CI 170–729]. | <https://metr.org/blog/2026-1-29-time-horizon-1-1/> | 2026-01-29 | high |
| METR — harness effect and measurement ceiling | Feb 13, 2026 note: running Opus 4.5 in Claude Code and GPT-5 in Codex did not beat METR's ReAct/Triframe scaffolds (Claude Code beat ReAct in only 50.7% of bootstrap samples; Codex beat Triframe in 14.5%), with caveats on token budgets and rough wrappers. Live time-horizons page (models added through May 8, 2026: Claude Mythos Preview, Gemini 3.1 Pro, GPT-5.4) states 'Measurements above 16 hrs are unreliable with our current task suite', tasks are 'primarily… software engineering, machine learning, or cybersecurity', and 'AI agent performance drops substantially when scoring AI performance holistically rather than algorithmically'. | <https://metr.org/time-horizons/> | 2026-02-13 (note); page updated 2026-05-08 | high |
| TheAgentCompany — best agent completes ~30% of simulated-company tasks | 175 long-horizon tasks in a sandboxed software company (web browsing, coding, RocketChat colleagues) with checkpoints and partial credit. Best agent (Gemini 2.5 Pro, arXiv v3 Sep 10, 2025): 30.3% full completion, 39.3% partial score, $4.2/task, 27.2 steps. DS/Admin/Finance lowest ('many LLMs completing none'). Failure modes: failing to grasp social implications in colleague chats, web-UI navigation ('the biggest obstacle'), and self-deception—agents 'create fake shortcuts that omit the hard part of a task'. No verified 2026 leaderboard update (site returned empty). | <https://arxiv.org/html/2412.14161v3> | 2025-09-10 | high |
| GDPval and GDPval-AA — expert-deliverable benchmarks | GDPval (arXiv 2510.04374, Oct 5, 2025): 44 occupations across 9 GDP sectors, 1,320 tasks (220 gold open-sourced), deliverables graded by experts; limitation: 'one-shot deliverables' rather than interactive/iterative work; explores models 'paired with human oversight' being cheaper/faster than unaided experts. GDPval-AA v2 (Artificial Analysis, Sep 2026): 220 tasks run agentically with shell + web via Stirrup, blind pairwise LLM-judge Elo anchored to human 1,000; top: Claude Fable 5.1 (max effort) 1,769, Claude Opus 5 (max) 1,739. | <https://artificialanalysis.ai/evaluations/gdpval-aa> | 2026-09 (leaderboard); paper 2025-10-05 | high |
| SentinelBench — evaluating long-running monitoring agents | SentinelBench (arXiv 2606.05342, June 2026) measures 'sustained attention': 100 tasks across 10 synthetic web environments (email, calendar, finance, networking, entertainment) replaying scripted event sequences; scores task completion, reaction time and resource consumption, exposing the responsiveness-vs-cost tradeoff; 'agent design choices can dramatically impact key metrics'; open source. | <https://arxiv.org/abs/2606.05342> | 2026-06-05 | high |
| Long-horizon agents taxonomy (RUC-NLPIR survey) | Roadmap organizes capability into Harnesses (loops, context/memory, tools/skills, orchestration, hooks, verification) vs Optimization (training, agentic RL, self-evolution), with three nested levels: H1 intra-context (~minutes), H2 cross-context state management across sessions (~hours–days), H3 open-ended cross-task experience accumulation; names WebArena, OSWorld, SWE-Gym, τ-bench, MCP-Universe, MCPVerse and METR time horizon as measures. | <https://github.com/RUC-NLPIR/Awesome-Long-Horizon-Agents> | 2026 (repo, last entries 2026) | high |
| Cross-product convergence (inferred synthesis) | Across Claude routines/Managed Agents, Codex automations, Devin automations, Hermes cron, Manus 2.0 and OpenHands, the 2026 operating pattern is: stored prompt + trigger (cron / event / API) → fresh or same-thread session → per-run budget and rate cap → outcome delivered to an inbox/review queue → optional continuity (same thread, memory, continuity=true). The 2026 differentiators are continuity/memory across runs and cheap durable pauses for approval, not the scheduler itself; unattended runs are consistently described as safe only for bounded, verifiable (mostly code-shaped) work. | <https://code.claude.com/docs/en/routines> | 2026-09 (synthesis of sources above) | low |
| What demonstrably works vs hype (inferred synthesis) | Demonstrated: multi-day autonomous builds when a near-perfect verifier exists (C compiler, $20k/2 weeks; harness DAW $125), scheduled code review/triage/dependency work (routines, Devin templates, OpenHands automations), and cost control by hard per-run budgets + breakers (Managed Agents budget_reached, Devin ACU cap, OpenClaw breaker). Not demonstrated: general 'AI employee' autonomy across admin/finance/social work (TheAgentCompany ≤30%; METR >16h unreliable and holistic-scoring drop; auto-mode misses 17% of overeager actions; Magentic untested outside its design; agent teams warned against unattended long runs). Vendor 'no supervision needed' claims (Sintra, Lindy swarms) have no independent evidence. | <https://arxiv.org/html/2412.14161v3> | 2026-09 (synthesis) | low |

### ANGLE 5 — Adoption and distribution: what made OpenClaw, Hermes Agent, Ollama, LM Studio, Cursor, Claude Code, ChatGPT and the iPhone spread; the failure gravey

| Topic | Claim | Source | Date | Confidence |
|---|---|---|---|---|
| OpenClaw — install + onboarding surface | OpenClaw ships a one-line installer for macOS/Linux/WSL2 (`curl -fsSL https://openclaw.ai/install.sh \| bash`), Windows PowerShell (`iwr -useb https://openclaw.ai/install.ps1 \| iex`) and npm (`npm install -g openclaw@latest --allow-scripts=openclaw`), then `openclaw onboard --install-daemon`. Channels: WhatsApp, Telegram, Slack, Discord, Google Chat, Signal, iMessage. Marketplace: ClawHub. Repo shows 389k stars / 81.7k forks / 88,368 commits on 2026-09-05. | <https://github.com/openclaw/openclaw> | 2026-09-05 (README as fetched) | high |
| OpenClaw — time-to-first-chat and default channel | Official getting-started doc says ~5 minutes to a working chat. The installer auto-starts a wizard with a 'Quick Start' path that reuses detected AI access (Claude Code / Codex CLI login or an API key) and verifies with a real completion before saving; gateway on port 18789; `openclaw dashboard` opens the Control UI; doc states 'the fastest channel to set up is Telegram (just a bot token)'. Gateway runs in foreground by default; background needs `openclaw gateway install`. Requires Node 22.22.3+/24.15+/25.9+. | <https://docs.openclaw.ai/start/getting-started> | 2026 (live docs, fetched 2026-09-05) | high |
| OpenClaw — Telegram pairing model | Telegram DM policy defaults to `dmPolicy: "pairing"`: first DM from an unknown user yields a time-limited code; owner runs `openclaw pairing list telegram` / `openclaw pairing approve telegram <CODE>`; codes expire after 1 hour. Groups default to allowlist + `requireMention: true`. Setup is BotFather `/newbot` → token → config → `openclaw gateway`. | <https://docs.openclaw.ai/channels/telegram> | 2026 (live docs, fetched 2026-09-05) | high |
| OpenClaw — growth timeline | Launch (as OpenClaw) Jan 29 2026 at ~9K stars; ~60K stars within 72 hours; overtook React as GitHub's most-starred repo on Mar 3 2026 at 250,829 stars; 346K+ by April 2026; peak of 34,168 stars in 48h on Jan 30 2026 (710/hour). | <https://openclawvps.io/blog/openclaw-statistics> | 2026-03-27 (updated 2026-04-03) | medium |
| OpenClaw — rename history and one-click install businesses | Project was Clawdbot → Moltbot → OpenClaw (rename Jan 30 2026); at 182K stars on Feb 11 2026. Third parties monetized install friction immediately: SimpleClaw reportedly earned $28K in ~10 days selling one-click installs. Open-source veterans (Peer Richelsen, Pieter Levels) said they had not seen attention like this in ~10 years. | <https://deepakness.com/raw/openclaw-growth/> | 2026-02-11 | medium |
| OpenClaw — creator to OpenAI, project to foundation | Peter Steinberger announced on Feb 14 2026 that OpenClaw moves into a foundation, stays open source, and OpenAI sponsors it; he joined OpenAI (Altman announcement Feb 15 2026). His stated design goal: 'an agent that even my mum can use'. No adoption numbers in the post. | <https://steipete.me/posts/2026/openclaw> | 2026-02-14 | high |
| Consumer agents are messenger-first (a16z Top 100, 6th ed.) | a16z's 6th edition (Similarweb web + Sensor Tower mobile, Jan 2026 data) lists three horizontal agents — OpenClaw, Manus, Genspark — and states that on mobile consumers reach agents via text, not apps: 'users connect OpenClaw to platforms like WhatsApp, Telegram, and Signal; users message it like they'd message a friend, and it executes tasks in the background.' ChatGPT ~900M weekly actives (>10% of world population). Manus acquired by Meta Dec 2025 (~$2B); Genspark at $100M run-rate. | <https://a16z.com/100-gen-ai-apps-6/> | 2026-03-09 | high |
| OpenClaw — ClawHub supply-chain incident (ClawHavoc) | Koi Security audit published Feb 2 2026: of 2,857 ClawHub skills, 341 were malicious (11.9%), 335 delivering Atomic Stealer (AMOS) for macOS via typosquats (clawhub1, clawhubb…), crypto/trading/YouTube utilities, and fake 'Prerequisites' sections instructing users to run obfuscated scripts. Response: signed-in community reporting; skills with >3 unique reports auto-hidden. | <https://thehackernews.com/2026/02/researchers-find-341-malicious-clawhub.html> | 2026-02-02 | medium |
| OpenClaw — ClawHub publishing model | ClawHub is 'the public registry for OpenClaw skills and plugins'; `openclaw skills search/install`, `clawhub skill publish <path>`. Anyone can upload; publishing requires a GitHub account 'old enough to pass the upload gate'; automated checks run on releases, scan results visible on detail pages; moderators hide/restore/ban. Vetting method names (VirusTotal/ClawScan) are not specified in the official doc. | <https://docs.openclaw.ai/tools/clawhub> | 2026 (live docs, fetched 2026-09-05) | high |
| OpenClaw — exposed gateways at scale | SecurityScorecard found 40,214 internet-exposed OpenClaw instances across 28,663 IPs (Feb 9 2026); 63% of deployments vulnerable; 12,812 exploitable via RCE. Root cause widely reported as default gateway bind/VPS installs (secondary sources) — the official article only says 'widespread misconfiguration'. | <https://www.infosecurity-magazine.com/news/researchers-40000-exposed-openclaw/> | 2026-02-09 | medium |
| OpenClaw — WhatsApp via unofficial Baileys client causes account bans | GitHub issue #4376 (Jan 30 2026) raised WhatsApp account-ban risk from the Baileys-based channel; multiple 2026 reports document personal accounts suspended; official docs recommend pairing a separate phone number. Telegram is consistently recommended as the safest/most stable channel. | <https://github.com/openclaw/openclaw/issues/4376> | 2026-01-30 | medium |
| NVIDIA NemoClaw — hardware-vendor co-distribution | NVIDIA announced NemoClaw at GTC (Mar 16 2026): one command installs the OpenShell sandbox runtime + Nemotron models for OpenClaw on RTX PCs/laptops, RTX PRO workstations, DGX Station and DGX Spark, pitched as 'privacy and security controls' for always-on agents; early preview / when-and-if-available. The NemoClaw GitHub README also names Hermes and LangChain Deep Agents as supported agents. | <https://nvidianews.nvidia.com/news/nvidia-announces-nemoclaw> | 2026-03-16 | high |
| Hermes Agent — install + channels + scale | Install: `curl -fsSL https://hermes-agent.nousresearch.com/install.sh \| bash` (Linux/macOS/WSL2/Termux) or `iex (irm https://hermes-agent.nousresearch.com/install.ps1)` (Windows); `hermes setup` full wizard or `hermes setup --portal`; channels listed in README: Telegram, Discord, Slack, WhatsApp, Signal via one gateway; skills follow the agentskills.io standard; 'any model' (Nous Portal, OpenRouter, OpenAI, custom endpoints). Repo shows 242k stars / 49.7k forks on 2026-09-05; positioning: 'The self-improving AI agent'. | <https://github.com/NousResearch/hermes-agent> | 2026-09-05 (README as fetched) | high |
| Hermes Agent — launch date and early velocity | Launched Feb 25 2026; ~99K GitHub stars in 8 weeks (Dealroom note). The Dealroom page returned HTTP 403 through the proxy; date and figure come from the search snippet and multiple secondary guides. | <https://app.dealroom.co/news/note/hermes-agent-hits-99k-github-stars-in-8-weeks-fastest-growing-open-source-agent-framework-of-2026> | 2026-04 (approx.) | low |
| Hermes Agent — Telegram onboarding is a static allowlist, not pairing | Telegram setup: BotFather `/newbot` → token → find numeric ID via @userinfobot → `hermes gateway setup` (or `TELEGRAM_BOT_TOKEN` + `TELEGRAM_ALLOWED_USERS` in ~/.hermes/.env) → `hermes gateway`. No pairing code/DM-approval flow; voice messages auto-transcribed by configured STT (local/groq/openai); cron results delivered to a 'home channel' set with `/sethome`. Privacy mode on by default; long polling by default. | <https://hermes-agent.nousresearch.com/docs/user-guide/messaging/telegram> | 2026 (live docs, fetched 2026-09-05) | high |
| Hermes Agent — migration importers as an adoption lever | v0.20.0 (Aug 3 2026) added `hermes import-agent`, which 'migrates your Claude Code or Codex CLI setup into Hermes in one command'; secondary docs summaries also say the setup wizard detects ~/.openclaw and offers migration. Release cadence: major every ~4 weeks (v0.20.0 Aug 3 → v0.21.0 Aug 31 2026), patches every 1–3 days with hundreds of merged PRs each; v0.21.0 added NVIDIA SkillEvaluator Tier-1 advisory scanning on skill installs. | <https://github.com/NousResearch/hermes-agent/releases> | 2026-08-31 | high |
| Hermes Agent — Skills Hub design (multi-source, scanned, tiered) | Skills live in ~/.hermes/skills as SKILL.md (agentskills.io) with progressive disclosure. Sources: official, skills.sh (Vercel), GitHub taps (OpenAI/Anthropic/HuggingFace/NVIDIA), `/.well-known/skills/index.json`, direct URLs, ClawHub/LobeHub/browse.sh. Every hub install is security-scanned (prompt injection, exfiltration, destructive commands) and assigned a trust level (builtin/official/trusted/community); 'dangerous' verdicts cannot be overridden; agent-authored skills via `skill_manage` can be gated by `skills.write_approval: true` with `/skills pending` and `/skills diff` review. | <https://hermes-agent.nousresearch.com/docs/user-guide/features/skills> | 2026 (live docs, fetched 2026-09-05) | high |
| Ollama — install commands and first-run | `curl -fsSL https://ollama.com/install.sh \| sh` (macOS/Linux), `irm https://ollama.com/install.ps1 \| iex` (Windows), DMG/EXE downloads, official Docker image; first command `ollama run <model>`; OpenAI-compatible REST API; README advertises integrations with Claude Code, Copilot CLI and others. 180.2k stars on 2026-09-05. | <https://github.com/ollama/ollama> | 2026-09-05 (README as fetched) | high |
| Ollama — the install script does hardware detection and service setup | scripts/install.sh detects OS/arch (uname), WSL2, and GPUs via `check_gpu()` using lspci/lshw/nvidia-smi; installs CUDA drivers (apt/dnf/yum) or ROCm for AMD; `configure_systemd()` creates an `ollama` user and a systemd unit; ends with 'The Ollama API is now available at 127.0.0.1:11434' and 'Install complete. Run "ollama" from the command line.' Loopback bind by default. | <https://raw.githubusercontent.com/ollama/ollama/main/scripts/install.sh> | 2026-09-05 (main branch as fetched) | high |
| Ollama — Homebrew/winget distribution and measured install volume | Homebrew cask `ollama-app` (v0.33.3, auto-updates) shows 8,976 installs/30d, 27,554/90d, 84,532/365d. Windows: `winget install Ollama.Ollama` (per winstall listing). | <https://formulae.brew.sh/cask/ollama-app> | 2026-09-05 (live analytics) | high |
| Ollama — origin, license, scale | First release Jul 8 2023; founders Jeffrey Morgan and Michael Chiang; MIT; Go + llama.cpp; default port 11434. Wikipedia (citing TechCrunch) reports $65M raised and ~9M users as of Jul 2026. | <https://en.wikipedia.org/wiki/Ollama> | 2026 (fetched 2026-09-05) | medium |
| LM Studio — GUI-first local runtime with dual API and package-manager presence | Free desktop app by Element Labs; 0.4.23 (Aug 2026) on macOS 14+ Apple Silicon, Windows, Linux; bundles llama.cpp and MLX; serves an OpenAI- and Anthropic-compatible API on localhost:1234; `brew install --cask lm-studio`, `winget install ElementLabs.LMStudio`; Element Labs acquired Locally AI in Apr 2026 for native Apple apps. Regarded as the best local chat GUI on Windows. | <https://www.sitepoint.com/local-llms-are-getting-easier-the-complete-guide-2026/> | 2026 | medium |
| Cursor — scale at Series C | Cursor announced >$500M ARR and use by 'over half of the Fortune 500, including NVIDIA, Uber, and Adobe' (Jun 6 2025). The post does not explain growth mechanics. | <https://cursor.com/blog/series-c> | 2025-06-06 | high |
| Cursor — growth mechanics | $100M ARR 12 months after launch, $300M at 24, $500M at 30; ~100 employees (90% engineers); forked VS Code to own the UI; Truell: 'We had an overriding fear of focusing on anything other than making the product better'; adoption was word-of-mouth via developer networks and shareable screenshots of moments no plugin could do; no paid marketing/sales team per multiple secondary analyses. | <https://www.news.aakashg.com/p/how-cursor-grows> | 2025-07-03 (updated 2026-04-19) | medium |
| Claude Code — launch framing | Introduced Feb 24 2025 as 'a limited research preview' and 'our first agentic coding tool', accessed by joining the preview via docs; positioned as a terminal collaborator that reads/edits code, runs tests, commits and uses CLI tools 'while keeping you in the loop'. | <https://www.anthropic.com/news/claude-3-7-sonnet> | 2025-02-24 | high |
| Claude Code — distribution stack (native installer + every package manager + signing) | Native install `curl -fsSL https://claude.ai/install.sh \| bash`, `irm https://claude.ai/install.ps1 \| iex`, CMD variant; `brew install --cask claude-code` (stable) and `claude-code@latest`; `winget install Anthropic.ClaudeCode`; signed apt/dnf/apk repos with published GPG fingerprint; native installs auto-update in the background with 'latest'/'stable' channels and `minimumVersion` pins; binaries notarized (macOS) / Authenticode (Windows) with a signed SHA256 manifest; `claude doctor` diagnostics; npm package now installs the same native binary. Requires a paid Claude plan or API provider — no free tier. | <https://code.claude.com/docs/en/setup> | 2026 (live docs, fetched 2026-09-05; mentions v2.1.207/2.1.211) | high |
| ChatGPT — fastest consumer ramp and why | 100M MAU in Jan 2023, two months after launch (Similarweb; UBS: 'in 20 years following the internet space, we cannot recall a faster ramp'). Instagram took 2.5 years, TikTok 9 months. Cited drivers: search-box-simple UI, public launch with minimal barriers, iterative prompting, novelty-to-utility spread across writing/code. | <https://time.com/6253615/chatgpt-fastest-growing/> | 2023-02-08 | medium |
| iPhone — App Store launch numbers | 10M app downloads in the first three days after the Jul 10 2008 launch; 800+ native apps, 200+ free, 90%+ under $10; Jobs: 'a grand slam'. | <https://www.apple.com/newsroom/2008/07/14iPhone-App-Store-Downloads-Top-10-Million-in-First-Weekend/> | 2008-07-14 | high |
| iPhone — hardware + store launched together | 1M iPhone 3G units sold in the first weekend across 21 countries at $199/$299 on a two-year contract, shipping with iPhone 2.0 software and 'over 800 third party applications available through the new App Store' — the device and the distribution channel launched in the same week. | <https://www.apple.com/newsroom/2008/07/14Apple-Sells-One-Million-iPhone-3Gs-in-First-Weekend/> | 2008-07-14 | high |
| Poke — messenger-first onboarding with no app | Onboarding is poke.com → 'Get Started' → enter phone number; 'There's no app to install as the assistant operates over text messaging'; free to start with usage-based pricing for real-time features; channels iMessage, SMS, Telegram, WhatsApp (limited by Meta restrictions); $25M raised at $300M post; signups '10x'ed over the past couple of months'. | <https://techcrunch.com/2026/04/08/poke-makes-ai-agents-as-easy-as-sending-a-text/> | 2026-04-08 | high |
| Poke — Apple approved the first third-party AI agent inside Messages | Jun 4 2026: Poke became the first AI agent approved on Apple Messages for Business; Poke pays a per-user fee to its messaging service provider ('significantly lower than Meta AI, after it increased fees in response to EU regulation'); Apple takes no direct payment; Poke had relayed ~100M messages; 10-person team. | <https://techcrunch.com/2026/06/04/apple-approves-poke-as-the-first-ai-agent-on-its-messages-for-business-platform/> | 2026-06-04 | high |
| Telegram — reach | Durov announced 'significantly over 1 billion monthly active users' on Mar 19 2025 (up from 950M in Jul 2024), second messaging app worldwide excluding WeChat. (X post not fetched directly; multiple secondary confirmations.) | <https://x.com/durov/status/1902454590747902091> | 2025-03-19 | medium |
| Meta AI — distribution inside existing messengers | Meta AI scaled by living inside WhatsApp/Instagram/Facebook/Messenger rather than as a standalone app; aggregator sites report 1B MAU (Oct 2025) → 1.2B (Q1 2026) with ~63% of interactions on WhatsApp. WhatsApp itself >3B users. Secondary/aggregator figures only. | <https://www.demandsage.com/meta-ai-users/> | 2026 | low |
| Discord — reach for a bot channel | Discord's own figure is '200M+ MAU'; third-party trackers cite ~260M; ~28M servers and ~12M active bots (aggregator figures; Discord stopped publishing quarterly numbers after 2023). | <https://sqmagazine.co.uk/discord-statistics/> | 2026 | low |
| Skills ecosystems — Vercel skills.sh | Vercel launched the open agent-skills ecosystem on Jan 20 2026: `npx skills add <package>` (recommended start `npx skills add vercel-labs/agent-skills`), supporting 17 agents including claude-code, clawdbot, codex, cursor, gemini-cli, github-copilot, opencode, windsurf; skills.sh is 'a directory and leaderboard for skill packages' with install counts. | <https://vercel.com/changelog/introducing-skills-the-open-agent-skills-ecosystem> | 2026-01-20 | high |
| Skills ecosystems — open standard | Anthropic released Agent Skills (SKILL.md) as an open standard with spec and SDK on Dec 18 2025 (agentskills.io documents the format and compatible clients but does not catalog skills). Secondary sources report skills.sh listing ~669K skills by Jun 2026 with top skill 'find-skills' at 2.0M installs and Anthropic's 'frontend-design' at 531.8K. | <https://agenticskills.io/ai-skills-directories> | 2026-06 | low |
| Templates go viral — a 65-line CLAUDE.md | forrestchang/andrej-karpathy-skills (created Jan 27 2026) packages Karpathy's observed LLM-coding failure modes into four rules in one CLAUDE.md; ~91.2K stars on the personal repo plus >132K on the multica-ai mirror (>220K combined, TechTimes May 18 2026). Karpathy did not write or endorse it — a derivative template still became one of the fastest-starred repos of 2026. | <https://www.techtimes.com/articles/316798/20260518/karpathy-inspired-claudemd-passes-220000-combined-github-stars-four-rules-that-stop-ai-breaking.htm> | 2026-05-18 | medium |
| Show HN — rules that define a launchable artifact | Show HN is for 'things people can run on their computers or hold in their hands'; excluded: blog posts, sign-up pages, landing pages, fundraisers, minor version bumps, and anything requiring signup/email before trying; advice: remove barriers so users can test immediately, explain how and why, don't solicit upvotes. | <https://news.ycombinator.com/showhn.html> | n/a (HN guidelines, fetched 2026-09-05) | high |
| Product Hunt — saturation in 2026 | H1 2026: 3,869 launches, average 144 upvotes; 49% of launches carry an AI component (61% in Mar 2026); AI products earn ~29% more upvotes but 'an AI tool' is invisible — advice is to launch as the month's specific sub-flavor (e.g. autonomous agent that does a job), not generic AI. | <https://crawlora.net/blog/product-hunt-trends-2013-2026> | 2026 | low |
| GitHub Trending — velocity-ranked and gameable | Trending ranks on star velocity; May 2026 trending AI-agent repos held ~290K aggregate stars with >73K new in seven days; spikes can come from a viral post, paid promotion or star-farming and do not reflect adoption; HN users have built several alternative trending pages. | <https://aiforautomation.io/news/2026-05-13-ai-agents-github-trending-star-ranking-broken> | 2026-05-13 | low |
| Failure — Humane AI Pin (returns > sales) | Aug 7 2024 (The Verge via MacRumors): ~10,000 units shipped, ~7,000 still in customer hands, $9M total sales vs $1M returns, more returned than sold May–Aug; first-year plan was 100,000 units; returned units cannot be refurbished (cellular limitation) and become e-waste. | <https://www.macrumors.com/2024/08/07/humane-ai-pin-returns/> | 2024-08-07 | medium |
| Failure — Humane AI Pin (end state) | HP bought Humane's assets for $116M (Feb 18 2025); Pin functionality shut down Feb 28 2025; price cut $699 → $499 → $299; charging-case battery-fire warning; >$230M raised; company sought $750M–$1B in May 2024. Positioned as a smartphone replacement with a subscription on top of hardware. | <https://techcrunch.com/2025/02/18/humanes-ai-pin-is-dead-as-hp-buys-startups-assets-for-116m> | 2025-02-18 | high |
| Failure — Rabbit R1 (sold the demo, not the product) | Announced CES Jan 9 2024 ($10M pre-orders); ~130,000 units sold; by Sept 2024 CEO Jesse Lyu said ~5,000 of ~100,000 buyers used it daily; the 'large action model' promise underdelivered and the OS was shown running as an Android APK on a Pixel 6a; MKBHD called it 'barely reviewable'; rabbitOS 2 (Sept 2025) was a reset acknowledging it 'launched before it was ready'. | <https://en.wikipedia.org/wiki/Rabbit_r1> | 2026 (fetched 2026-09-05); 5,000-DAU figure from 9to5google 2024-09-26 | medium |
| Failure — Rewind → Limitless → Meta (always-on capture hits regulation) | Rewind pivoted in 2024 to the $99 Limitless Pendant; Meta acquired Limitless Dec 5 2025; the Rewind macOS app got a kill switch Dec 19 2025 disabling capture; pendant sales halted; service ended in EU, UK, Brazil, South Korea, Israel, Turkey due to consent/biometric rules; local-first architecture conflicted with the acquirer's model. | <https://winbuzzer.com/2025/12/05/meta-acquires-ai-wearables-startup-limitless-kills-pendant-sales-and-sunsets-rewind-app-xcxwbn/> | 2025-12-05 | medium |
| Failure — Dot by New Computer (companion-first, iOS-only) | Launched on iOS Jun 2024; shutdown announced Sep 5 2025, offline Oct 5 2025; official reason: co-founders' visions diverged; company claimed 'hundreds of thousands' of users but Appfigures counted 24,500 lifetime iOS downloads; no Android; context of intensifying AI-companion safety scrutiny. | <https://techcrunch.com/2025/09/05/personalized-ai-companion-app-dot-is-shutting-down/> | 2025-09-05 | high |
| Retention benchmarks for instrumentation | Cross-industry medians (AppsFlyer 2025, Adjust 2026, data.ai 2026, UXCam 37K apps): D1 25% / D7 8% / D30 4%; Productivity 30% / 15% / 8%; Social 40% / 18% / 12%. Users who complete a meaningful action in session one retain at 2–3x the rate — activation is the lever. No AI-agent category exists; productivity is the closest proxy. | <https://uxcam.com/blog/mobile-app-retention-benchmarks/> | 2024-12-22 (updated 2026-04-21) | medium |
| Nerva repo — what already exists for activation (read in-repo) | `install.sh`/`INSTALL.bat` = venv + deps + `scripts/install_smoke.py` (~30s real boot + /readyz + one chat turn); `GET /api/onboarding/command-center` returns install health, model truth, wizard state (intro/model/test_chat/autonomy/product_posture) and honest `first_actions` (say_hello → /chat, morning_brief → /autonomy/brief, index_docs → /api/local-docs/index) plus `starter_outcomes`; `POST /api/onboarding/funnel` records `funnel.<step>.<event>` locally; `GET /api/metrics/north-star` computes accepted actions, night-shift share, interrupt/reject rate, local %, p95 and a proposal funnel. Telegram uses a static `allowed_user_ids` list; H12.19 `SenderPairing` (pending/approve/block + optional rotating pairing code, rate-limited) exists but is default-off behind `JARVIS_CHANNEL_PAIRING`. No Homebrew cask, winget manifest, GHCR image or signed Tauri installer exists; Gate-2 🚧1 (one-step design-partner bootstrap) and 🚧4 (first-30-minutes zero-key loop) are open; GAP-0 (2026-09-01) records 24,182 visitors / 165 interactions / 0 partners converted. | </home/user/jarvis-hub/agents/core/routers/onboarding.py; /home/user/jarvis-hub/agents/core/channels/pairing.py; /home/user/jarvis-hub/docs/METRICS.md; /home/user/jarvis-hub/docs/meetings/2026-07-07-pre-go-live-sync.md; /home/user/jarvis-hub/BACKLOG.md#L670> | 2026-09-05 (repo HEAD) | high |
