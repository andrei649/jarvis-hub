# Model-job toolset restrictions

Goal: implement the bounded enabled_toolsets portion of H146/H449. Base: 5a8efc9e6e5d93f5736a6b904c51febae387a569. Generated 2026-09-15 UTC. Head: source commit containing this plan. Next action: independent exact-head review; parent integrates publication and global evidence.

## Contract and exclusions

Only model-bearing ask jobs accept options.enabled_toolsets. Missing/null retains existing default behavior; [] offers no tools; a bounded unique list of known installed IDs selects an upper bound, intersected with all existing posture/profile/approval rules. Unknown or incompletely registered groups refuse before model execution. No model workspace/skills, cross-profile aggregation, schedule.create, new authority, or full H146/H449 equivalence.

Explicit catalog: basic = echo/time; files = file_read/file_list/file_search/file_write/file_delete; terminal = terminal_run. Handler audit: coordinator echo/time are direct value/clock returns; file_tools register_file_tools routes direct bounded FileTools reads/mutations with existing preflight and trusted approval; terminal_run routes GovernedTargetRunner with durable task ID, default-off targets, existing root/target policy. No handler calls another ToolRPC tool. Registration availability does not promise backend health or override default-off policy. Exclude execute_code, delegation/desktop planning, image special intake, dynamic plugins, web and OSINT in this increment. No inferred names or wildcard categories.

## Interfaces and steps

1. New agents/core/job_toolsets.py: fixed immutable catalog, strict parsing, installed-member resolution, immutable allowed-name scope with shared revocation lifetime and parent intersections. Top-level absent/null creates no frame; nested null preserves parent restrictions/lifetime. Closed inherited frame refuses instead of resetting to defaults. Tests first: invalid shapes/IDs/duplicates, missing registration, []/null, concurrent scopes, nested revocation.
2. jobs.py validates action/options and resolves live orch.tool_rpc before _ask. Scope surrounds the actual process await. Tool-profile resolve_tools filters offers; ToolRPC.handle checks before preflight/ASK/handler. No change to approved execute payload or registry metadata. Tests first: real JobRunner→runtime→RPC offers and dispatch, hallucination/direct intake, cancellation/late child, no-tools text fallback, immutable saved approval after edits/restart, queued-job identity changes.
3. jobs_health.py reports catalog plus the selected tool-name upper bound (not a claim about effective contextual offers) and refusal diagnostics; CLI create/edit flag authors the real field with explicit complete-options replacement; HUD default/no-tools/selected checkboxes reads the installed catalog from existing doctor response. Saved unknown IDs remain visible/removable. Tests cover create/edit/clear/default, disabled groups, retained other options, actual request JSON and doctor. Native editor remains outside this increment; API accepts same field.
4. Run guarded focused backend tests, one-worker relevant frontend tests and TypeScript; whole Ruff/Bandit, manual scoped Graft freshness. No full suite, generated production bundle, global assessment/status or publication. Record exact counts and source head for independent review.

## Concurrency and approval

Frozen scope names never widen after registry/job edits; live dispatch registration still must exist. Parent revocation propagates through nested frames without sharing mutable allowlists. Every active model call's offered schemas and intake obey the same intersection. [] retains ordinary text generation. Intake checks before any side effect or approval creation. An already-created ASK task contains the existing frozen concrete tool/args only: execution retains its separate durable kernel approval, does not restore a request scope or reread current job options. Revocation stops new intake, not already approved operations. No cancellation claim for an already-started handler.

## Verification/rollback

RED/GREEN evidence:

- Initial 12 scope/schema tests failed on the absent module, then passed. Three intake/runtime/authoring cases exposed missing boundaries; subsequent real Agent tests verify both selected offers and [] text fallback. Added CLI/doctor RED cases and two HUD control RED cases; a real-client HUD case caught missing catalog propagation through JobCreateDialog. Three malformed runner-option RED cases caught early AttributeError and now preserve ValueError validation.
- Final guarded backend batch: 357 passed, one deselected known baseline test (27 new cases in test_job_toolsets.py). Command uses /tmp/nerva-run-isolated.py with product Python3.12, TMPDIR=/private/tmp, NERVA_PUBLIC_PROFILE=0, fish PATH, unchanged pytest.ini guards. Files: test_job_toolsets, test_job_model_pins, test_tool_rpc_h20_1, test_agent_runtime_v2, test_nerva_cli, test_owner_jobs, test_tool_profiles, test_orchestrator_bindings, test_job_dispatch, test_sandbox_tool_rpc_pipeline, test_tool_rpc_intake. Log/XML: /tmp/nerva-toolsets-focused.{log,xml}.
- The sole deselection is test_script_approval_reservation_outlives_manual_dispatch: reproduced alone on untouched base5a8 and candidate after22h, pending due to the second fixture runner's real quiet hours. Parent already owns the independent fixture fix in01f26553; no production behavior or test guard changed here. Baseline log /tmp/nerva-toolsets-dispatch-baseline.log.
- Frontend: 22 focused tests pass (3 new), jobs.test.tsx/job-toolsets.test.tsx/jobs-entry.test.tsx, one Vitest worker. All existing TypeScript configurations pass: tsconfig.json, tsconfig.e2e.json, dispatch-e2e/tsconfig.json. No API schema change: existing generic job options/doctor object response.
- Supplemental real Chromium HUD at1440×1000 and390×844: navigate /v2/jobs, select installed basic, verify files disabled, submit exact options.enabled_toolsets JSON, reload and verify fresh default draft. Job API mocked; unrelated local hub GETs were unavailable. No live channel/provider calls or native proof. Script/log /tmp/nerva-toolsets-browser.{cjs,log}; committed functional regressions remain the CI gate.
- Whole Ruff and baseline Bandit pass. Manual scoped Graft build/check covers13 owned source/test files,287 nodes, fresh; no deep/model pass, secrets/dependencies/hook installation or committed cache. No full suite or production bundle generated.

Doctor deliberately reports toolset_upper_bound, not effective offered tools. Existing posture, agent patterns, backend support, configuration and approval checks apply at execution. H146/H449 remain partial for broader accepted obligations.

 Owned paths: module, jobs.py/jobs_health.py, tool_profiles.py/tool_rpc.py, CLI, HUD jobs/job-builder/job-create-dialog and focused tests, this plan; only narrow runtime/test harness adjustments if demonstrated necessary. Roll back the source unit to restore prior default job behavior. Dependencies: existing Python/React/tool registry only; no package changes. Parent owns integration/global metadata.


## Integrated verification

Source b33ecee6997ec89ffb926116276908e89598690c integrates the two owned commits onto xAI final b02d3fe9 and includes the production HUD rebuild. Root independent review passed 337 guarded backend cases, including an external real two-job plus ordinary-caller concurrency probe against the same registry/runtime (/tmp/nerva-toolsets-independent.xml, .log). All 22 targeted frontend cases passed independently. Review corrected the UI's overly broad "No code execution" phrase: it now names the excluded dedicated code-runner tool group, because terminal execution retains its existing approved command capabilities.

After integration, 302 guarded cases passed across toolsets, xAI replay, continuation, ToolRPC and job dispatch (/tmp/nerva-toolsets-integrated-focused.xml, .log). The previously time-dependent dispatch fixture is included and passes with the independently reviewed base correction. Actual collection is 802 modules and 11976 backend cases (+27). Full frontend passed all 1329 cases (+3), with zero failures (/tmp/nerva-toolsets-full-frontend.json); three TypeScript configurations and the production Vite build passed (/tmp/nerva-toolsets-build.log). Whole Ruff and baseline Bandit1.9.4 passed (/tmp/nerva-toolsets-integrated-ruff.log, -bandit.log). Mobile140 and routes502 are unchanged.

The integrated full backend run passed: 11976 collected, 11952 passed, 23 skipped, one expected failure, zero unexpected failures/errors in 262.305 seconds (/tmp/nerva-toolsets-full.xml, .log). Root verified that tracked files stayed unchanged throughout the run. H146/H449 remain partial for model workdir, full skill attachment, cross-profile aggregation and the separately protected schedule.create grant. This bounded tool catalog does not claim native editing, live channel/provider validation, effective contextual tool offers in doctor, or arbitrary plugin/delegation/code-runner groups. Missing/null selects existing defaults; [] means no tools. Existing posture, backend support, configuration and human approvals remain authoritative.

Final scoped Graft check covers exactly 13 source/test files in /tmp/nerva-toolsets-scoped-graft; wiring is fresh (/tmp/nerva-toolsets-scoped-graft-build.log, -check.log), without semantic indexing or tracked cache changes. Final metadata validation is recorded in /tmp/nerva-toolsets-metadata.xml and .log.
