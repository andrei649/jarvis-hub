# Models and conversation context delivery

Generated 2026-09-15. Goal: H363/H364/H671/H673/H679 remaining request-shaping and context contracts. Base/head f75d7c32. Changed paths at planning: none. Next action: failing clock regressions.

Use existing checkpoint sessions table for conversation birth and request-local ContextVar for session cache identity. Agent.generate_response is the shared entry boundary; process and orchestrator streaming pass their resolved session. A missing identity remains unknown. Remove streaming's per-turn birthday reset. Cache keys are hashed and opt-in via explicit provider capability.

Provider vocabularies are model-specific and declared from reviewed vendor tables or operator overrides at construction; unknown schemas emit no extra fields. Gemini and compatible adapters normalize every request. Keep local-only routing unchanged. Ollama sends configured num_ctx and probes /api/show without assuming architectural context equals loaded context. Clamp compression to resolved ceilings, cap hard threshold at 85%, and reserve Gemini output.

Tests before implementation: checkpoint restart and duplicate-create birth; concurrent session request isolation and cleanup; cache capability off/on and different sessions; explicit empty/unknown reasoning vocabularies; Gemini wire low/high clamp; Ollama configured/probed/unknown ceilings; compression 85% and output reserve. Run focused existing suites, route/parity checks applicable to metadata. No live provider calls, spend, protected-root changes, or broad provider claims.

Rollback: independently reversible clock/request-context, provider shaping, and context budget commits. Dependencies: existing auth/guardrail binding and checkpoint store only; no migration needed. Hermes and backlog retain explicit residual vendor/live-service gaps.

Verification 2026-09-15: 454 focused tests passed (one upstream Starlette/httpx deprecation warning), including route/lifespan and concurrent-session guards. Ruff and git diff --check pass. status_sync reused JS counts and collected 10,599 backend tests; Hermes generated reports are in sync. Graft scoped wiring rebuilt and check passes; no deep/paid pass. Runtime implementation commits f59916a2, 7adee459, a90c839e. Next action: controller independent review. All five Hermes rows remain partial with precise residual limitations in docs/hermes/assessment.json; no live provider, model comprehension or billing claim.

Review repair plan 2026-09-15 (base/head 411bc5d1): reproduce the four independent failures before edits. Resolve implicit local current dates with the current local offset rather than the birth's fixed DST offset; retain explicit zones. Bind constructor capability declarations to immutable profile snapshots, reusing canonical vocabulary normalization/clamping, leaving the legacy registry available to its existing callers. Normalize effective soft/hard thresholds and keep the actual compression target below the trigger. Preserve protected head/tail. Add boundary, refresh, instance isolation and full-compaction tests; run focused suites, whole-tree Ruff and generated evidence checks. Rollback: one bounded review-repair commit. No changes to routing authority, live providers or protected roots.

Review repair verification: original independent repros produced 4 failures/1 pass; repaired and expanded cases cover both DST directions, reconnect deletion, Gemini suppression, mutable inputs, simultaneous compatible adapters, nested scope restoration and four compaction threshold pairs. 464 focused/runtime/route/lifespan tests passed (one upstream Starlette/httpx deprecation warning); whole-tree Ruff, git diff --check and Hermes check passed. status_sync collected 10,609 backend tests with JS counts reused. Scoped Graft wiring rebuilt and checked fresh. Evidence: /tmp/nerva-models-review-fixes-{focused,suite,ruff,status}.txt. No provider calls, publishing or integration. Next action: controller review of repairs.
