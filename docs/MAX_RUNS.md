# Max run ledger

> Append-only. One row per Max run (`MAX.md` §3.6). The **next** column is the boot
> instruction for the following run — keep it specific enough to start without archaeology.
> Run names come from the entropy table in `MAX.md` §6; never reuse one.

| # | date | run name | slice shipped | PR | spark | next |
|---|------|----------|---------------|----|-------|------|
| 000 | 2026-08-11 | *(bootstrap)* | The Max protocol itself: `MAX.md`, this ledger, `docs/SPARKS.md`, the `max` trigger skill, CLAUDE/AGENTS/BACKLOG wiring | #893 | S-001 (run-name entropy ritual) | Reconcile `BACKLOG.md` top-of-queue: first unblocked 1.0 proof-track item (H23 tail / ⭐B0 prep), per `MAX.md` §3.1a |
| 001 | 2026-08-11 | nimble-beacon | Three audit-theme fixes + a spark, delivered as **four owner-mandated successor PRs** (the original bundle #894 was split on an integrator HOLD): **SEC-B6** read-auth gate (#896), **ADV-087** reality-harness fix (#897), **chapter-15** hermetic evidence (#898), **Spark S-002** run-name tooling (this PR). Lesson recorded: `MAX.md` §3 says *one run = one shippable slice* — bundling four slices into one PR violated the repo's bounded-slice discipline; the split restored it. | #896–#899 | S-002 (`scripts/max_run_name.py`) | Land the four successors green; then per §3.1b: the SEC-B6 export/purge-list-drift gap, or the SEC-B5 dataflow-taint tail (mind the open `nerva2/sec-b5*` draft PRs) |
| 002 | 2026-08-12 | copper-nectar | Locked the **forget/export erasure invariant**: nothing an export names (`EXPORT_DBS`/`EXPORT_JSON`) may sit on the forget KEEP list, so deleting your data erases everything an export could reveal. New `tests/test_forget_export_purge_parity.py` (holds today + has teeth) + a pointer comment in `data_purge.py`. Closes the export/purge-drift gap that was run 001's next-pointer. *(Ledger row deferred one PR: `MAX_RUNS.md` was mid-flight in #899 when #900 shipped, so this row is the catch-up.)* | #900 | — | Owner-gated 1.0 proof track (⭐B0/RTX) stays the critical path; agent-side per §3.1b–c: continue chapter-15 hermetic verification of the other no-hardware CLOSED probes (§15.3 skill-signing / §15.6 honesty-badge / §15.8 reality), or a stranger's-first-ten-minutes onboarding slice (§3.1c). Avoid the open `nerva2/sec-b5*` PRs (locked). |
| 003 | 2026-08-12 | midnight-quill | Recorded the chapter-15 section 15.3 signing-gate verdict on current `main`: ADV-035/036 are **FIXED-SINCE**, keyed refusal (ADV-037) is confirmed, and the independent reproduction keeps ADV-038's default unsigned in-process import explicitly **OPEN**. Corrected the manual's observed route/case-count drift (409 to 410; 2,853 to 2,854). | #902 | - | Close ADV-038 at the imported/user-skill execution boundary without breaking bundled skills: separate repository-trusted code from owner/imported code, preserve visibility/quarantine, and prove no untrusted module executes in-process at the shipped default. Avoid #876's locked roadmap files. |
| 004 | 2026-08-12 | bold-dune | Closed ADV-038 at the external skill execution boundary: unsigned owner-home, marketplace, and imported-sidecar modules remain visible but cannot import in-process at the shipped default. Keyed external skills and content-bound owner approvals still activate; marketplace packages cannot smuggle approval, and post-approval edits return to quarantine. Bundled skills are unchanged. | #903 | - | Land #903 only after every Linux and Windows gate is green; then take the next unowned chapter-15 residue, preferably G24/ADV-087 reality-harness implementation resolution or G27/ADV-098 enforced reality-case coverage. Avoid #876's locked roadmap files. |
| 005 | 2026-08-12 | lucid-harbor | Closed the chapter-15 reality-proof residue: the readiness matrix now computes `PENDING_VERIFY` from declared reality bindings and rejects missing, duplicate, mismatched, or non-promotable cases. Current measurement is 94 records, 93 proof-eligible, 133 cases, and zero gaps. Re-baselined ADV-087/G24 to the already-shipped #897 fix and corrected its stale probe output. | #904 | - | Land #904 only after every Linux and Windows gate is green; then investigate G35/ADV-131 end to end: enumerate every private ingestion/archive write root and enforce parity across purge, retention, and export. Avoid #876's locked roadmap files. |
| 006 | 2026-08-13 | quiet-orbit | Closed G35/ADV-131: Howard's raw `ingestion/` drop and derived `archive/` share one canonical export, retention and forget inventory. Export is recursive, scheduled retention clears both live pipelines, forget clears RAG/raw-text keys, symlink boundaries fail safely, and pre-G35 repo-local imports stay discoverable while export/forget report them incomplete pending owner resolution. | #907 | — | Land #907 only after exact-head CI, independent privacy fix-verification and a separate R3 integrator are green; then investigate G36/ADV-132 end to end: enumerate every MCP RPC tool, classify governed versus ungoverned execution, and close any action-kernel bypass. Avoid #876's locked roadmap files. |
| 007 | 2026-08-13 | bold-quill | G36/ADV-132 candidate plus owner-escalated HOLD remediation: MCP RPC inventories transcript and route state honestly; hidden skills are refused; route mutation is identity/contract/kernel/audit fail-closed. The remaining `ask_*` local-model fast-path is now restricted to identity-verified `ask_jarvis` and a shared `host.control` boundary (system-control → contract → kernel `GRANT` → durable audit) for LM Studio and Ollama. Offline load authorizes/audits start and load as separate effects; controllers refuse implicit auto-start; Ollama inventory failure cannot masquerade as successful unload or empty residency. Fixed argv/API controllers preserve local-first operation and zero-effect denial evidence. | #908 | — | Obtain a **fresh distinct** independent security review on the new exact head, then a separate R3 integrator; do not reuse the exhausted two-round reviewer or merge as builder. After #908 lands, exercise G37/ADV-133 on a verified safe copy of a populated data root. If unavailable, take G38/ADV-130's bounded orchestrator-attribute residue. Avoid #876's locked roadmap files. |
| 008 | 2026-08-14 | copper-lantern | Closed G38/ADV-130: the 15 orchestrator slots populated by plugin, web, autonomy, ambient and scheduler lifecycle owners now exist explicitly before wiring and share one typed writer API, a runtime-checkable structural protocol, and an exact lexical callsite inventory. Hostile AST fixtures reject undeclared writes through typed, aliased, nested, direct/imported `setattr`, and direct/imported `object.__setattr__` forms; tuple-destructured, tuple-subscript, module and literal-`getattr` binding aliases are tracked while dynamic binding-API `getattr` fails closed. Statement-ordered reaching bindings retain calls before later reassignment, conservatively join `if` expressions, `while` and `try` paths, propagate function-default and loop-target aliases, include literal `getattr(builtins, "setattr")`, and stop treating a scope-local receiver as an orchestrator after replacement. Binding calls are matched bidirectionally by path, binding, line and column; unrelated imports, duplicate locations, and same-named lexical shadows cannot satisfy evidence. This is a CI lexical contract, not a proof against arbitrary dynamic Python. Runtime routing and authority behavior are unchanged. | #910 (post-merge repair for #909) | — | G37/ADV-133 remains owner-only because it needs a destructive migration exercise on a safe populated-data copy. Agent-side next: reconcile the stale G29/SEC-B3 chapter-15 PARTIAL entry against the already-shipped Telegram allowlist, owner binding and fail-closed pairing evidence; then select the next unowned PARTIAL. |
| 009 | 2026-08-14 | quiet-gale | SEC-B8 candidate moves external-skill approval out of the candidate tree: linked/junction provenance fails closed; owner approval binds canonical path plus every regular artifact's relative path, type and bytes; decision-time registry reload and shared process/file locking prevent stale or lost authority; and in-process execution plus relative artifacts use the validated materialized snapshot. Candidate markers and unkeyed signatures cannot grant trust. Keyed and bundled controls remain unchanged. | #911 | — | Hold as draft until fresh exact-head security review and hosted CI pass. Approval rows/lock files have no prune/revoke lifecycle yet; retained rows cannot authorize missing or changed bytes, but explicit privacy cleanup remains separate. After integration, rebase the B2/#856 fail-closed verifier repair and keep #906 external authority blocking. |
| 010 | 2026-08-16 | jade-orbit | Repaired the #906 external acceptance-state core after adversarial R3 review: every delivery now binds the repository-scoped review ID, exact subject, reviewer and monotonic revision; rejected first observations cannot be rebound; revocation and identity conflicts invalidate accepted replay/verdicts; distinct approvals remain independently live; malformed/non-built-in/oversized state and primitives fail closed; and a write that fills capacity cannot briefly return acceptance. This remains a candidate-independent library with no GitHub, credential, check, ruleset or merge capability. | #916 | — | Publish one immutable repaired #916 head, rerun hosted Python 3.12 gates and obtain fresh distinct exact-head R3 acceptance. Keep #906 blocking until the owner provisions a non-admin builder credential, distinct integrator/App, external store/hosting/keys, additive App-ID-pinned no-bypass PR ruleset and hostile rollback drills; #912 remains draft/HOLD. |
| 011 | 2026-08-16 | lucid-cipher | Completed the B7 task-persisted Ultron mediation candidate: finalized task bytes and the actual kernel tier are sealed beside a signed effective policy tier; mediation provenance cannot be downgraded through mutable kind; event time cannot regress; an external monotonic head plus atomic SQLite enqueue/claim validation rejects rollback, replay, substitution, tamper and ambiguous writes; the worker requires a private claim context; verified events drive counters; legacy rows quarantine; hold is the proof-safe rollback; and canonical `agents.core.*` identity is enforced. The surface remains default-off and adds no Night Shift or authority floor. | #912 | — | Obtain fresh exact-head R3 review and terminal Linux/Windows hosted gates. Keep #912 draft/HOLD until #906's technically distinct live integration authority is activated; any #906 service successor must be a separate rollback unit. |
| 012 | 2026-08-17 | feral-quill | Repaired the B7 authority candidate after #912 merged under a final R3 BLOCK: every enforce/hold dispatch now authenticates the global mediation head before the direct path; raw executors cannot bypass that gate; a one-use permit binds and revalidates the complete immutable persisted authority/routing tuple; refusal cannot be recorded as success; and the exact guarded handler input is reconstructed as a trusted plain task so async mutation and hostile copy hooks cannot substitute execution bytes. Default-off compatibility, routes and authority floors are unchanged. | #918 | — | Hold #918 as draft until exact-head Linux/Windows CI, a fresh distinct R3 PASS and separate integration are complete; keep #906 live distinct-authority activation open. After this corrective B7 slice is accepted, take G29/SEC-B3 chapter-15 reconciliation; do not start a B7-dependent feature first. |
| 013 | 2026-08-20 | quiet-quill | **The no-telemetry gate** (finishes half of 0.22). `PRIVACY.md`'s promise had no enforcement: pytest-socket is test hygiene, not a product proof, and best-effort call sites swallow a blocked connect. `tests/test_no_telemetry_proof.py` now records non-loopback egress across TCP connect, connected **and unconnected UDP**, and raw sends, plus network-capable subprocess spawns — while booting the real lifespan, holding a real `/chat` turn, and shutting down. Measured: **zero**. **Round-1 independent review rejected v1** for a reproducible false negative (connect-only spy, boot-only exercise); v2 adds regressions that fail against the old approach, was red-proofed against a UDP beacon planted on `/api/status`, and **narrows every doc/backlog claim to what is actually measured** (in-process only; the static half is an explicit known-vendor ratchet). Lesson: a privacy gate that over-claims manufactures false assurance — worse than none. | #939 | — | Finish 0.22's last item: **uninstall** (a provable removal path — the mirror of the forget/export invariant). Owner-gated ⭐B0/RTX remains the 1.0 critical path. |
| 014 | 2026-08-28 | amber-ember | **Backlog Zero run 1 — Phase 0 inventory + 0.22's last tail.** Swept the full `BACKLOG.md` (2938 lines) into `docs/BACKLOG_ZERO_LEDGER.md`: 97 open rows bucketed AI-EXEC/OWNER-CONFIG/OWNER-DECISION/R3-REVIEW-GATED with a terminal-state target each, ordered per `MAX.md` §3.1. Then shipped this run's own top pointer: **T-0.22 uninstall**, closing the theme. `agents/core/uninstall.py` mirrors the `data_purge.py` forget/export invariant for the *software* side — a single `UNINSTALL_TARGETS` tuple removes exactly what `install.sh`/`INSTALL.bat` create (`.venv/`, WorldView's `node_modules/` + generated env files), never the data root unless `--purge-data` explicitly bridges to the already-audited `data_purge.purge_data` (run **before** target removal, so a failed backup verification — or the venv's own dependencies going missing mid-run — can't strand the caller). `uninstall.sh`/`UNINSTALL.bat` wrappers + a `deploy/systemd/README.md` Uninstall section (parity with the Windows NSSM doc, which already had one) + `tests/test_uninstall.py` (+10, incl. an installer-text drift guard). *Branch note: this run developed on the harness-pinned `claude/nerva-backlog-zero-0k83jc` rather than a fresh `max/<run-name>` branch — see the PR body.* | #971 | — | Ledger's next AI-EXEC pick (top 5, `docs/BACKLOG_ZERO_LEDGER.md` Tier 1): **A8-iv** — redesign the `ungoverned_actions==0` live counter per `docs/superpowers/plans/2026-08-02-qa4-ungoverned-counter-park.md` (persist the kernel decision at `govern_enqueue`, read it at the worker seam). A1/A8/H23.23/AUD-0 rank higher but are OWNER-PACKET targets (RTX hardware / owner ratification) — advance their packets in `docs/OWNER_TASKS.md` alongside, don't block on them. |
| 015 | 2026-08-28 | lucid-orbit | **Backlog Zero run 2 — recount caught a stale next-pointer, then closed the honesty gap it left behind.** Row 014's next-pointer (A8-iv) turned out **already shipped**: `#946` ("authenticate QA4 intake evidence", merged 2026-08-26, *before* run 014's own ledger-generation timestamp) had already landed the exact persisted-stamp redesign the parked plan called for — `AutonomyWorker.govern_enqueue` stamps HMAC-signed intake evidence at intake, the worker seam re-verifies it before execution, `KernelMetrics.record_ungoverned` tallies a breach on any missing/invalid stamp (12 tests in `tests/test_qa4_ungoverned_counter.py`, all green pre-existing). Run 014's Phase 0 sweep missed this because it trusted `BACKLOG.md`'s stale "Still open: A8-iv" prose instead of grepping the code. What was genuinely still missing per the park doc: the two "sound and reusable" snapshot fields, `enabled` and a scalar `ungoverned_actions` total — without `enabled`, a `0` reading can't be told apart from "the kernel was never on". Shipped both in `agents/core/kernel/metrics.py` (red-proved first: `KeyError: 'enabled'` / `'ungoverned_actions'`, then green), corrected `BACKLOG.md` line 1066 and closed the `A8-iv` + `T-0.22` ledger rows (the latter was already shipped by run 014's own #971 but the ledger row was never marked closed). Verification: targeted (`test_kernel_metrics.py` + `test_qa4_ungoverned_counter.py`) green, adjacent kernel/action-auth/autonomy/house/north-star-guardrails/audit-gates sweep green (no full-suite run — this is an additive, backward-compatible dict-key change on one observability endpoint with no route/OpenAPI-schema/frontend consumer), ruff + py_compile clean on touched files, `scripts/status_sync.py --reuse-js-counts` refreshed the generated backend test count (6,978→6,980) across `project-status.json`/`README.md`/`NERVA.md`/`GO_LIVE_PLAN.md`/`STATUS.md`. **Per this session's explicit instruction, worked directly on `main`-tracking branch `claude/nerva-backlog-zero-f161fe` with no PR** — commit only, no draft/review cycle; treat as provisional until an owner/reviewer looks at it, same caution as any unreviewed change to the kernel-metrics surface. **Same run, second slice — cleared the ledger's three Tier-3 `NEEDS-RECOUNT` rows** with code-grep verification (no fresh implementation, per this row's own prior next-pointer): **K4-HUD** confirmed done (`gap.tsx`'s `KillSwitchPanel` one-tap HALT-ALL/disengage already exists, matching H23.3 — K4's stale "Pending" clause corrected); **H20.R1-RESIDUAL** narrowed, not closed (a repo-wide `register_tool(` grep found ORIZONT 28 wired one new tool into the model-directed loop since this row was written — `desktop_run`, OS-level click/type/launch via H28.4's `WindowsDesktopDriver` — but GovernedBrowser/Playwright browser control, file/process tools, and the rest of the "still open" list remain genuinely unwired); **BUG-2b.2** reclassified AI-EXEC→OWNER-DECISION (it was never an ambiguous test gap — `H10.2`/`H10.7`, its ride-along deps, shipped backend-only, and no drag-drop SVG canvas component exists anywhere in `frontend/src` to write pointer-event tests against; a build-vs-drop packet is now in `docs/OWNER_TASKS.md`'s Parking lot). All three corrected in `BACKLOG.md` + the ledger. | *(local commits, no PR — see branch `claude/nerva-backlog-zero-f161fe`)* | — | `refused_unmediated` is deliberately still not on `KERNEL_METRICS` — it lives in the separate B7 `TaskQueue.verified_mediation_stats()` ledger and has no route yet; wiring it into `/api/metrics/kernel` (or deciding the two counters should stay separate) is a small, well-scoped next AI-EXEC slice. **Same run, third phase — a 7-agent parallel recount swept all remaining Tier 4/6 AI-EXEC rows (26 items)** to test whether the "several may already be shipped" hypothesis held at scale. It didn't, mostly: **6 CLOSED** (T-0.45 contract-template adoption, T-0.51 via H32.3, GAP-1/6/7/8 — all doc-stale, real code/PRs already existed), **2 PARTIAL** (T-0.28 voice consent+barge-in built but not HUD-visible, GAP-9 doc-half only), and **18 confirmed genuinely STILL OPEN** with fresh file:line evidence each (T-0.20, T-0.29, T-0.37, T-0.41, T-0.49, T-0.50, T-0.53, T-0.58, H5.16, H18.24, H18.25, WV-170, H34.3, AUD-13, AUD-14, AUD-15, AUD-18, plus **E731-CONTINUITY-IDENTITY reclassified AI-EXEC→OWNER-DECISION** since its own reconciliation doc says the fix is an owner-scoping call, not a build). Net lesson revised: roughly 1-in-4 stale-but-shipped, not "most" — recounting is still worth doing before every pick, but don't assume a row is secretly done without checking. All 8 BACKLOG.md rows needing a text correction were updated (6 closed + 2 partial), the ledger's Tier 4/6 tables were rewritten with per-row recount evidence, a Tier-6 reading-note was added to stop future agents misreading the "terminal-state target" column as a completion claim, and an E731 packet was added to `docs/OWNER_TASKS.md`. Verification: `test_release_gate.py`/`test_status_sync.py` green, `scripts/status_sync.py --check` in_sync, workflow policy check passed (doc-only diff, no code touched, no targeted pytest run beyond the drift gates). | *(local commit, no PR)* | — | **Next AI-EXEC picks, now genuinely verified open** (smallest first): **H34.3** (dev-swarm PR/CI feed — `oracle_bridge.py` already polls GitHub, just needs `/pulls`+check-run calls added next to the existing commit-poll) or **WV-170** (add a real-Neo4j CI job/test, infra-only) are the cheapest confirmed-open slices; **T-0.53** (wire `design_manifest.py` to a route+HUD panel, Figma sync can stay a follow-up) and **T-0.20** (vault router/HUD/export wiring) are the next size up. AUD-13/14/15/18 are large, genuinely open, explicitly post-1.0/P2 — don't start those as a single-session slice. |
| 015b | 2026-08-28 | lucid-orbit (cont.) | **Same run, fourth phase — H34.3 (dev-swarm PR/CI feed), the cheapest confirmed-genuinely-open item from the recount sweep.** `OracleBridgePlugin` gained `_refresh_pr_feed()`: lists up to 10 open PRs (most-recently-updated first) + each one's check-run tally via GitHub's Checks API, strictly gated on an explicit `github_token` (an unauthenticated call is capped at 60/hr — far below what this needs — so no token means an honest disabled state, not silent rate-limiting), refreshed on a bounded 120s cadence piggybacked on the existing 30s commit-poll watcher tick (never a live call on the request path — `build_swarm_summary` reads the plugin's already-cached snapshot). Surfaced at the pre-existing `GET /api/oracle/status` (free — just added to `status()`) and a new `pr_feed` key on `GET /api/swarm/summary`; `mission_control.html` gained a new "PR / CI" panel next to Dev Swarm rendering a colored check-state dot, draft badge, and pass/fail/pending summary per PR, verified visually in-browser across all three states (green/red/gray) via injected fixture data since no live GitHub token exists in this environment. Test-first: `tests/test_oracle_pr_feed.py` (+9, red on missing `_refresh_pr_feed` first) + `tests/test_swarm_summary.py` (+3). Verification: targeted + adjacent (oracle/swarm/route-parity) sweep green, ruff + py_compile clean, `scripts/status_sync.py` resynced both the test count and the H34 horizon done/open counters, `check_ai_workflow_policy.py` passed. `BACKLOG.md` H34.3 checked off; ORIZONT 34 is now fully delivered. | *(local commit, no PR)* | — | The recount sweep's other cheap confirmed-open picks remain available: **WV-170** (real-Neo4j CI job, infra-only — no code change, just a docker-compose-backed CI lane) or **T-0.53** (wire `design_manifest.py` to a route + HUD panel; Figma token sync is a separate owner-gated follow-up, don't block on it). |
| 015c | 2026-08-28 | lucid-orbit (cont.) | **Same run, fifth phase — T-0.53 (design manifest route + HUD panel).** `GET /api/design-manifest` (new router, open like the sibling meters — design tokens aren't personal data) serves the pre-existing `design_manifest.build_manifest()` live; Console → Observe gained `DesignManifestPanel` (source filename, base-token/variant/component counts, variant chips), verified in the real frontend test suite (551 passed, +2), a clean `tsc --noEmit`, and a clean production HUD build (rebuilt `agents/web/v2/` bundle included in this commit, matching this repo's existing checked-in-build-output convention). Backend: `tests/test_design_manifest.py` (+3 route/endpoint tests, red-proved first via `AttributeError`-shaped absence). New-route bookkeeping done in the same slice: route-surface/OpenAPI/route-auth snapshots reseeded (406 routes, `GET /api/design-manifest` added to `INTENTIONALLY_OPEN_READS` with its substance-of-handler reason), `scripts/status_sync.py` resynced backend/frontend counts and the route count across all five generated docs. Figma token sync (the row's other half) stays explicitly owner-gated — needs a provisioned Figma API token, not deferred laziness. *(Toolchain note for future runs in this worktree: `frontend/node_modules` and `mobile/node_modules` had to be junctioned to the main checkout's — `New-Item -ItemType Junction` — since this worktree ships no `node_modules` of its own; lockfiles matched byte-for-byte so this is safe. `mobile`'s `npx jest` still resolves an npx-fetched jest instead of the junctioned one for an unknown reason — worked around by hand-editing `project-status.json`'s tracked frontend count before `--reuse-js-counts` rather than sinking more time into a mobile toolchain nothing in this slice touched.)* | *(local commit, no PR)* | — | Recount-sweep picks still open and cheap: **WV-170** (real-Neo4j CI job, infra-only) is next; **T-0.20** (vault router/HUD/export wiring) is the next size up after that. Diminishing-returns checkpoint: this run has now shipped 5 slices (A8-iv, 3-row recount, 26-row recount, H34.3, T-0.53) in one sitting — a future session should re-run the Phase-0 recount rule against `main` before trusting this ledger's remaining "STILL OPEN" verdicts, since they age the same way the ones this run corrected did. |
| 015d | 2026-08-28 | lucid-orbit (cont.) | **Same run, sixth phase — WV-170 (real-Neo4j property-search validation), honesty-tested against local infra limits.** Wrote `tests/test_neo4j_live_property_search.py` (4 cases: property-not-name match — the exact WV-170 ask — plus name match, relation round-trip, case-insensitivity), gated `JARVIS_NEO4J_LIVE=1` so it's a clean no-op skip in the default suite. Added a `neo4j-live` job to `.github/workflows/reality.yml` with a `neo4j:5` service container (`NEO4J_AUTH: none`, matching `docker-compose.yml`'s existing runtime config), same schedule-only/dispatch-only boundary as the existing `reality` job — never on the PR path, mirroring `worldview.yml`'s already-proven TimescaleDB/Redis service-container pattern. **Attempted local verification and could not complete it**: Docker Desktop's daemon would not come up after two launch attempts and ~6 minutes of waiting (`docker info` itself eventually stopped responding within a bounded 15s check) — logged honestly in `BACKLOG.md`/the ledger rather than claimed as verified. The test and CI job are code-complete and match a pattern this repo has already proven works (worldview.yml's own service-container job); first real execution will be on GitHub Actions. | *(local commit, no PR)* | — | If a future session has working Docker locally, run `docker compose up -d neo4j && JARVIS_NEO4J_LIVE=1 python -m pytest tests/test_neo4j_live_property_search.py -v` once to close the loop, then update this row from "pending first live CI run" to genuinely verified. |
| 015e | 2026-08-28 | lucid-orbit (cont.) | **Same run, seventh phase — T-0.20 (vault router + HUD panel + export/forget wiring), the largest slice of this run.** `agents/core/vault.py` (encrypted blob vault: crypto, atomic commits, quotas, cross-process locking) had zero live callers despite being fully built and tested since an earlier session. Closed all three remaining gaps: **forget** needed no code — verified the vault root was never on `data_purge.KEEP_DIRS` (the AUDIT-2 KEEP-inverted sweep already erases it), pinned by a new regression test instead of just asserting it; **export** — `data_export._dump_vault()` decrypts and embeds every item (a raw ciphertext copy would defeat the "readable, portable" export contract), TDD'd red-first (`KeyError: 'vault'`) then green, with a test-authoring bug of its own caught and fixed along the way (constructing the test vault with an explicit passphrase while `_dump_vault` opens the default no-passphrase mode — different key-derivation paths, so the second instance couldn't decrypt the first's ciphertext until the test matched production's actual construction); **router** — new `agents/core/routers/vault.py` (`GET/POST /api/vault`, `GET/DELETE /api/vault/{id}`, user-guarded like notes/memory since content is meant to be retrieved by its owner, unlike `secrets.py`'s admin-only never-comes-back-out broker), async-offloaded (`asyncio.to_thread`) per this repo's established off-event-loop discipline, with an HTTP-layer 25 MiB cap deliberately far below the vault's own generous 1 GiB internal ceiling; **HUD** — Console → Memory gained `VaultPanel` (store text via textarea or files via a picker, list without ever showing content, explicit per-item download as a real Blob, delete) — the first file-upload/download UI pattern in this HUD, verified against all 5 new frontend tests plus the full 556-test suite, a clean `tsc --noEmit`, and a clean production build. New-route bookkeeping: route-surface/OpenAPI/route-auth snapshots reseeded (414 routes; the 4 new routes correctly classified `user`, not `open`, in the auth-matrix snapshot). Verification: `tests/test_vault_router.py` (+9), `tests/test_data_export.py` (+4), `frontend/src/test/vault-panel.test.tsx` (+5), adjacent `test_forget_export_purge_parity.py`/`test_data_purge.py` sweep green, ruff/bandit/py_compile clean, `scripts/status_sync.py` resynced backend (7,011) and route counts. | *(local commit, no PR)* | — | This is the eighth distinct slice shipped in one sitting on this run — a genuinely unusual amount of throughput for one session. Before this ledger's remaining rows (T-0.29, T-0.37, T-0.41, T-0.49, T-0.50, T-0.58, H5.16, H18.24/25, AUD-13/14/15/18) are picked up next, a fresh session should re-verify none of them were touched by a parallel `opencode`/other-agent session in the meantime — `git worktree list` in this run showed several other active worktrees on this same repo (`fix/qa4-authenticated-evidence`, `fix/secb4-*`, `wave/*` branches), confirming other agents are actively shipping to this repo outside this session's view. |
