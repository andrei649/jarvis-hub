# Max run ledger

> Append-only. One row per Max run (`MAX.md` §3.6). The **next** column is the boot
> instruction for the following run — keep it specific enough to start without archaeology.
> Run names come from the entropy table in `MAX.md` §6; never reuse one.

| # | date | run name | slice shipped | PR | spark | next |
|---|------|----------|---------------|----|-------|------|
| 000 | 2026-08-11 | *(bootstrap)* | The Max protocol itself: `MAX.md`, this ledger, `docs/SPARKS.md`, the `max` trigger skill, CLAUDE/AGENTS/BACKLOG wiring | #893 | S-001 (run-name entropy ritual) | Reconcile `BACKLOG.md` top-of-queue: first unblocked 1.0 proof-track item (H23 tail / ⭐B0 prep), per `MAX.md` §3.1a |
| 001 | 2026-08-11 | nimble-beacon | Three audit-theme fixes + a spark, delivered as **four owner-mandated successor PRs** (the original bundle #894 was split on an integrator HOLD): **SEC-B6** read-auth gate (#896), **ADV-087** reality-harness fix (#897), **chapter-15** hermetic evidence (#898), **Spark S-002** run-name tooling (this PR). Lesson recorded: `MAX.md` §3 says *one run = one shippable slice* — bundling four slices into one PR violated the repo's bounded-slice discipline; the split restored it. | #896–#899 | S-002 (`scripts/max_run_name.py`) | Land the four successors green; then per §3.1b: the SEC-B6 export/purge-list-drift gap, or the SEC-B5 dataflow-taint tail (mind the open `nerva2/sec-b5*` draft PRs) |
| 002 | 2026-08-12 | copper-nectar | Locked the **forget/export erasure invariant**: nothing an export names (`EXPORT_DBS`/`EXPORT_JSON`) may sit on the forget KEEP list, so deleting your data erases everything an export could reveal. New `tests/test_forget_export_purge_parity.py` (holds today + has teeth) + a pointer comment in `data_purge.py`. Closes the export/purge-drift gap that was run 001's next-pointer. *(Ledger row deferred one PR: `MAX_RUNS.md` was mid-flight in #899 when #900 shipped, so this row is the catch-up.)* | #900 | — | Owner-gated 1.0 proof track (⭐B0/RTX) stays the critical path; agent-side per §3.1b–c: continue chapter-15 hermetic verification of the other no-hardware CLOSED probes (§15.3 skill-signing / §15.6 honesty-badge / §15.8 reality), or a stranger's-first-ten-minutes onboarding slice (§3.1c). Avoid the open `nerva2/sec-b5*` PRs (locked). |
| 003 | 2026-08-12 | midnight-quill | Recorded the chapter-15 section 15.3 signing-gate verdict on current `main`: ADV-035/036 are **FIXED-SINCE**, keyed refusal (ADV-037) is confirmed, and the independent reproduction keeps ADV-038's default unsigned in-process import explicitly **OPEN**. Corrected the manual's observed route/case-count drift (409 to 410; 2,853 to 2,854). | #902 | - | Close ADV-038 at the imported/user-skill execution boundary without breaking bundled skills: separate repository-trusted code from owner/imported code, preserve visibility/quarantine, and prove no untrusted module executes in-process at the shipped default. Avoid #876's locked roadmap files. |
| 004 | 2026-08-12 | bold-dune | Closed ADV-038 at the external skill execution boundary: unsigned owner-home, marketplace, and imported-sidecar modules remain visible but cannot import in-process at the shipped default. Keyed external skills and content-bound owner approvals still activate; marketplace packages cannot smuggle approval, and post-approval edits return to quarantine. Bundled skills are unchanged. | #903 | - | Land #903 only after every Linux and Windows gate is green; then take the next unowned chapter-15 residue, preferably G24/ADV-087 reality-harness implementation resolution or G27/ADV-098 enforced reality-case coverage. Avoid #876's locked roadmap files. |
| 005 | 2026-08-12 | lucid-harbor | Closed the chapter-15 reality-proof residue: the readiness matrix now computes `PENDING_VERIFY` from declared reality bindings and rejects missing, duplicate, mismatched, or non-promotable cases. Current measurement is 94 records, 93 proof-eligible, 133 cases, and zero gaps. Re-baselined ADV-087/G24 to the already-shipped #897 fix and corrected its stale probe output. | #904 | - | Land #904 only after every Linux and Windows gate is green; then investigate G35/ADV-131 end to end: enumerate every private ingestion/archive write root and enforce parity across purge, retention, and export. Avoid #876's locked roadmap files. |
| 006 | 2026-08-13 | quiet-orbit | Closed G35/ADV-131: Howard's raw `ingestion/` drop and derived `archive/` share one canonical export, retention and forget inventory. Export is recursive, scheduled retention clears both live pipelines, forget clears RAG/raw-text keys, symlink boundaries fail safely, and pre-G35 repo-local imports stay discoverable while export/forget report them incomplete pending owner resolution. | #907 | — | Land #907 only after exact-head CI, independent privacy fix-verification and a separate R3 integrator are green; then investigate G36/ADV-132 end to end: enumerate every MCP RPC tool, classify governed versus ungoverned execution, and close any action-kernel bypass. Avoid #876's locked roadmap files. |
| 007 | 2026-08-13 | bold-quill | G36/ADV-132 candidate plus owner-escalated HOLD remediation: MCP RPC inventories transcript and route state honestly; hidden skills are refused; route mutation is identity/contract/kernel/audit fail-closed. The remaining `ask_*` local-model fast-path is now restricted to identity-verified `ask_jarvis` and a shared `host.control` boundary (system-control → contract → kernel `GRANT` → durable audit) for LM Studio and Ollama. Offline load authorizes/audits start and load as separate effects; controllers refuse implicit auto-start; Ollama inventory failure cannot masquerade as successful unload or empty residency. Fixed argv/API controllers preserve local-first operation and zero-effect denial evidence. | #908 | — | Obtain a **fresh distinct** independent security review on the new exact head, then a separate R3 integrator; do not reuse the exhausted two-round reviewer or merge as builder. After #908 lands, exercise G37/ADV-133 on a verified safe copy of a populated data root. If unavailable, take G38/ADV-130's bounded orchestrator-attribute residue. Avoid #876's locked roadmap files. |
