# WorldView ingestion workers env. Copy to .env.
KAFKA_BROKERS=localhost:9092
SCHEMA_REGISTRY_URL=http://localhost:8081

# Source credentials (fill per worker; never commit real keys).
OPENSKY_USERNAME=
OPENSKY_PASSWORD=
AISSTREAM_API_KEY=
SPACETRACK_USERNAME=
SPACETRACK_PASSWORD=
