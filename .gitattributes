# Pin Markdown to LF so tools/apps that rewrite docs on Windows don't flip
# every line to CRLF and trigger whole-file diffs / git conflicts (see BUG-4).
*.md text eol=lf

# Normalize common text types; let git auto-detect the rest.
*.py text eol=lf
*.js text eol=lf
*.json text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# Keep Vite's HTML source and generated shell byte-reproducible on Windows.
frontend/index.html text eol=lf
agents/web/v2/index.html text eol=lf
