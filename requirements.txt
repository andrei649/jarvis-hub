# Cabinet v0.1.0 — Python Dependencies
#
# AUD-10 (F33): editable source. The hash-pinned requirements.lock is generated
# from this file by ./scripts/lock_deps.sh — regenerate and commit it after edits.
# Core
pyyaml>=6.0.3
apscheduler>=3.11.2
httpx>=0.28.1
python-dotenv>=1.2.2

# LLM
ollama>=0.6.2

# Web
# Pin <0.137: 0.137.0 regressed app.include_router (mounted routers add 0 routes
# -> app loses the core/routers/* domains). See requirements-beta.txt + the
# fastapi 0.137 route-introspection change handled by tests/_route_introspect.py;
# starlette pinned below the untested 1.x major. See
# docs/research/2026-06-19-fastapi-0.137-include-router-regression.md
fastapi>=0.138.0,<0.139
starlette>=1.3.1,<2.0
uvicorn>=0.49.0

# Security
cryptography>=48.0.1

# Voice (optional — install manually or use requirements-beta.txt)
# openwakeword
# faster-whisper
# kokoro-onnx

# Memory (optional — install manually or use requirements-beta.txt)
# numpy>=1.26
# qdrant-client
# neo4j
# letta
