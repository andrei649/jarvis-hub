# Cabinet Beta — dependency groups
# Install all: pip install -r requirements-beta.txt

# === Core (always required) ===
pyyaml>=6.0.3
apscheduler>=3.11.2
httpx>=0.28.1
cryptography>=49.0.0
python-dotenv>=1.2.2
ollama>=0.6.2
# Pin below 0.137: 0.137 wraps `include_router` results in an opaque _IncludedRouter
# object instead of flattening them into app.routes, so the route-parity / auth-matrix
# guards (which iterate app.routes) see only ~83 of ~296 routes. The app itself is fine
# (routes serve + appear in app.openapi()); only the introspection guards regress.
# Root cause + unblock plan (recursive flattener, then bump with a bounded starlette pin):
# docs/research/2026-06-19-fastapi-0.137-include-router-regression.md
fastapi>=0.136.3,<0.137
uvicorn>=0.49.0
psutil>=7.2.2

# === Memory (optional — improves with numpy) ===
numpy>=2.4.6

# === Token counting + HTML parsing (used by newer code; app still boots without them) ===
tiktoken>=0.13.0
beautifulsoup4>=4.15.0

# === Memory — Qdrant Vector DB (optional) ===
# Docker: docker run -p 6333:6333 qdrant/qdrant
# Then set VECTOR_BACKEND=qdrant in .env

# === Voice Pipeline (optional groups) ===

# Wake word detection
# pip install openwakeword pyaudio

# Speech-to-text (GPU-accelerated)
# pip install faster-whisper torch

# Text-to-speech (online, high quality)
# pip install edge-tts
# OR offline:
# pip install kokoro

# Audio playback
# pip install pygame

# === Knowledge Graph — Neo4j (optional) ===
# Docker: docker run -p 7474:7474 -p 7687:7687 -e NEO4J_AUTH=neo4j/password neo4j
# Then set KNOWLEDGE_GRAPH_BACKEND=neo4j in .env

# === Plugins (no extra deps — use httpx) ===
# Weather: uses wttr.in (free, no API key)
# News: uses BBC RSS feeds

# === Development ===
pytest>=9.1.0
pytest-asyncio>=1.4.0
pytest-timeout>=2.4.0
pytest-xdist>=3.8.0
