# Cabinet Beta — dependency groups
# Install all: pip install -r requirements-beta.txt
#
# AUD-10 (F33): this file is the editable source of truth. CI installs from the
# hash-pinned requirements-beta.lock with `pip install --require-hashes`. After
# editing here, run ./scripts/lock_deps.sh and commit the regenerated lock — the
# Lockfiles workflow fails the PR otherwise.

# === Core (always required) ===
pyyaml>=6.0.3
apscheduler>=3.11.3
httpx>=0.28.1
websockets>=16.1,<17
pillow>=12.3.0,<13
cryptography>=49.0.0
defusedxml>=0.7.1  # hardened parsing of untrusted RSS/Atom feeds (news + digest)
python-dotenv>=1.2.2
ollama>=0.6.2
# fastapi 0.137 wraps include_router results in an opaque _IncludedRouter instead of
# flattening them into app.routes. The app is unaffected (routes serve + appear in
# OpenAPI); the route-parity / auth-matrix guards now flatten the wrappers via
# tests/_route_introspect.iter_effective_routes. starlette is pinned below the 1.x
# major (untested with this fastapi). Background:
# docs/research/2026-06-19-fastapi-0.137-include-router-regression.md
fastapi>=0.139.0,<0.140
starlette>=1.3.1,<2.0
uvicorn>=0.51.0
psutil>=7.2.2

# === Memory (optional — improves with numpy) ===
# numpy 2.5+ dropped Python 3.11; the marker keeps the install working on 3.11 boxes
numpy>=2.5.1; python_version >= "3.12"
numpy>=2.0,<2.5; python_version < "3.12"

# === Token counting + HTML parsing (used by newer code; app still boots without them) ===
tiktoken>=0.13.0
beautifulsoup4>=4.15.0

# === Memory — Qdrant Vector DB (optional) ===
# Docker: docker run -p 6333:6333 qdrant/qdrant
# Then set VECTOR_BACKEND=qdrant in .env

# === Voice Pipeline (optional groups) ===

# Wake word detection
# pip install openwakeword pyaudio

# Speech-to-text (GPU-accelerated)
# pip install faster-whisper torch

# Text-to-speech (online, high quality)
# pip install edge-tts
# OR offline:
# pip install kokoro

# Audio playback
# pip install pygame

# === Knowledge Graph — Neo4j (optional) ===
# Docker: docker run -p 7474:7474 -p 7687:7687 -e NEO4J_AUTH=neo4j/password neo4j
# Then set KNOWLEDGE_GRAPH_BACKEND=neo4j in .env

# === Plugins (no extra deps — use httpx) ===
# Weather: uses wttr.in (free, no API key)
# News: uses BBC RSS feeds

# === Development ===
pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-timeout>=2.4.0
pytest-xdist>=3.8.0
# AUD-10 (F37): fail fast on a stray real network call in a test instead of
# hanging to the --timeout backstop. pytest.ini restricts connect() to loopback;
# a real outbound connect raises SocketConnectBlockedError immediately.
pytest-socket>=0.8.0
